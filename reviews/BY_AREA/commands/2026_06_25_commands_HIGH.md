# TUI Review — area `commands` — 2026-06-25

## Executive Summary
20 findings. Severity: CRITICAL 0, HIGH 7, MEDIUM 5, LOW 8. Buckets: AUTO_FIXABLE 6, FLOW_FIX 2, LOG 12.

## Risk Assessment
Status **HIGH**.

## Findings
#### T-004 — [HIGH/AUTO_FIXABLE] `internal/cli/cmd_init.go:115` (keyboard-nav)

- **Problem:** The footer hint printed at the top of `abysslink init` advertises keybindings that the underlying huh v1.0.0 forms do NOT implement. It says "↑/↓ navigate  •  space toggle  •  enter select  •  esc back  •  ctrl+c quit". Verified against the pinned huh keymap (GOMODCACHE huh@v1.0.0/keymap.go NewDefaultKeyMap and form.go:556-563): (1) "esc back" is wrong — the back binding is shift+tab for every field (Input.Prev/Select.Prev/Confirm.Prev/Note.Prev = shift+tab, keymap.go:112,133,140,156,173,178); esc is bound only to filter set/clear and is WithDisabled() by default (keymap.go:148-149,163-164), so on the Input/Confirm/Select widgets the wizard actually uses, esc is a no-op. (2) esc does NOT quit either — form.go:558 aborts only on key.Matches(msg, f.keymap.Quit) and Quit = ctrl+c only (keymap.go:109). (3) "space toggle" is wrong — space (" ") is bound to Toggle ONLY on MultiSelect (keymap.go:159); the init flow uses Select/Input/Confirm exclusively (flow/steps.go, cmd_init.go:1035-1110) with ZERO MultiSelect fields, and the Confirm widget toggles via h/l/←/→ (keymap.go:181), not space. So two of the five advertised gestures (esc back, space toggle) do nothing in the actual wizard.
- **Impact:** A user reads "esc back" and presses Esc to return to a previous stage after a mistake — nothing happens, they are stuck on the current field with no advertised way back (the real key, shift+tab, is never shown). A user presses Space expecting to toggle a Yes/No confirm — nothing happens. The one piece of keyboard documentation the product offers is actively misleading, eroding trust in the first 60 seconds of the first-run experience.
- **Before (rendered):** Top of the `abysslink init` screen: the ABYSS/LINK figlet banner, then a single muted-steel line reading "↑/↓ navigate  •  space toggle  •  enter select  •  esc back  •  ctrl+c quit", then the first bordered huh group (Backend type select). Pressing Esc or Space on that select produces no visible state change.
- **Fix:** Correct the hint to match huh v1.0.0's real keymap and the widgets actually used: e.g. "↑/↓ navigate  •  ←/→ or y/n toggle  •  enter confirm  •  shift+tab back  •  ctrl+c quit". Drop "space toggle" (no MultiSelect in the flow) and replace "esc back" with "shift+tab back". If esc-to-go-back is genuinely desired, pass a customized *huh.KeyMap via form.WithKeyMap() that adds esc to the Prev bindings, rather than documenting a binding that does not exist.
- **Fix steps:** In internal/cli/cmd_init.go:116 change the styleFooterHint string to match the real huh v1.0.0 keymap and the widgets used: "↑/↓ navigate  •  ←/→ or y/n toggle  •  enter confirm  •  shift+tab back  •  ctrl+c quit". Update the matching descriptive comment in internal/cli/styles.go:100. Run `make lint test`.
- **Confidence:** high  ·  verify 2/2

#### T-005 — [HIGH/LOG] `internal/cli/cmd_init.go:108` (info-arch)

- **Problem:** The 8-stage init wizard (StepAccount through StepDone) renders each huh form with NO indication of which stage the user is on or how many remain. A purpose-built progress strip, tui.JourneyHeader (internal/tui/header.go:42), exists and renders exactly 'Abysslink Setup — Stage N of M' with a dot-row (✓/●/·) and bracketed labels '[1] Account [2] Prerequisites …', but a repo-wide grep shows it is never invoked from any production code path — only its own definition and tests reference it. The init RunE prints a one-time banner + static footer hint, then runs eight visually-identical forms with no breadcrumb, counter, or stage label.
- **Impact:** During the longest, highest-stakes flow in the product (account → prereqs → converge → lock → enroll → verify → ACL → done), the user has no sense of place: 'how many more questions?', 'which step is this?', 'did I already do the lock step?'. After Ctrl-C and --resume, the resume message says 'Resuming from stage 4' but the forms themselves never echo a stage number, so the user can't correlate. This is the classic 'no indication of current screen/mode' failure for a multi-step wizard.
- **Before (rendered):** A terminal showing the ABYSSLINK FIGlet banner, one muted footer line of key hints, then a single bordered cyan box containing one question (e.g. 'Backend type' with three options). No 'Stage 1 of 8', no dot progress row, no breadcrumb anywhere. Pressing enter replaces it with another identical-looking bordered box ('Account email') — again with no stage marker. Eight screens in a row, indistinguishable as a sequence.
- **Fix:** Call tui.JourneyHeader before each step in runFlowSteps (internal/cli/cmd_init.go:237-263): pass the 1-based stage index, total=8, and a labels slice ([]string{"Account","Prerequisites","Converge","Lock","Enroll","Verify","ACL","Done"}), printing it via printerInfo(p, ...) immediately before running the step's form. This wires the already-built component and gives every wizard screen a 'Stage N of 8' breadcrumb. If JourneyHeader is intentionally dead, delete it; either way the wizard must show stage context.
- **Confidence:** high  ·  verify 2/2

#### T-006 — [HIGH/LOG] `internal/cli/cmd_init.go:275` (dead-handlers)

- **Problem:** The Stage 2 Converge step (flow.StepConverge, steps.go:154-171) builds a huh.Confirm with Affirmative("Yes, run it") / Negative("No, I'll run it later") bound to a local `runNow` that the step discards (`_ = runNow`, steps.go:169). The caller runFlowStep (cmd_init.go:275-306) runs the form purely for display, then unconditionally persists the stage counter (cmd_init.go:304) and continues. It NEVER reads the user's Yes/No and NEVER invokes `abysslink up --apply`. Proof of non-reachability: runFlowStep takes the *huh.Form but inspects only `i` (step index) and `state.BackendType`/`state.HaveAuthCode` (cmd_init.go:277-301); it has no branch on the confirm result, and grep shows StageConvergeDone (the documented sink, steps.go:168) has zero writers/readers repo-wide (only state.go:77 defines it).
- **Impact:** A user who selects "Yes, run it" at the Converge stage gets nothing — `up --apply` is never run, no convergence happens, no error, no feedback. The wizard claims to offer to converge the system and silently no-ops. This is a no-op menu action on a load-bearing setup step; users believe their rig was converged when it was not.
- **Before (rendered):** Stage 2 of the setup journey shows a confirm prompt 'Run `abysslink up --apply` now?' with a highlighted 'Yes, run it' button; pressing Enter on Yes advances straight to Stage 3 with no command output, no spinner, no convergence — identical behavior to choosing 'No'.
- **Fix:** Have flow.StepConverge expose the confirm result to the caller (e.g. write state.StageConvergeDone, which already exists in FlowState), then in runFlowStep branch on it: if the user confirmed, invoke the up command (call the up RunE / a shared runUp helper) before persisting the stage. Either wire it end-to-end or remove the confirm and replace it with a non-interactive Note that tells the user to run `abysslink up --apply` next (the summary at cmd_init.go:251-254 already says this).
- **Confidence:** high  ·  verify 2/2

#### T-008 — [HIGH/LOG] `internal/cli/cmd_init.go:224` (state-feedback)

- **Problem:** runFlowSteps drives 8 huh form stages in a loop but never renders any stage/progress indicator. A purpose-built renderer for exactly this — tui.JourneyHeader (internal/tui/header.go) producing 'Abysslink Setup — Stage N of M' with a ✓/●/· dot row — exists and is fully implemented, but grep confirms it is invoked nowhere in production (only header.go and tests reference it). init prints only a one-time static banner and a footer hint (cmd_init.go:109-116) before entering the loop; each stage form renders with no 'Stage 3 of 8' context.
- **Impact:** During a multi-minute, 8-step setup the user has no sense of where they are or how much remains. Each huh form looks identical (same Abyss-themed bordered box), so the user cannot tell if they are 2 stages in or 7, cannot tell that the wizard is making forward progress, and on resume has no visual confirmation of which stages were already completed. The product ships a dead progress component that would have solved this.
- **Before (rendered):** A single Abyss-themed bordered confirm/select box centered on screen (e.g. 'Backend type' select), with a faint footer hint line below it ('↑/↓ navigate • space toggle • enter select • esc back • ctrl+c quit'). No header strip, no '3/8', no dot row — nothing indicating this is one step in an 8-stage journey.
- **Fix:** Invoke tui.JourneyHeader before each step in runFlowSteps: printerInfo(p, tui.JourneyHeader(i+1, len(steps), flow.StageLabels())) so the user sees 'Abysslink Setup — Stage N of 8' with completed (✓), current (●), and remaining (·) dots above every form. It already has NO_COLOR/ASCII fallbacks built in.
- **Confidence:** high  ·  verify 2/2

#### T-010 — [HIGH/LOG] `internal/cli/cmd_init.go:333` (missing-components)

- **Problem:** runAccountStepWithBrowserCallback presents a real-looking OAuth authorization flow for headscale/netbird backends: it generates a PKCE verifier, prints 'Browser opened → …' / 'Waiting for OAuth callback (5 min timeout)…', opens the system browser, and blocks on browser.ListenCallback for up to 5 minutes. But the authorization URL is HARDCODED to placeholder hosts — 'https://headscale.example.com/oauth/authorize' (line 346) and 'https://nb.example.com/oauth/authorize' (line 348) — never the user's real server URL. The real URL collected earlier (cmd_init.go:883/887, cfg.Server.Headscale/NetBird.ServerURL) lives in a separate initFormResult and is unreachable here; the Account step's FlowState only carries BackendType+Email, not the server URL. Even if a callback arrived, the received auth code is discarded (`_ = code` at line 387 with comment 'would be: keychain.Store(...)').
- **Impact:** A self-hosted (Headscale/NetBird) user during `abysslink init` is shown a fully-staged OAuth ceremony that cannot complete: the browser opens to a non-existent example.com host, and the wizard blocks for up to 5 minutes waiting for a callback that can never come, then errors with 'init: OAuth callback: …' (or times out). The feature is presented as functional but is a stub seam — and the code path that would store the result is a no-op. This is a non-functional action masquerading as a working one on a load-bearing setup step.
- **Before (rendered):** Selecting 'Headscale (self-hosted)' as the backend in the init Account stage, the terminal prints 'Browser opened → http://127.0.0.1:PORT/callback' and 'Waiting for OAuth callback (5 min timeout)…', a browser tab opens to https://headscale.example.com/oauth/authorize?... which fails to resolve, and the CLI hangs for five minutes before failing — no credential is ever obtained or stored.
- **Fix:** Either (a) thread the real server URL into this step (carry cfg.Server.Headscale/NetBird.ServerURL or a BackendServerURL field on FlowState — note FlowState's D-12 no-secret rule still permits a URL) and build authURL from it, and actually consume the returned code (wire the keychain store instead of `_ = code`); or (b) if OAuth is not implemented for v3, do not run the callback loop at all for self-hosted backends — skip it (as is already done for tailscale at lines 329-331) and print a manual instruction instead. Do not open a browser to example.com and block on a callback that cannot arrive.
- **Confidence:** high  ·  verify 2/2

#### T-011 — [HIGH/FLOW_FIX] `internal/cli/cmd_init.go:237` (error-paths)

- **Problem:** When the user presses Ctrl-C / Esc to abort a huh form mid-wizard, runFlowStep catches huh.ErrUserAborted, prints "Aborted." and returns nil (cmd_init.go:283-289 interactive path, and :318-324 in the Account/browser-callback path). The driver loop runFlowSteps only stops on a non-nil error (`if err := runFlowStep(...); err != nil { return err }`, line 260-262). A nil return means the for-loop at line 237 simply CONTINUES to the next stage and runs the next form. So aborting at, say, the Converge stage does NOT exit the wizard — it marches the user through Lock, Enroll, Verify, ACL, Done forms one after another, each printing "Aborted." as the user keeps hitting Esc. Worse: runFlowStep unconditionally calls flow.WriteFlowState(stateFile, i+1) at line 304 AFTER an abort, so the aborted stage is persisted as COMPLETED in journey-state.json — a later `init --resume` skips the stage the user never actually finished.
- **Impact:** The single most common 'get me out of here' gesture (Ctrl-C / Esc) does not work mid-wizard: instead of exiting, abysslink walks the user through every remaining setup form. The user must abort 6+ forms in a row to escape. Resume state is also corrupted — aborted stages are marked done, so --resume silently skips real configuration steps. This is a TUI that wedges the user into the flow rather than letting them out.
- **Before (rendered):** User on the 'Run `abysslink lock init --apply` now?' confirm presses Esc expecting to quit. Terminal prints 'Aborted.' then immediately renders the next form 'Enable Tailscale SSH?'. Esc again → 'Aborted.' → 'ntfy listen port' input. The wizard refuses to release the user; only repeated aborts (or killing the process) escape it.
- **Fix:** Introduce an abort sentinel: have runFlowStep return a distinct sentinel (e.g. errWizardAborted) instead of nil when huh.ErrUserAborted is seen, and in runFlowSteps break/return cleanly on it (errors.Is(err, errWizardAborted) -> print a single 'Setup cancelled.' and return nil). Do NOT advance journey state (skip WriteFlowState) on the abort path so --resume reflects the true last-completed stage. Apply to both the interactive branch (line 283) and the Account/browser-callback branch (line 318).
- **Fix steps:** In internal/cli/cmd_init.go define a package sentinel `var errWizardAborted = errors.New("wizard aborted")`. In runFlowStep change the interactive abort branch (line 284-287) to print 'Setup cancelled.' and `return errWizardAborted` (do NOT fall through to WriteFlowState at line 304 — return before it). Mirror in runAccountStepWithBrowserCallback (line 319-322). In runFlowSteps (line 260-262) change the error handling to: `if err := runFlowStep(...); err != nil { if errors.Is(err, errWizardAborted) { return nil }; return err }`. Restructure runFlowStep so the abort path returns before persisting state (move the WriteFlowState call so it only runs on the success path). Add a test in cmd_init_test.go asserting that an aborted stage does not advance journey-state.json and the loop stops. Run `make lint test`.
- **Confidence:** high  ·  verify 2/2

#### T-003 — [HIGH/AUTO_FIXABLE] `internal/cli/styles.go:48` (color-contrast)

- **Problem:** styleCode renders ColorInfo (#5C7CFA blue) foreground on a ColorDim (#4B5563 grey) background with Padding(0,1). Measured contrast of #5C7CFA on #4B5563 is 2.06:1 — fails WCAG AA badly regardless of terminal theme, because both fore and background are explicitly set so the actual terminal background is irrelevant. The identical pairing is mirrored in ui.StyleCode (theme.go:95) and in the glamour Code/CodeBlock style (glamour.go:117-118,124-125, #5C7CFA on #4B5563). styleCode is used heavily: every doctor remediation hint ('fix: ...' at cmd_doctor.go:620,630), status 'not initialised' hints (cmd_status.go:315,326), and the up success/next-step command snippets (cmd_up.go:919,924).
- **Impact:** The command snippets the user is supposed to copy/run (e.g. `abysslink init`, the `fix:` remediation commands in doctor, the connect command in `up`) are low-contrast blue-on-grey that is hard to read on any terminal — the exact text the user most needs to act on is the least legible. On a 256-color or low-quality phone SSH render the snippet can be nearly unreadable.
- **Before (rendered):** abysslink doctor failing-check line: under the red FATAL row, an indented 'fix: sudo systemctl ...' pill renders as muted periwinkle-blue text on a flat dark-grey block; the letters have minimal separation from their background, reading as a smudged bar rather than crisp text.
- **Fix:** Increase contrast of the code pill. Either lighten the foreground (use ColorFg #F8F8F2 on #4B5563 = ~7.5:1) keeping the blue as an accent border instead, or darken the background substantially. Simplest: drop the background and render code as bold ColorInfo on the terminal default, or use a near-black background (#1e1e1e) with the existing blue (#5C7CFA on #1e1e1e = 4.54:1, marginal — prefer brightening the text). Apply the same change to ui.StyleCode (theme.go:95) and the glamour Code/CodeBlock fg in glamour.go to keep the single-source-of-color contract.
- **Fix steps:** In internal/ui/theme.go:95 change StyleCode foreground from ColorInfo to ColorFg (keep Background(ColorDim).Padding(0,1)). In internal/cli/styles.go:48 change styleCode foreground from colorBlue to ui.ColorFg (add the var if needed). In internal/ui/glamour.go set Code.Color and CodeBlock.Color (lines 117,124) from ptr("#5C7CFA") to ptr("#F8F8F2"), leaving BackgroundColor #4B5563. Delete and regenerate the affected golden fixtures in internal/cli/testdata and internal/tui/testdata, then `make lint test`.
- **Confidence:** high  ·  verify 2/2

#### T-029 — [MEDIUM/LOG] `internal/cli/cmd_enroll.go:615` (state-feedback)

- **Problem:** enrollWithAdminKey prints a single static line 'Waiting for the phone to join the tailnet (up to 2 minutes)...' (cmd_enroll.go:623) then enters a silent 2-minute poll loop (3s ticks calling admin.Devices) that emits no further output until success or timeout. derr from admin.Devices(ctx) is swallowed with `continue` (line 633-634), so if the admin API is down for the whole window the user sees only the static 'Waiting…' line for 2 full minutes and then a generic timeout, with no hint that every poll actually errored. (The same static-wait pattern appears at cmd_init.go:377 'Waiting for OAuth callback (5 min timeout)...' which blocks up to 5 minutes with no progress.)
- **Impact:** For up to two minutes (five for OAuth) the screen is frozen on a single 'Waiting…' line with no spinner, no elapsed time, and no per-poll heartbeat. The user cannot tell whether the tool is polling, whether the API is erroring, or whether it has silently hung — during the highest-anxiety moment of pairing (did my phone connect or not?). A repeatedly-failing Devices() call produces an identical idle-looking screen to a healthy 'phone hasn't joined yet' wait.
- **Before (rendered):** Two static lines: '2. In the Tailscale app choose "Sign in with auth key" and scan:' above an ANSI QR block, then 'Waiting for the phone to join the tailnet (up to 2 minutes)...' which then stays on screen unchanged for up to 120 seconds before either a green 'Phone joined: …' or a yellow timeout warning appears.
- **Fix:** Show liveness during the poll: print a periodic heartbeat (e.g. an animated dot or 'still waiting (Ns elapsed)…' every ~15s) or drive the wait via tui.RunSpinner. Surface a degraded signal when admin.Devices keeps erroring (e.g. after N consecutive failures print one 'admin API not responding — still retrying' line) instead of silently `continue`-ing for the entire window.
- **Confidence:** high  ·  verify None/0

#### T-038 — [MEDIUM/AUTO_FIXABLE] `internal/cli/cmd_init.go:119` (broken-flows)

- **Problem:** The resume banner prints "Resuming from stage N. Run with --no-resume to start over." (cmd_init.go:120). Only a `--resume` bool flag is registered (cmd_init.go:142); there is no `--no-resume` flag. Cobra does not auto-generate `--no-<name>` negations for ordinary bool flags, so `abysslink init --no-resume` exits with "unknown flag: --no-resume". The actual way to start over is to run `abysslink init` WITHOUT `--resume` (which forces startStage=0 at cmd_init.go:104-106).
- **Impact:** A user following the tool's own printed instruction to start over hits an "unknown flag" error and a non-zero exit — a literal dead-end created by the wizard's own copy. They are left without a working path to reset unless they guess that omitting --resume is the answer.
- **Before (rendered):** After `abysslink init --resume`, the screen reads: "✓  Resuming from stage 3. Run with --no-resume to start over." User runs `abysslink init --no-resume` and gets: "Error: unknown flag: --no-resume" with usage text and exit code 1.
- **Fix:** Change the hint to the correct instruction, e.g. "Run `abysslink init` (without --resume) to start over." Alternatively register a real `--no-resume` flag, but the cleaner fix is to correct the copy since omitting --resume already resets.
- **Fix steps:** In internal/cli/cmd_init.go:120 change the resume-hint string from "Run with --no-resume to start over." to "Run `abysslink init` (without --resume) to start over." Run `make lint test`.
- **Confidence:** high  ·  verify None/0

#### T-039 — [MEDIUM/LOG] `internal/cli/cmd_init.go:250` (broken-flows)

- **Problem:** After the 8-stage flow, StepDone renders a glamour summary that says "Your rig is ready." and shows a `mosh <your-rig> -- tmux new -A -s main` connect command, then "Run `abysslink up --apply` to converge and `abysslink doctor` to verify." Because the Converge/Lock/Verify/ACL confirms are no-ops (see CRITICAL finding), the rig is NOT actually converged at this point, and the summary uses the literal placeholder `<your-rig>` instead of the hostname the user just entered (cmd_init.go:251-254 hardcodes the markdown string with no hostname interpolation, even though Hostname was collected).
- **Impact:** The terminal screen of onboarding both over-promises ("Your rig is ready") and under-delivers (tells the user to still run up/doctor), which is internally contradictory, and hands the user an un-runnable connect command with a literal `<your-rig>` token instead of their real hostname. The user does not know whether setup actually finished or what host to connect to.
- **Before (rendered):** Final panel: a cyan-bordered glamour box titled "Setup Complete" — "Your rig is ready. Connect from your phone:" followed by a code block literally reading `mosh <your-rig> -- tmux new -A -s main`, then a blockquote "Run `abysslink up --apply` to converge and `abysslink doctor` to verify."
- **Fix:** Interpolate the real hostname (from config or FlowState) into the connect command like printSuccessSummary does (cmd_up.go:898-919), and make the wording consistent with what actually ran: if the converge stage is wired up, say setup completed; if it is intentionally deferred, say "Almost done — run `abysslink up --apply` next" rather than "Your rig is ready."
- **Confidence:** high  ·  verify None/0

#### T-041 — [MEDIUM/FLOW_FIX] `internal/cli/cmd_init.go:283` (error-paths)

- **Problem:** runFlowStep (line 284) and runAccountStepWithBrowserCallback (line 319) detect a user abort with `runErr == huh.ErrUserAborted` (exact equality). huh v1.0.0 only returns the bare ErrUserAborted when the abort comes from a key event inside the program (form.go:690). The forms run via form.WithTheme(...).RunWithContext(ctx) where ctx is the SIGINT/SIGTERM signal.NotifyContext built in cmd/abysslink/main.go:49 and threaded as the program's external context. If that context is cancelled (real OS signal: `kill -INT`/`kill -TERM`, a SIGINT delivered while not in raw mode, or a parent process tearing the group down), bubbletea v1.3.10 returns ErrProgramKilled wrapping context.Canceled (tea.go:727), and huh maps ErrProgramKilled to **ErrTimeout** (form.go:693-694), NOT ErrUserAborted. The `== huh.ErrUserAborted` check is then false, so control falls through to line 288/323 `return fmt.Errorf("init: step %d: %w", i, runErr)` — surfacing 'init: step 2: timeout' for what was actually a signal/abort.
- **Impact:** A signal-driven interruption of the init wizard (SIGTERM from a process manager, kill -INT, or a SIGINT not consumed by raw-mode key handling) is reported to the user as a spurious 'timeout' error with a non-zero exit, instead of a clean cancellation. Misleading diagnostics during the most security-sensitive command, and a non-clean exit code on what should be a user-cancel.
- **Before (rendered):** Operator runs `abysslink init`, then from another shell `kill -INT <pid>` (or a CI/process-manager sends SIGTERM). Instead of 'Aborted.' the terminal shows 'Error: init: step 0: timeout' and exits non-zero.
- **Fix:** Replace the exact-equality checks with errors.Is and also treat the timeout/killed mapping as a cancel when ctx is done: e.g. `if errors.Is(runErr, huh.ErrUserAborted) || (errors.Is(runErr, huh.ErrTimeout) && ctx.Err() != nil) { print Aborted; return clean }`. Equivalently, check ctx.Err() first and short-circuit to a clean cancel before classifying the huh error. Mirror the same fix in both runFlowStep and runAccountStepWithBrowserCallback (and the gallery, which already uses errors.Is but does not account for the ErrTimeout-on-cancel mapping).
- **Fix steps:** In internal/cli/cmd_init.go runFlowStep (line ~284) and runAccountStepWithBrowserCallback (line ~319) replace `if runErr == huh.ErrUserAborted {` with a cancel-aware check: `if errors.Is(runErr, huh.ErrUserAborted) || ctx.Err() != nil || errors.Is(runErr, huh.ErrTimeout) && ctx.Err() != nil {` — simplest correct form: check `if ctx.Err() != nil || errors.Is(runErr, huh.ErrUserAborted) { <clean abort> }` BEFORE the generic `return fmt.Errorf(...)`. Ensure `errors` is imported. Align the clean-abort return with finding 10's errWizardAborted sentinel so the two fixes compose (abort short-circuits before WriteFlowState). Add a test injecting a cancelled context that asserts a clean cancel (not a 'timeout' error). Apply the same to the gallery if a cancel-mapping gap exists there (cmd_gallery.go:75 already uses errors.Is for ErrUserAborted; add the ctx.Err() guard). Run `make lint test`.
- **Confidence:** medium  ·  verify None/0

#### T-014 — [MEDIUM/AUTO_FIXABLE] `internal/cli/cmd_up.go:459` (layout)

- **Problem:** Horizontal rule separators are drawn with a hardcoded length: cmd_up.go:459 `strings.Repeat("─", 48)` (applying divider) and cmd_up.go:883 `strings.Repeat("─", 48)` (between next-steps entries). These ignore terminal width entirely. The same anti-pattern is in cmd_doctor.go:604 and :731 with `strings.Repeat("─", 50)`. 48/50-column rules are wider than a narrow phone terminal and narrower than a wide desktop terminal, so they never match the surrounding box width (boxWidth() = min(54, termwidth-2)) nor the viewport.
- **Impact:** On phones the 48/50-char rules soft-wrap onto a second line (a short dangling '──' fragment under the rule); on wide terminals they look stubby and disconnected from the content. Either way the dividers do not line up with the boxes/content they separate.
- **Before (rendered):** On a 40-column terminal: a line of 40 dashes followed on the next wrapped line by 8 more dashes, producing a broken two-line divider. Inside the green/yellow boxes, the inter-step rule is shorter than the box interior, floating with uneven margins.
- **Fix:** Compute rule length from the current box interior width (boxWidth()-padding) or from terminalWidth(), clamped to the viewport, instead of a literal 48/50. A single helper (e.g. hrule(width)) reused across cmd_up.go and cmd_doctor.go keeps it consistent.
- **Fix steps:** Add a helper in internal/cli/styles.go: `func hrule() string { return strings.Repeat("─", boxWidth()-2) }` (interior width = box width minus the two padding/border columns; clamp to >=1). Replace strings.Repeat("─", 48) at cmd_up.go:459 and :883 and strings.Repeat("─", 50) at cmd_doctor.go:604 and :731 with hrule(). For the ASCII/non-TTY path mirror boxBorder()'s glyph choice if a golden requires it. Delete/regenerate affected goldens, run `make lint test`.
- **Confidence:** high  ·  verify None/0

#### T-045 — [LOW/LOG] `internal/cli/cmd_approve.go:398` (state-mgmt)

- **Problem:** promptTTY spawns an inner goroutine that calls scanner.Scan() on /dev/tty (lines 400-406) and reports via a buffered doneCh. The outer select (lines 409-413) returns on ctx.Done() (the approval timeout) without unblocking the scanner goroutine. scanner.Scan() is a blocking read on /dev/tty with no deadline, so on a timeout (or when the phone arm wins the CAS and the user never types) the goroutine stays parked until the user presses Enter — which may be never. doneCh is buffered (size 1) so the eventual send does not block, but the goroutine and its open /dev/tty handle are leaked for the rest of the process lifetime.
- **Impact:** A goroutine leak per timed-out interactive approval. Bounded in practice because approve runs as a short-lived Claude Code hook process that exits right after the decision, so the OS reclaims it. Still a textbook 'goroutine blocked on uncancellable read after ctx fired' pattern; in any long-lived host of this code it would accumulate.
- **Before (rendered):** Not visually observable. Behaviorally: an approval prompt appears on the controlling terminal, the 120s/10s timeout elapses (or the phone approves first), the CLI exits the prompt — but a background reader on /dev/tty remains parked until the process ends.
- **Fix:** Make the tty read cancellable: set a read deadline on the *os.File before scanning, or close the tty handle on ctx.Done() to unblock scanner.Scan(), so the inner goroutine exits when promptTTY returns. Alternatively document explicitly that this leak is acceptable because the hook process exits immediately.
- **Confidence:** medium  ·  verify None/0

#### T-036 — [LOW/LOG] `internal/cli/cmd_gallery.go:58` (dead-handlers)

- **Problem:** The hidden gallery command renders a sample huh.Select bound to a local `choice` (cmd_gallery.go:59,68) but `choice` is never read after the form runs — the selection has no effect. The form exists only to visually demonstrate the focused/blurred theme states.
- **Impact:** Minimal — this is an intentional theme-preview screen with documented no-mutation constraints (D-15), so a no-op selection is by design. Flagged for completeness: a viewer's choice (Tailscale vs Headscale) does nothing, which is acceptable here but is technically a discarded handler.
- **Before (rendered):** Gallery shows a 'Abyss theme preview' select with Tailscale/Headscale options; choosing either then pressing Enter simply proceeds to the glamour markdown sample with no behavioral difference.
- **Fix:** Acceptable as-is given the gallery's preview-only contract; optionally add a `_ = choice` or a comment clarifying the selection is intentionally inert so it isn't mistaken for a wiring gap.
- **Confidence:** high  ·  verify None/0

#### T-044 — [LOW/AUTO_FIXABLE] `internal/cli/cmd_gallery.go:55` (error-paths)

- **Problem:** The gallery's doc (line 35) and the inline comment (line 56-57) claim the sample huh form is 'skipped in headless / non-TTY (CI, --json, pipe)'. But line 58 calls interactive(false, false) with both yes and jsonOut HARD-CODED to false, instead of reading the command's actual --json/--yes flags. interactive() therefore only checks stdinIsTTY(); under `abysslink --json gallery` (or `gallery --yes`) with a TTY stdin, the interactive form STILL runs, contradicting the documented contract that --json is non-interactive. The form's own error handling (WR-05, line 75-77) is correct, but the gate that should suppress the form under --json is bypassed.
- **Impact:** Low blast radius (gallery is hidden, has no mutations/network/keychain, and aborts cleanly to exit 0), but `--json gallery` would emit ANSI form chrome into what is supposed to be a machine-readable stream, and the form blocks waiting for input in a context the code documents as non-interactive. It is a latent contract violation that could surprise anyone scripting against the gallery for theme snapshots.
- **Before (rendered):** `abysslink --json gallery` on an interactive terminal: instead of emitting only JSON records and exiting, it renders the styled 'Abyss theme preview' select form and blocks for a keypress.
- **Fix:** Read the real flags: `jsonOut, _ := cmd.Flags().GetBool("json"); yes, _ := cmd.Flags().GetBool("yes")` and call `if interactive(yes, jsonOut)`. This makes the runtime behavior match the documented 'skipped under --json / headless' contract.
- **Fix steps:** In internal/cli/cmd_gallery.go RunE, before the form block read the real flags: `jsonOut, _ := cmd.Flags().GetBool("json")` and `yes, _ := cmd.Flags().GetBool("yes")` (use the same flag-resolution helper other commands use if one exists), then change line 58 to `if interactive(yes, jsonOut) {`. Also pass the real jsonOut to RenderBanner/RenderMarkdown color computation if it currently relies on colorEnabled() only (verify consistency). Run `make lint test`.
- **Confidence:** high  ·  verify None/0

#### T-015 — [LOW/LOG] `internal/cli/cmd_init.go:1150` (layout)

- **Problem:** previewAndConfirmConfig prints each YAML line as `"  " + styleCode.Render(line)`. styleCode applies a background color plus Padding(0,1) (styles.go:48). Long YAML values (e.g. a headscale https URL, a long email) plus the 2-space indent plus the 1-col padding on each side can exceed a narrow terminal width; because the span has a background fill, the soft-wrap continuation starts a new background block at column 0, producing a ragged two-tone band rather than a clean code line.
- **Impact:** On narrow terminals the config preview shows YAML lines with background color bleeding to the screen edge and wrapping into a misaligned colored continuation row, making the preview harder to scan before the user confirms a config write.
- **Before (rendered):** A list of indented code-styled YAML lines on a dim background; one long 'server_url: https://...' line wraps, and the wrapped remainder appears as a second background-filled stripe flush to the left margin, breaking the indented column alignment.
- **Fix:** For block content like a multi-line YAML preview, avoid per-line background+padding styling; render the block in a single bordered/dim box (lipgloss with a terminal-aware Width and soft-wrap) or strip the background for code blocks and rely on indentation, so wrapped lines stay aligned.
- **Confidence:** medium  ·  verify None/0

#### T-043 — [LOW/AUTO_FIXABLE] `internal/cli/cmd_status.go:232` (error-paths)

- **Problem:** b.Status(ctx) is called at line 236 and the error tsErr is used ONLY as a guard (`if tsErr == nil`). When tsErr is non-nil — tailscaled socket unreachable, backend RPC error, permission failure — the code leaves tsRunning at its default literal 'not running' (line 232) and never inspects or surfaces tsErr. A genuine error (daemon crashed, socket perms, API failure) is rendered to the user as the same 'not running' row as a cleanly-stopped daemon.
- **Impact:** The status dashboard conflates 'Tailscale is stopped' with 'querying Tailscale errored'. A user whose tailscaled is wedged or whose socket is inaccessible sees a benign-looking red 'not running' and is steered toward `tailscale up` rather than toward the real fault. The underlying error is neither logged (no slog) nor shown, so it is unrecoverable from the user's view.
- **Before (rendered):** tailscaled socket exists but returns a permission/RPC error. `abysslink status` shows 'Tailscale  not running' identical to a machine where the daemon was never started — masking the distinct failure mode.
- **Fix:** Distinguish the error case: when tsErr != nil, render a distinct value (e.g. 'unknown — could not query tailscaled') and/or slog.Debug the error so `-v` surfaces it. Reserve 'not running' for a successful query returning a stopped backend state. At minimum log tsErr instead of dropping it on the floor.
- **Fix steps:** In internal/cli/cmd_status.go, after `st, tsErr := b.Status(ctx)` add an else branch: `} else { tsRunning = "unknown"; slog.Debug("status: backend query failed", "err", tsErr) }` (keep tsErr otherwise unused). Map the new 'unknown' value through statusRowStateFor so it renders rowBad (it already falls to default rowBad). Ensure `log/slog` is imported. Re-bless status goldens in internal/cli/testdata and run `make lint test`. Note: 'unknown' must NOT be classified as 'disabled'/'running'/'encrypted' so it shows as a problem, not a neutral state.
- **Confidence:** high  ·  verify None/0

#### T-030 — [LOW/LOG] `internal/cli/cmd_up.go:431` (state-feedback)

- **Problem:** runApply builds the ConfirmBlast summary string only from sudoLines (cmd_up.go:432-441). When the planned changes require no sudo (e.g. a plan that is purely a 'tailscale login' interactive step, or pure file-writes), `summary` is the empty string, and ConfirmBlast renders huh.NewNote().Title("") above the 'Apply N change(s)?' confirm (tui/forms.go:174-176). The blast prompt therefore tells the user a count ('Apply 1 change(s)?') but shows no detail about WHAT the change is, even though printPlanDetail ran earlier and uniqueActions is available.
- **Impact:** At the final, last-chance mutation gate the user is asked to approve 'N changes' with an empty detail box for any plan that doesn't happen to include a sudo keyword. The most informative moment (the irreversible go/no-go) shows the least context, forcing the user to scroll back up to the earlier plan output to recall what they are approving.
- **Before (rendered):** A bordered confirm dialog with an empty Note region (blank line) directly above the bold title 'Apply 1 change(s)? This will modify your system.' and [Yes]/[No] options — no list of which module or what action is about to run.
- **Fix:** Always populate the ConfirmBlast summary with the per-action lines (module + description from uniqueActions), not just the sudo subset, so the blast gate restates exactly what will change. Keep the 'sudo required for:' block as an additional emphasized section when present.
- **Confidence:** medium  ·  verify None/0

#### T-026 — [LOW/LOG] `internal/cli/styles.go:80` (resize)

- **Problem:** styleHeaderBox, styleNextStepBox and styleStatusBox are package-level vars that evaluate boxWidth() and boxBorder() exactly ONCE at package initialization (process start), then are reused for every render (cmd_status.go:525, cmd_up.go:199/202/888). The width is captured before the command runs and never re-read.
- **Impact:** For these one-shot CLI commands a single render at the start-of-process width is usually fine, but if the terminal is resized between process start and the box being printed (or in any future long-lived reuse), the boxes use a stale width and can overflow/underflow the current terminal. It also means tests/imports that trigger package init at an unknown width bake that width into the style. Low impact today because each command prints once, but it is a latent resize correctness bug if these styles are ever reused across an interactive session.
- **Before (rendered):** Not visibly broken in the common path; the latent case is a status box rendered at 80 cols of width on a terminal the user shrank to 50 cols after launching, where the right border sits beyond the viewport and wraps.
- **Fix:** Make these functions (or call boxWidth()/boxBorder() at render time inside a helper) rather than package-level vars, so each render reflects the current terminal width. e.g. styleStatusBox() lipgloss.Style returning a freshly-sized style.
- **Confidence:** medium  ·  verify None/0

#### T-031 — [LOW/LOG] `internal/cli/styles.go:106` (info-arch)

- **Problem:** The 'healthy/good' state uses two different glyphs depending on the screen. The status panel renders healthy rows with iconOK='●' (filled dot) via iconOKStr (styles.go:107,122; used at cmd_status.go:471). Every other surface — init prereqs table, doctor 'ok' findings and pass line, up scan/apply 'done' rows, verify, panic — uses iconDone='✓' (checkmark) via iconDoneStr. So 'this is fine' is a checkmark almost everywhere but a filled circle on the one screen literally titled 'Abysslink Status'.
- **Impact:** A user who has learned that ✓ means 'good' across init and doctor lands on `abysslink status` and sees ● instead. The inconsistent symbol for the same semantic ('healthy/enabled') forces a momentary re-read ('is ● good or just neutral?') — especially since the neutral/disabled state on that same panel is ○ (open circle), making filled-vs-open dot the only differentiator rather than the checkmark used elsewhere.
- **Before (rendered):** The 'Abysslink Status' box lists rows like '● Tailscale running', '● Tailscale SSH enabled', '○ ntfy disabled'. Running `abysslink doctor` right after shows '✓ ... All checks passed' — a different success glyph for the same green/healthy meaning, side by side in the same session.
- **Fix:** Unify the healthy-state glyph: either use iconDone='✓' for rowOK in statusRow (cmd_status.go:467-479) so status matches doctor/init/up, or standardize all success output on ●. Keep ○ for the deliberately-disabled neutral state. One glyph for 'good' across every screen.
- **Confidence:** medium  ·  verify None/0

## Checklist
- [ ] T-003 (HIGH/AUTO_FIXABLE) internal/cli/styles.go:48
- [ ] T-004 (HIGH/AUTO_FIXABLE) internal/cli/cmd_init.go:115
- [ ] T-005 (HIGH/LOG) internal/cli/cmd_init.go:108
- [ ] T-006 (HIGH/LOG) internal/cli/cmd_init.go:275
- [ ] T-008 (HIGH/LOG) internal/cli/cmd_init.go:224
- [ ] T-010 (HIGH/LOG) internal/cli/cmd_init.go:333
- [ ] T-011 (HIGH/FLOW_FIX) internal/cli/cmd_init.go:237
- [ ] T-014 (MEDIUM/AUTO_FIXABLE) internal/cli/cmd_up.go:459
- [ ] T-029 (MEDIUM/LOG) internal/cli/cmd_enroll.go:615
- [ ] T-038 (MEDIUM/AUTO_FIXABLE) internal/cli/cmd_init.go:119
- [ ] T-039 (MEDIUM/LOG) internal/cli/cmd_init.go:250
- [ ] T-041 (MEDIUM/FLOW_FIX) internal/cli/cmd_init.go:283
- [ ] T-015 (LOW/LOG) internal/cli/cmd_init.go:1150
- [ ] T-026 (LOW/LOG) internal/cli/styles.go:80
- [ ] T-030 (LOW/LOG) internal/cli/cmd_up.go:431
- [ ] T-031 (LOW/LOG) internal/cli/styles.go:106
- [ ] T-036 (LOW/LOG) internal/cli/cmd_gallery.go:58
- [ ] T-043 (LOW/AUTO_FIXABLE) internal/cli/cmd_status.go:232
- [ ] T-044 (LOW/AUTO_FIXABLE) internal/cli/cmd_gallery.go:55
- [ ] T-045 (LOW/LOG) internal/cli/cmd_approve.go:398