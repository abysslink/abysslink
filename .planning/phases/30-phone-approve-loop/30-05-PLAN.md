---
phase: 30-phone-approve-loop
plan: "05"
type: execute
wave: 5
depends_on:
  - "30-04"
files_modified:
  - internal/approve/registry_test.go
  - internal/gate/gate_test.go
  - internal/modules/claudecode/module_test.go
autonomous: true
requirements:
  - APPR-05
  - APPR-06

must_haves:
  truths:
    - "TestApprove_NoClaude AST parse: internal/approve imports no claudecode, gate, or modules/* package (APPR-06 zero-coupling structural guard)"
    - "TestGate_NoClaude AST parse: internal/gate imports no claudecode package (existing AST test extended)"
    - "make lint test exits 0 on the full Phase 30 codebase — the combined gate is green"
    - "abysslink approve --check --blocking end-to-end: daemon IPC round-trip with real unix socket; approved request → exit 0; denied request → exit 2"
    - "abysslink approve --permission-request end-to-end: returns within 200ms; stdout valid allow JSON"
  artifacts:
    - path: "internal/approve/registry_test.go"
      provides: "TestApprove_NoClaude — AST-level import coupling test (APPR-06 structural)"
      contains: "TestApprove_NoClaude"
    - path: "internal/gate/gate_test.go"
      provides: "TestGate_NoClaude extended — confirms gate imports no claudecode"
      contains: "TestGate_NoClaude"
  key_links:
    - from: "TestApprove_NoClaude"
      to: "internal/approve/registry.go, hmac.go, tier.go"
      via: "go/parser AST walk checking ImportSpec.Path.Value strings"
      pattern: "ast\\.Inspect"
---

<objective>
Integration gate: add AST-level coupling tests to enforce the APPR-06 quarantine invariant, then run `make lint test` to confirm the full Phase 30 codebase is green.

This plan's primary deliverable is the green `make lint test` gate. Two new AST-based tests structurally enforce the zero-coupling invariant. The ntfy Actions[] wire format is verified end-to-end with a short integration test using an httptest.Server.

Purpose: Close the APPR-05 and APPR-06 quality gates; prove no coupling regression is possible at compile time via AST checks.
Output: Two new AST coupling tests; make lint test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-phone-approve-loop/30-CONTEXT.md
@.planning/phases/30-phone-approve-loop/30-RESEARCH.md
@.planning/phases/30-phone-approve-loop/30-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST coupling tests — TestApprove_NoClaude + TestGate_NoClaude extension (APPR-06)</name>
  <files>
    internal/approve/registry_test.go
    internal/gate/gate_test.go
  </files>
  <read_first>
    - internal/gate/gate_test.go lines 244-266 — TestGate_NoOSExecImport: exact AST-parse pattern to copy verbatim; uses go/parser, go/ast, go/token; walks ImportSpec.Path.Value; fails with t.Errorf per forbidden import found
    - internal/approve/registry_test.go — existing tests from Plan 01; add new test at the end
    - .planning/phases/30-phone-approve-loop/30-RESEARCH.md §Validation Architecture "TestApprove_NoClaude" entry in Phase Requirements → Test Map
    - .planning/phases/30-phone-approve-loop/30-CONTEXT.md §APPR-06 quarantine rule: "Zero Claude coupling permitted in approve/notify/gate"
  </read_first>
  <behavior>
    Test assertions:
    - TestApprove_NoClaude: parse all .go files in the internal/approve/ directory; walk imports; assert no import path contains any of: "claudecode", "internal/gate", "internal/daemon", "internal/modules" — the approve package is a leaf with no reverse imports into consumers
    - TestGate_NoClaude (extend existing TestGate_NoOSExecImport or add parallel): parse all .go files in internal/gate/; walk imports; assert no import contains "claudecode" — gate may import approve (approved in Plan 03) but never claudecode
    - Both tests must fail if the import is accidentally added (self-enforcing: if someone adds `import "github.com/abysslink/abysslink/internal/modules/claudecode"` to gate.go, the test catches it at CI)
  </behavior>
  <action>
    In internal/approve/registry_test.go — add at the end (do not disturb existing tests):

    TestApprove_NoClaude: copy the pattern from gate_test.go lines 244-266. Parse all Go source files in "." (the internal/approve package directory) using go/parser. Walk the parsed AST. For each ImportSpec node, check if the import path value contains any of the forbidden strings: "claudecode", "internal/gate", "internal/daemon", "internal/modules". Use t.Errorf for each violation found. Run all test files including non-_test.go files (the coupling guard applies to production code). Handle the fact that registry_test.go is itself in the package — filter self-referential test imports if they exist.

    Forbidden import strings for internal/approve:
    - "claudecode" (any path containing this string)
    - "internal/gate" (approve must not import gate; gate imports approve, not the reverse)
    - "internal/daemon" (approve must not import daemon)
    - "internal/modules" (approve must not import any module package)

    In internal/gate/gate_test.go — extend existing TestGate_NoOSExecImport or add TestGate_NoClaude alongside it:
    Add a check for "claudecode" in the forbidden import list for gate.go (gate may import internal/approve per Plan 03, which is explicitly permitted; it must never import claudecode).

    Forbidden import strings for internal/gate:
    - "claudecode"
    - (os/exec is already checked in the existing test — preserve that)

    Both AST tests should use filepath.Glob("*.go") to find files, or go/parser.ParseDir to parse the whole package directory. Use build tags-aware parsing (parser.ParseComments to avoid skipping build-constrained files). If a file fails to parse, t.Fatalf — a parse failure is itself a test failure.
  </action>
  <verify>
    <automated>go test ./internal/approve/... ./internal/gate/... -run TestApprove_NoClaude -run TestGate_NoClaude -count=1 -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TestApprove_NoClaude passes: no forbidden import found in any internal/approve/*.go file
    - TestGate_NoClaude passes: no claudecode import in any internal/gate/*.go file
    - If a forbidden import is added and the test re-run, it fails (verified during development by temporarily adding a forbidden import and checking the test fails)
    - go test ./internal/approve/... ./internal/gate/... -count=1 -race exits 0 (all tests including AST coupling tests pass)
  </acceptance_criteria>
  <done>
    AST-level coupling tests enforce the APPR-06 quarantine structurally. Any future accidental import of claudecode into gate or approve will fail CI.
  </done>
</task>

<task type="auto">
  <name>Task 2: make lint test green — full Phase 30 integration gate (APPR-01..06)</name>
  <files>
    internal/modules/claudecode/module_test.go
  </files>
  <read_first>
    - .planning/phases/30-phone-approve-loop/30-01-SUMMARY.md — what Plan 01 produced
    - .planning/phases/30-phone-approve-loop/30-02-SUMMARY.md — what Plan 02 produced
    - .planning/phases/30-phone-approve-loop/30-03-SUMMARY.md — what Plan 03 produced
    - .planning/phases/30-phone-approve-loop/30-04-SUMMARY.md — what Plan 04 produced
    - internal/modules/claudecode/module_test.go — existing tests; verify TestModule_PermissionRequestHook from Plan 04 is in place
  </read_first>
  <action>
    This task's primary action is running `make lint test` and fixing any issues found.

    Systematic fix protocol — in order:
    1. Run: go build ./... — fix any compile errors first
    2. Run: go vet ./... — fix any vet warnings (unused imports, unreachable code, etc.)
    3. Run: go test ./internal/approve/... ./internal/gate/... ./internal/daemon/... ./internal/notifyv2/... ./internal/modules/claudecode/... ./cmd/abysslink/... -count=1 -race — fix any test failures
    4. Run: make lint — fix any golangci-lint findings (errcheck, gosec, revive, gofmt, goimports, gocyclo, ineffassign, unused, staticcheck)
    5. Run: make test — confirm full test suite is green

    Common issues to watch for (from RESEARCH.md pitfalls):
    - Pitfall 3 (daemon IPC block): ensure all unix socket dial calls have context timeouts
    - Pitfall 5 (capability URL logged): grep -rn 'ablk_ok_\|ablk_no_\|approveURL\|denyURL' internal/ | grep -v '_test\.' — confirm no capability URLs in slog calls or error messages
    - Pitfall 6 (tier downgrade): verify approve.Tier() always checks CriticalPatterns before extraCritical; verify no code path removes from CriticalPatterns
    - D-40 bypass: grep for WithEnforcing in cmd/abysslinkd/ — confirm it is NEVER called on the daemon-internal runner; confirm it is called only on the CLI-facing composition root
    - gosec G304 (open file): scriptContentHash already has a nolint annotation for G304; confirm approve package never opens files (it should not — only gate.go's closureHash reads files)
    - errcheck: all error returns from slog.Warn paths are fire-and-forget (correct); all error returns from actual operations must be checked

    After all fixes, add one integration test to internal/modules/claudecode/module_test.go if the approve E2E test from Plan 04 (TestApproveCheck_ExitTwoDenied etc.) was placed in a different file — confirm the test coverage is connected.

    Final gate: make lint test must exit 0.
  </action>
  <verify>
    <automated>make lint test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - make lint test exits 0
    - go test ./... -count=1 -race exits 0 (all packages, including Phase 27/28/29 packages that must be unaffected)
    - golangci-lint ./... exits 0 (zero lint findings)
    - Capability URL grep: grep -rn 'approveURL\|denyURL' internal/daemon/ internal/approve/ shows only variable assignments/comments, never slog.Warn/slog.Info/slog.Error calls with those values
    - D-40 bypass grep: grep -rn 'WithEnforcing' cmd/ shows it is only called in the CLI composition root path (not in cmd/abysslinkd/)
    - All APPR-01..06 behaviors verifiable:
      - APPR-01: TestGated_ClosureHashMismatch passes
      - APPR-02: TestCAS_FirstAnswerWins passes; TestResolution_TimeoutDeny passes
      - APPR-03: TestHandleApprove_ValidResolves confirms audit.Append called
      - APPR-04: TestHandleApprove_CrossKind404 passes; TestTier_CriticalRefused passes
      - APPR-05: TestGated_ShadowMode passes; TestGated_EnforcingNoToken passes
      - APPR-06: TestApprove_NoClaude passes; TestModule_PermissionRequestHook passes
  </acceptance_criteria>
  <done>
    make lint test is green. All 6 APPR requirements have passing automated test coverage. Phase 30 is ready for /gsd-verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI→codebase | golangci-lint + go test -race enforce structural invariants (coupling, memory safety, error handling) |
| AST test→source files | Tests parse production source at test time — any future forbidden import fails CI immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-24 | Tampering | Future developer adds claudecode import to internal/approve | mitigate | TestApprove_NoClaude AST test catches it at CI; fails immediately |
| T-30-25 | Tampering | Future developer adds claudecode import to internal/gate | mitigate | TestGate_NoClaude AST test catches it at CI |
| T-30-26 | Elevation of privilege | WithEnforcing accidentally applied to daemon-internal runner | mitigate | make lint test + D-40 bypass grep verifies the composition root wiring; daemon-internal runner is never WithEnforcing in cmd/abysslinkd/ |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in this plan |
</threat_model>

<verification>
make lint test exits 0
All APPR-01..06 test names pass in go test output
TestApprove_NoClaude and TestGate_NoClaude both pass
Capability URL never appears in non-test slog calls (grep confirms)
</verification>

<success_criteria>
Phase 30 codebase is lint-clean and test-green with -race. The six APPR requirements all have automated behavioral test coverage. The APPR-06 quarantine is enforced structurally by two AST-level tests that will catch any future coupling regression at CI time.
</success_criteria>

<output>
Create `.planning/phases/30-phone-approve-loop/30-05-SUMMARY.md` when done.
</output>
