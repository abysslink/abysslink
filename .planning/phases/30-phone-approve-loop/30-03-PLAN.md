---
phase: 30-phone-approve-loop
plan: "03"
type: execute
wave: 2
depends_on:
  - "30-01"
  - "30-02"
files_modified:
  - internal/gate/gate.go
  - internal/gate/gate_test.go
  - internal/notifyv2/render.go
  - internal/modules/ntfy/module.go
autonomous: true
requirements:
  - APPR-01
  - APPR-05

must_haves:
  truths:
    - "Gated.Run in enforcing mode with a mismatched closure hash returns ErrClosureHashMismatch and never delegates to the inner runner (APPR-01 TOCTOU structurally closed)"
    - "Gated.Run in enforcing mode with no ApprovalToken in ctx returns ErrApprovalRequired for an approval-required exec (APPR-05)"
    - "Gated.Run in shadow mode (enforcing:false) never blocks, never calls approve.Request(), increments the counter, and delegates verbatim — identical to Phase 27 behavior (APPR-05 shadow default)"
    - "Daemon-internal runner constructed with plain gate.New(inner) and never WithEnforcing() cannot be forced into enforcing mode by any config change (D-40 structural bypass)"
    - "RenderedNote.Actions is populated for KindApprovalRequest messages with Actions[] and emitted as X-Actions: header by the ntfy delivery module"
  artifacts:
    - path: "internal/gate/gate.go"
      provides: "WithEnforcing option, ApprovalToken context threading, ErrClosureHashMismatch, ErrApprovalRequired, enforcing field on Gated"
      exports: ["WithEnforcing", "WithApprovalToken", "ErrClosureHashMismatch", "ErrApprovalRequired"]
    - path: "internal/notifyv2/render.go"
      provides: "RenderedAction struct, Actions []RenderedAction on RenderedNote (un-drops D-18)"
      exports: ["RenderedAction", "RenderedNote.Actions"]
  key_links:
    - from: "internal/gate/gate.go Gated.Run enforcing path"
      to: "internal/gate/gate.go closureHash"
      via: "closureHash(name, args) called at Run time; compared to ApprovalToken.ClosureHash from ctx"
      pattern: "closureHash.*ApprovalToken\\.ClosureHash"
    - from: "internal/gate/gate.go"
      to: "internal/approve/registry.go"
      via: "WithEnforcing(registry approve.Registry) injects the registry; gate.go imports internal/approve"
      pattern: "approve\\.Registry"
    - from: "internal/notifyv2/render.go"
      to: "internal/modules/ntfy/module.go"
      via: "RenderedNote.Actions []RenderedAction consumed by ntfy delivery to emit X-Actions: header"
      pattern: "X-Actions"
---

<objective>
Flip the GatedRunner from observe-only to enforcing mode (APPR-05) and un-drop the ntfy Actions[] field (D-18, APPR-04).

This plan has two distinct concerns:
1. gate.go enforcing mode: add WithEnforcing(registry) Option, ApprovalToken context key pair (WithApprovalToken/approvalTokenFromCtx), closure-hash re-verify at Run time (ErrClosureHashMismatch), and ErrApprovalRequired for execs that need a token but don't have one. Shadow mode (enforcing:false) is the default per D-04.
2. notifyv2/render.go + ntfy module: add RenderedAction struct + Actions []RenderedAction to RenderedNote; remove the D-18 "deliberately no actions field" comment block; wire the ntfy delivery module to emit X-Actions: headers for KindApprovalRequest messages.

Purpose: Close APPR-01 (TOCTOU re-verify at exec) and APPR-05 (enforcing flip + daemon-internal bypass structural).
Output: Enforcing gate + live ntfy Approve/Deny action buttons.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-phone-approve-loop/30-CONTEXT.md
@.planning/phases/30-phone-approve-loop/30-RESEARCH.md
@.planning/phases/30-phone-approve-loop/30-PATTERNS.md
@.planning/phases/30-phone-approve-loop/30-01-SUMMARY.md
@.planning/phases/30-phone-approve-loop/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gate.go enforcing mode — WithEnforcing, ApprovalToken ctx, TOCTOU re-verify (APPR-01, APPR-05)</name>
  <files>
    internal/gate/gate.go
    internal/gate/gate_test.go
  </files>
  <read_first>
    - internal/gate/gate.go lines 1-210 — full file: all existing types, Option pattern, Gated struct fields, Run/RunStream/etc. methods, closureHash, writeField, scriptContentHash
    - internal/gate/gate_test.go — full file: existing tests to extend; fakeInner mock (lines 41-88); TestGate_NoOSExecImport AST lint test (lines 244-266)
    - internal/approve/registry.go (from Plan 01 output) — Registry type, ApprovalToken struct, statePending/stateApproved/stateDenied constants
    - .planning/phases/30-phone-approve-loop/30-PATTERNS.md §gate.go (extending — enforcing mode): Option pattern, closure-hash re-verify, context key pattern
    - .planning/phases/30-phone-approve-loop/30-RESEARCH.md §Pattern 5 (TOCTOU re-verify), §Pattern 6 (GatedRunner observe→enforce flip), §Pattern 1 (ApprovalToken shape)
  </read_first>
  <behavior>
    Extend gate_test.go (do not replace existing tests):
    - TestGated_ShadowMode: Gated constructed with gate.New(inner) only (no WithEnforcing); call Run; assert inner was called, counter incremented; assert no blocking occurred (returns immediately)
    - TestGated_EnforcingNoToken: Gated with WithEnforcing(fakeRegistry); Run with a context that has no ApprovalToken attached; assert returns ErrApprovalRequired and inner.Run NOT called
    - TestGated_ClosureHashMismatch: Gated with WithEnforcing; attach an ApprovalToken to ctx with a WRONG ClosureHash (e.g. all-zeros); call Run("echo", "hello"); assert returns ErrClosureHashMismatch; inner.Run NOT called
    - TestGated_ClosureHashMatch: Gated with WithEnforcing; compute real closureHash("echo", []string{"hello"}); attach ApprovalToken{ClosureHash: that hash}; call Run("echo", "hello"); assert inner.Run WAS called (no mismatch)
    - TestGated_InternalBypass: Gated constructed with plain New(inner) — no WithEnforcing; even if approveRegistry is set on a different instance, this instance delegates without checking; proves the structural bypass is purely at construction time (D-40)
    - TestGated_TokenInContext: WithApprovalToken(ctx, tok) returns a new ctx; approvalTokenFromCtx(ctx) returns (tok, true); approvalTokenFromCtx(context.Background()) returns (nil, false)
    - TestGated_CounterShadow: shadow mode still increments Count() on every Run (Phase 27 behavior preserved)
  </behavior>
  <action>
    Extend internal/gate/gate.go — add to the existing file without removing anything:

    New imports to add: "internal/approve" (for approve.Registry and approve.ApprovalToken re-use). Add sentinel errors: ErrClosureHashMismatch = errors.New("gate: closure hash mismatch — exec refused (TOCTOU)") and ErrApprovalRequired = errors.New("gate: approval token required in enforcing mode").

    Extend Gated struct with two new fields:
    - enforcing bool (comment: when true, Run checks ctx for ApprovalToken and re-verifies closure hash; default false per D-04)
    - registry approve.Registry (comment: daemon-side approve registry client; only meaningful when enforcing==true; never set on the daemon-internal runner (D-40 structural bypass))

    Add WithEnforcing option (per PATTERNS.md):
    func WithEnforcing(registry approve.Registry) Option:
      returns func(g *Gated) { g.enforcing = true; g.registry = registry }

    Add context key helpers (package-private type per RESEARCH.md A1/Pattern 5):
    - type ctxKey struct{} (unexported — prevents cross-package collision)
    - func WithApprovalToken(ctx context.Context, tok *approve.ApprovalToken) context.Context: context.WithValue(ctx, ctxKey{}, tok)
    - func approvalTokenFromCtx(ctx context.Context) (*approve.ApprovalToken, bool): ctx.Value(ctxKey{}).(*approve.ApprovalToken); return tok, ok && tok != nil

    Modify Run (and RunWithStdin, RunInteractive, RunWithEnv, RunStream similarly for blocking enforcement):
    func (g *Gated) Run(ctx context.Context, name string, args ...string) (shell.Result, error):
      g.record("Run", name, args)  // always runs, shadow or enforcing
      if g.enforcing {
          tok, hasTok := approvalTokenFromCtx(ctx)
          if hasTok {
              // Re-verify closure hash (TOCTOU closure per D-02/APPR-01)
              current := closureHash(name, args)
              if current != tok.ClosureHash {
                  slog.Warn("gate: closure hash mismatch — exec refused (TOCTOU)",
                      "approved_prefix", hex.EncodeToString(tok.ClosureHash[:8]),
                      "current_prefix", hex.EncodeToString(current[:8]))
                  return shell.Result{}, ErrClosureHashMismatch
              }
              // Hash matches — token was already CAS-resolved in the registry
              // (single-use: this exec proceeds)
          } else if g.requiresApproval(name, args) {
              // Enforcing mode + no token + exec requires approval → refuse
              return shell.Result{}, ErrApprovalRequired
          }
          // else: exec does not require approval in enforcing mode → pass through
      }
      return g.inner.Run(ctx, name, args...)

    Add g.requiresApproval(name string, args []string) bool — private helper: for Phase 30, all execs in enforcing mode that don't carry a token are refused. Future phases may add per-command allowlisting. Simple first implementation: return true (block all unapproved execs when enforcing). Add a comment: "Phase 30: all execs require approval in enforcing mode. Phase 31 will add per-action tier allowlisting."

    Apply the same enforcing check to RunWithStdin, RunInteractive, RunWithEnv, RunStream. For RunStream the return signature differs but the pattern is the same: check enforcing + token + hash before delegating.

    Note on RunInteractive: it returns error (no shell.Result); adapt accordingly. RunStream returns (*shell.Stream, error) — adapt similarly.

    Critical invariant (D-40): WithEnforcing is NEVER called on the daemon-internal runner. This is enforced at the composition root (cmd/abysslinkd), not in this file. Document with a comment: "The daemon-internal runner (used for tmux -CC, probes, etc.) is constructed with plain New(inner) — never WithEnforcing. This is the structural self-deadlock bypass (D-40): visible in the dep graph, not runtime-toggleable."

    Do NOT modify closureHash, argvHash, writeField, scriptContentHash, or record() — those are unchanged.
  </action>
  <verify>
    <automated>go test ./internal/gate/... -count=1 -race -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All new tests pass: TestGated_ShadowMode, TestGated_EnforcingNoToken, TestGated_ClosureHashMismatch, TestGated_ClosureHashMatch, TestGated_InternalBypass, TestGated_TokenInContext, TestGated_CounterShadow
    - All existing gate_test.go tests still pass (the enforcing mode is additive; shadow behavior is unchanged)
    - TestGated_ClosureHashMismatch: inner.Run is never called when hash mismatches
    - TestGated_EnforcingNoToken: ErrApprovalRequired returned; counter still incremented
    - go test ./internal/gate/... -count=1 -race exits 0
    - ErrClosureHashMismatch and ErrApprovalRequired exported from internal/gate
  </acceptance_criteria>
  <done>
    gate.go has WithEnforcing option, ApprovalToken context threading, TOCTOU re-verify at Run, ErrClosureHashMismatch and ErrApprovalRequired sentinels. Shadow mode behavior is unchanged. All tests pass under -race.
  </done>
</task>

<task type="auto">
  <name>Task 2: notifyv2/render.go un-drop Actions[] + ntfy X-Actions: header (APPR-04, D-18)</name>
  <files>
    internal/notifyv2/render.go
    internal/modules/ntfy/module.go
  </files>
  <read_first>
    - internal/notifyv2/render.go — full file: RenderedNote struct, Render() function, the D-18 comment block that drops Actions[] (find and remove); Tags rendering pattern (to model X-Actions: rendering)
    - internal/notifyv2/message.go lines 28-55 — Action struct definition (verify it has ID and Label or URL fields); KindApprovalRequest constant
    - internal/modules/ntfy/module.go — the ntfy delivery function that reads RenderedNote and sets ntfy HTTP headers (find where Tags/Click/Priority headers are set; add X-Actions: header alongside)
    - .planning/phases/30-phone-approve-loop/30-PATTERNS.md §render.go (extending — un-drop Actions[])
    - .planning/phases/30-phone-approve-loop/30-RESEARCH.md open question 1 (ntfy X-Actions wire format)
  </read_first>
  <action>
    internal/notifyv2/render.go changes:

    1. Add RenderedAction struct:
       type RenderedAction struct {
           Label string // "Approve" or "Deny"
           URL   string // single-use capability URL — NEVER logged
       }
       (Comment: RenderedAction carries one ntfy action button. The URL is a 256-bit single-use capability token — never log it, never persist it. Phase 30 populates this for KindApprovalRequest messages.)

    2. Extend RenderedNote struct — add Actions []RenderedAction field:
       Actions []RenderedAction // nil unless KindApprovalRequest with populated Actions[]
       (Remove the D-18 comment block: "There is deliberately no actions field..." — this is the phase that wires it)

    3. Extend Render() (or equivalent function that builds RenderedNote) to populate Actions:
       For messages with Kind == KindApprovalRequest and len(msg.Actions) > 0:
       - Map each msg.Action{ID:"approve", Label:"Approve", URL:approveURL} → RenderedAction{Label:"Approve", URL:approveURL}
       - Map each msg.Action{ID:"deny", Label:"Deny", URL:denyURL} → RenderedAction{Label:"Deny", URL:denyURL}
       - Assign to note.Actions
       For all other kinds, Actions remains nil (no behavioral change).

    Verify the Action type in message.go — check whether it has a URL field. If the Phase 27 Action type only has {ID, Label} and no URL, add a URL field to Action in message.go (the Phase 27 renderer dropped Actions, so the URL field may have been intentionally omitted from the wire type). This is the URL to embed in the ntfy button. Add: URL string `json:"url,omitempty"` to the Action struct.

    internal/modules/ntfy/module.go changes:

    4. Find the function that sends ntfy notifications via HTTP (the one that sets request headers like X-Title, X-Tags, X-Click, X-Priority). Add X-Actions: header generation when note.Actions is non-empty:
       if len(note.Actions) > 0 {
           parts := make([]string, 0, len(note.Actions))
           for _, a := range note.Actions {
               // ntfy X-Actions format: "view, Label, URL"
               // Source: ntfy docs (https://docs.ntfy.sh/publish/#action-buttons)
               // "view" type opens the URL in a browser on the phone.
               parts = append(parts, "view, "+a.Label+", "+a.URL)
           }
           req.Header.Set("X-Actions", strings.Join(parts, "; "))
       }
       (Separation of concerns: render.go produces []RenderedAction with Label+URL; the ntfy delivery module composes the X-Actions: string — consistent with the existing pattern where render.go produces Tags as a string and ntfy sets X-Tags.)

    Do NOT add tests for the ntfy HTTP delivery in this plan (the existing ntfy tests mock the HTTP client; extending them is Plan 05 scope). However, verify the X-Actions header format is correctly constructed by reading the ntfy docs format and checking it matches "view, Label, URL" per docs.ntfy.sh/publish/#action-buttons.

    Add a brief unit test in notifyv2 for the render function (extend existing render tests — do not create a new test file if one exists):
    - TestRender_ApprovalRequest_Actions: Build a Message{Kind: KindApprovalRequest, Actions: [{ID:"approve", Label:"Approve", URL:"https://example.ts.net/approve/ablk_ok_xxx"}, {ID:"deny", ...}]}; call Render(msg); assert note.Actions has 2 entries with correct Label+URL
    - TestRender_OtherKind_NoActions: Build a Message{Kind: KindNeedsInput}; Render → note.Actions is nil
  </action>
  <verify>
    <automated>go test ./internal/notifyv2/... -count=1 -race -v 2>&1 | tail -20 && go build ./internal/modules/ntfy/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - RenderedNote struct has Actions []RenderedAction field
    - RenderedAction struct exported from internal/notifyv2
    - D-18 comment block removed from render.go
    - TestRender_ApprovalRequest_Actions passes: Render produces non-nil Actions with Approve+Deny labels and URLs
    - TestRender_OtherKind_NoActions passes: non-approval messages have nil Actions
    - ntfy module.go compiles with X-Actions: header set for non-empty Actions
    - go test ./internal/notifyv2/... -count=1 -race exits 0
    - go build ./internal/modules/ntfy/... exits 0
  </acceptance_criteria>
  <done>
    notifyv2/render.go exports RenderedAction + populates Actions[] for KindApprovalRequest. ntfy module emits X-Actions: header when Actions is non-nil. Two-button Approve/Deny on the phone is live. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval call site→Gated.Run | ApprovalToken threaded via context; closure hash re-verified at Run time (TOCTOU window closed between approve and exec) |
| notifyv2 renderer→ntfy HTTP header | URLs in X-Actions: header are single-use capability tokens — must never appear in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-14 | Tampering | Binary swap between approve and exec (TOCTOU) | mitigate | closureHash includes resolved binary path; Gated.Run re-verifies hash; mismatch → ErrClosureHashMismatch, exec refused (APPR-01) |
| T-30-15 | Tampering | cwd change between approve and exec | mitigate | cwd is part of closureHash; change → mismatch → refused. Current confirm sites don't `cd` between confirm and exec — acceptable risk documented |
| T-30-16 | Elevation of privilege | Exec bypassing enforcing mode | mitigate | Daemon-internal runner is constructed with plain New(inner) — no WithEnforcing; structural (D-40 dep graph); no runtime-toggleable path to add enforcing after construction |
| T-30-17 | Information disclosure | approveURL/denyURL in ntfy X-Actions: header exposed to ntfy server | mitigate | X-Actions: URLs are the capability tokens; they are sent to the phone's configured ntfy server. Mitigation: ntfy is the operator's own instance (sovereign path per D-15 APPR-04); the phone taps the URL exactly once. No logging of X-Actions header content in the daemon. |
| T-30-18 | Tampering | Tier downgrade in enforcing gate | mitigate | requiresApproval() enforces approval for all execs in enforcing mode; critical tier still returns ErrCriticalTierTTYOnly from approve.Open() before any capability URL is minted |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in this plan |
</threat_model>

<verification>
go test ./internal/gate/... ./internal/notifyv2/... -count=1 -race exits 0
ErrClosureHashMismatch and ErrApprovalRequired exported from internal/gate
RenderedNote.Actions populated for KindApprovalRequest
ntfy X-Actions: header emitted in module.go
D-18 comment removed from render.go
</verification>

<success_criteria>
Gated.Run in enforcing mode refuses execs with mismatched closure hashes (TOCTOU closed) or missing ApprovalToken. Shadow mode behavior is unchanged. ntfy renders two-button Approve/Deny for approval_request kind messages. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/30-phone-approve-loop/30-03-SUMMARY.md` when done.
</output>
