---
phase: 13-netbird-backend
plan: "03"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - internal/backend/netbird_config.go
  - internal/backend/netbird_config_test.go
  - internal/backend/netbird_doctor.go
  - internal/backend/netbird_doctor_internal_test.go
  - internal/backend/netbird_doctor_test.go
  - internal/backend/testdata/netbird/config_minimal.yaml
  - internal/backend/config_helpers.go
autonomous: true
requirements:
  - NB-02
  - NB-03

must_haves:
  truths:
    - "MergeNetBirdConfig() performs surgical YAML merge on config.yaml: disableAnonymousMetrics, metricsPort (9090), TLS fields, auth fields — idempotent re-runs"
    - "NetBirdDoctorChecks() returns exactly 9 DoctorFindings: (slot 0) nb-sshcheck WARN (D-03, always present), then the 7 canonical nb-* checks from ROADMAP SC-5 (nb-tls, nb-version, nb-zitadel, nb-mgmt-bind, nb-key-type, nb-api-auth, nb-proc-user), then nb-runtime (PR-A, 8th canonical check added to SC-5 by post-research amendment)"
    - "nb-version check: FAIL if binary version < v0.57.0; PASS if >= v0.57.0"
    - "nb-zitadel: detect-then-probe per D-02 — SKIP/PASS if no zitadel issuer in config.yaml; FAIL if POST /management/v1/users/_search returns 200 with results"
    - "nb-mgmt-bind: FAIL if metricsPort binds 0.0.0.0; PASS if loopback (127.0.0.1) or private range"
    - "nb-key-type: FAIL if any active setup key has type!=one-off or expires is zero/past"
    - "nb-api-auth: PASS on HTTP 200 from GET /api/groups; FAIL on 401/403"
    - "nb-proc-user: Linux — systemctl show property User must be non-root; macOS — SKIP (PR-A: macOS server is container; proc-user check is nb-runtime)"
    - "nb-runtime (PR-A, 8th check): macOS — assert docker/colima/podman present and daemon reachable (PASS if any runtime responds exit 0; FAIL if all absent); Linux — SKIP with DoctorOK"
    - "nb-sshcheck (slot 0): DoctorWarning, message contains exact string `WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled`"
    - "Table-driven YAML merge tests cover: no-op idempotent re-run, metricsPort correction, TLS cert injection"
    - "Doctor tests use httptest.Server + shell.MockRunner for all REST and shell calls"
  artifacts:
    - path: "internal/backend/config_helpers.go"
      provides: "setNestedKey() shared helper for surgical YAML merge"
      exports: ["setNestedKey"]
    - path: "internal/backend/netbird_config.go"
      provides: "MergeNetBirdConfig() surgical YAML merge"
      exports: ["MergeNetBirdConfig"]
    - path: "internal/backend/netbird_doctor.go"
      provides: "NetBirdDoctorChecks() returning 9 DoctorFinding entries (nb-sshcheck + 7 SC-5 checks + nb-runtime)"
      exports: ["NetBirdDoctorChecks", "NewNetBirdDoRequest"]
  key_links:
    - from: "internal/backend/netbird_doctor.go"
      to: "netbird REST API"
      via: "doRequestFunc (httptest-injectable) for nb-api-auth, nb-key-type, nb-zitadel probe"
      pattern: "doRequestFunc"
    - from: "internal/backend/netbird_config.go"
      to: "internal/audit"
      via: "audit.WriteFile for all config.yaml mutations"
      pattern: "audit.WriteFile"
---

<objective>
Implement the NetBird server config surgical merge and all 9 nb-* doctor findings.
These two files are the server-tier logic consumed by the server provisioner command
(Plan 04) and the doctor command (Plan 04 wiring).

Purpose: The surgical merge ensures idempotent hardening of netbird-server config.yaml.
The 9 doctor findings surface security posture for: nb-sshcheck (slot 0, D-03 mandatory
degradation WARN), then the 7 canonical nb-* checks from ROADMAP SC-5 (nb-tls,
nb-version, nb-zitadel, nb-mgmt-bind, nb-key-type, nb-api-auth, nb-proc-user), then
nb-runtime (PR-A, 8th check added by post-research amendment for macOS container path).

Output: config_helpers.go (shared setNestedKey), netbird_config.go,
netbird_config_test.go, netbird_doctor.go, netbird_doctor_internal_test.go,
netbird_doctor_test.go. `make lint test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-netbird-backend/13-CONTEXT.md
@.planning/phases/13-netbird-backend/13-RESEARCH.md
@.planning/phases/13-netbird-backend/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MergeNetBirdConfig() — surgical YAML merge for config.yaml (NB-02)</name>
  <read_first>
    - internal/backend/headscale_config.go — full reference: MergeHeadscaleConfig(), setNestedKey() pattern, audit.WriteFile usage, dryRun path, gopkg.in/yaml.v3 round-trip
    - internal/backend/headscale_config_test.go — table-driven test structure to mirror
    - internal/backend/testdata/netbird/config_minimal.yaml — input fixture
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §Code Examples — hardened config.yaml fields (disableAnonymousMetrics, metricsPort, etc.)
    - internal/audit — WriteFile signature (path, content, dryRun bool) for all writes
    - internal/config/config.go — cfg.Server.NetBird fields used as merge input
  </read_first>
  <files>internal/backend/config_helpers.go, internal/backend/netbird_config.go, internal/backend/netbird_config_test.go</files>
  <action>
    First, check whether `internal/backend/headscale_config.go` exports `setNestedKey` or keeps it unexported. If it is unexported (package-private), create `internal/backend/config_helpers.go` with Apache-2.0 header and move `setNestedKey(root map[string]any, key string, value any)` into it, updating the import in `headscale_config.go` accordingly. If headscale_config.go already exports it (unlikely given typical Go naming), re-export from config_helpers.go as a thin wrapper. Do NOT duplicate the implementation — one canonical copy lives in config_helpers.go and both headscale and netbird call it. Add config_helpers.go to files_modified so scope is declared. If refactoring headscale_config.go to use the shared helper causes a compile error in the headscale tests, fix those tests in the same task.

    Create `internal/backend/netbird_config.go` with Apache-2.0 header. Implement `MergeNetBirdConfig(cfgPath string, nb config.NetBirdServer, dryRun bool) error`.

    Read the existing config.yaml at cfgPath via os.ReadFile; if not found, start with an empty map. Unmarshal via gopkg.in/yaml.v3 into `map[string]any`. Apply these hardened keys using setNestedKey (always written, idempotent):
    - `disableAnonymousMetrics: true` (no telemetry — CLAUDE.md)
    - `disableGeoliteUpdate: true` (no outbound calls)
    - `metricsPort: 9090` (metrics listener)

    Apply these conditional keys:
    - If `nb.TLSCertFile != ""`: set `server.tls.certFile` and `server.tls.keyFile`
    - If both are empty: do NOT set tls keys (user may manage externally)
    - `server.auth.issuer` left untouched (Dex is default; operator may change)

    Marshal back via gopkg.in/yaml.v3. Compute diff (old vs new bytes as string). Pass to `audit.WriteFile(cfgPath, newBytes, dryRun)`. In dryRun mode, log the diff via `slog.Info("netbird config merge (dry-run)", "diff", diff)` and return nil. Never write secrets (store encryption key, API key) into config.yaml — keychain-only per CLAUDE.md.

    Create `netbird_config_test.go` with table-driven tests:
    - Idempotent re-run: apply merge twice, assert output identical on second run
    - metricsPort correction: input has `metricsPort: 8888`, output has `metricsPort: 9090`
    - TLS cert injection: input has no tls section, nb.TLSCertFile set, output has `server.tls.certFile`
    - DryRun: no file written, function returns nil
    - Missing config file: empty map base, hardened keys appear in output

    Use `t.TempDir()` for all file writes in tests. All test assertions use `testify/require`.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go test ./internal/backend/... -run TestMergeNetBird -v 2>&1 | grep -E "PASS|FAIL|RUN"</automated>
  </verify>
  <done>All TestMergeNetBird* tests pass; audit.WriteFile is called for non-dryRun paths; idempotent re-run test confirms no second write; setNestedKey has exactly one implementation in config_helpers.go (no duplicated copy); `go build ./internal/backend/...` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: NetBirdDoctorChecks() — all 9 findings with hermetic tests (NB-03, per D-02/PR-A/PR-C)</name>
  <read_first>
    - internal/backend/headscale_doctor.go — full reference: DoctorFinding types, doRequestFunc pattern, check function signatures, checkHsProcUserLinux() shell pattern, NewHeadscaleDoRequest() constructor pattern, checkHsTLS() x509 parse pattern
    - internal/backend/headscale_doctor_test.go — httptest.Server + MockRunner test patterns
    - internal/backend/headscale_doctor_internal_test.go — internal package test patterns (if any private functions are tested)
    - internal/backend/config_helpers.go — setNestedKey (created in Task 1)
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §RF-7 — each check's exact assertion shape
    - .planning/phases/13-netbird-backend/13-CONTEXT.md §post_research_resolutions — PR-A (nb-runtime), PR-C (drop D-05)
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §Anti-Patterns — nb-mgmt-bind: check metricsPort, not listenAddress
    - internal/backend/testdata/netbird/config_minimal.yaml — for config-file-based checks
    - internal/backend/testdata/netbird/config_zitadel.yaml — for nb-zitadel SKIP vs probe path
    - internal/shell — MockRunner for nb-version and nb-proc-user
  </read_first>
  <files>
    internal/backend/netbird_doctor.go,
    internal/backend/netbird_doctor_internal_test.go,
    internal/backend/netbird_doctor_test.go
  </files>
  <action>
    Create `internal/backend/netbird_doctor.go` with Apache-2.0 header.

    Define `NewNetBirdDoRequest(cfg *config.Config) doRequestFunc` — same pattern as `NewHeadscaleDoRequest`: returns a doRequestFunc closure bound to a fresh netbirdAdapter so the CLI can pass it into NetBirdDoctorChecks() without creating a circular dependency. API key flows only into Authorization: Token header inside doRequest — never logged.

    Implement `NetBirdDoctorChecks(ctx context.Context, cfg *config.Config, runner shell.Runner, doReq doRequestFunc) []DoctorFinding` returning exactly 9 findings in this order:

    Finding 0 — nb-sshcheck (D-03 mandatory degradation, always present): `DoctorFinding{Module:"netbird", Check:"nb-sshcheck", Severity:DoctorWarning, Message:"WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled. Setup-key-enrolled peers have no periodic re-auth mechanism in NetBird."}`. This is emitted unconditionally, before all other checks. The substring `WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled` must appear verbatim in the Message field.

    Findings 1–7 — the 7 canonical nb-* checks from ROADMAP SC-5 (nb-tls, nb-version, nb-zitadel, nb-mgmt-bind, nb-key-type, nb-api-auth, nb-proc-user), implemented as follows:

    `checkNbTLS(cfg)` — parse cfg.Server.NetBird.ConfigPath (config.yaml), read server.tls.certFile and server.tls.keyFile. If both are set: use `crypto/tls.LoadX509KeyPair` + `x509.ParseCertificate` — cert must not be expired, CN/SAN must match cfg.Server.NetBird.ServerURL host. PASS if valid. FAIL if expired or key missing. If both are empty: FAIL (TLS required). Mirrors `checkHsTLS()` from headscale_doctor.go.

    `checkNbVersion(ctx, runner, binaryPath string)` — runner.Run(ctx, binaryPath, "--version"). Parse output for semver. Compare to floor "0.57.0" via semverLessThan helper. FAIL if below floor. PASS if at/above. WARN if binary not found or parse fails. On macOS container path (cfg.Server.NetBird.BinaryPath is empty): return DoctorOK{Check:"nb-version", Message:"nb-version: macOS container path — version enforced by image tag at provision time"}.

    `checkNbZitadel(ctx, cfg, doReq)` — detect-then-probe per D-02: (1) read config.yaml server.auth.issuer; if strings.Contains(issuer, "zitadel") is false → return DoctorOK{Check:"nb-zitadel", Message:"no ZITADEL deployment detected — SKIP"}. (2) If ZITADEL detected: POST {issuer_base}/management/v1/users/_search with body `{"queries":[{"userNameQuery":{"userName":"zitadel-admin@","method":"TEXT_QUERY_METHOD_STARTS_WITH"}}]}`, header `Authorization: Bearer` (ZITADEL admin API, NOT NetBird Token). Parse response: HTTP 401 or result count==0 → PASS. HTTP 200 and result count > 0 → FAIL "ZITADEL default admin account still active (CVE-2025-10678)".

    `checkNbMgmtBind(cfg)` — read config.yaml; if any explicit `metricsListenAddress` field contains "0.0.0.0" → FAIL. If cfg.Server.NetBird.MgmtBindAddr contains "0.0.0.0" → FAIL. Otherwise PASS. (server.listenAddress `:443` is expected for peer connectivity; only metrics/admin ports are loopback-gated.)

    `checkNbKeyType(ctx, doReq)` — GET /api/setup-keys. For each active key: FAIL if type != "one-off". FAIL if Expires is zero or past. All pass → PASS. No active keys → WARN "no active setup keys found".

    `checkNbAPIAuth(ctx, doReq)` — GET /api/groups. HTTP 200 → PASS. HTTP 401/403 → FAIL. Network error → WARN.

    `checkNbProcUser(ctx, runner)` — Linux: runner.Run(ctx, "systemctl", "show", "netbird-server", "--property=User"). Parse "User=" value. Non-empty and != "root" and != "0" → PASS. Empty or root → FAIL. Service not found → WARN. macOS: return DoctorOK{Message:"nb-proc-user: macOS uses container path — process user enforced by container runtime (nb-runtime)"}.

    Finding 8 — nb-runtime (PR-A, 8th check added by post-research amendment):
    `checkNbRuntime(ctx, runner)` — macOS (runtime.GOOS=="darwin"): probe docker, colima, podman in that order via runner.Run(ctx, "docker", "info") (or colima/podman equivalent). First that returns exit 0 → PASS "container runtime {name} present and reachable". All fail → FAIL "no container runtime found — required for NetBird server on macOS (nb-runtime)". Linux: return DoctorOK{Check:"nb-runtime", Message:"nb-runtime: Linux uses native binary — SKIP"}.

    Create `netbird_doctor_test.go` with httptest.Server + shell.MockRunner tests:
    - nb-zitadel: SKIP path (config_minimal.yaml — no zitadel issuer)
    - nb-zitadel: PASS path (config_zitadel.yaml, mock POST returns 401)
    - nb-zitadel: FAIL path (config_zitadel.yaml, mock POST returns 200 with count>0)
    - nb-key-type: PASS (all one-off + valid expiry)
    - nb-key-type: FAIL (one key is reusable)
    - nb-key-type: FAIL (one key has zero expires)
    - nb-api-auth: PASS (GET /api/groups returns 200)
    - nb-api-auth: FAIL (GET /api/groups returns 401)
    - nb-version: PASS (MockRunner returns v0.71.4)
    - nb-version: FAIL (MockRunner returns v0.30.0, below floor)
    - nb-proc-user: Linux PASS (MockRunner returns "User=netbird-server")
    - nb-proc-user: Linux FAIL (MockRunner returns "User=root")
    - TestNbRuntime: macOS PASS path (MockRunner docker info returns exit 0)
    - TestNbRuntime: macOS FAIL path (MockRunner docker/colima/podman all return non-zero)
    - TestNbRuntime: Linux SKIP path (GOOS=linux, result is DoctorOK with "SKIP")
    - nb-sshcheck WARN is always the first (index 0) finding in the returned slice

    Create `netbird_doctor_internal_test.go` (package backend, not package backend_test) for private helper tests (e.g. parseNetBirdVersion, semverLessThan if not already tested in headscale_doctor tests).

    Apache-2.0 header on all new Go files. No `fmt.Println` — slog only. `context.Context` on all functions with I/O.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go test ./internal/backend/... -run 'TestNb|TestNbRuntime|TestNetBirdDoctor' -v 2>&1 | grep -E "PASS|FAIL|RUN" | head -50</automated>
  </verify>
  <done>All nb-* doctor check tests pass including TestNbRuntime PASS path, FAIL path, and Linux SKIP path; nb-zitadel SKIP/PASS/FAIL paths covered; nb-sshcheck WARN is index 0 in returned slice; NetBirdDoctorChecks() returns exactly 9 findings (nb-sshcheck + nb-tls + nb-version + nb-zitadel + nb-mgmt-bind + nb-key-type + nb-api-auth + nb-proc-user + nb-runtime); `make lint test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → MergeNetBirdConfig | Input file from filesystem; must not contain secret values |
| NetBird REST API → doctor checks | TLS enforced; doRequestFunc injectable for tests |
| ZITADEL probe → external ZITADEL admin API | Separate from NetBird API; uses Bearer header not Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Info Disclosure | config.yaml secrets | mitigate | MergeNetBirdConfig never writes API keys or setup keys to config.yaml — keychain-only per CLAUDE.md |
| T-13-03-02 | Elevation of Privilege | ZITADEL default admin (CVE-2025-10678) | mitigate | nb-zitadel detect-then-probe: FAIL if default admin account still active (HTTP 200 response from _search); D-02 logic implemented per spec |
| T-13-03-03 | Spoofing | Management port exposed to public (0.0.0.0) | mitigate | nb-mgmt-bind: FAIL if metricsListenAddress contains 0.0.0.0; config-level MgmtBindAddr also checked |
| T-13-03-04 | Elevation of Privilege | Setup key reuse enabling unauthorized enrollment | mitigate | nb-key-type: FAIL if any active key type!="one-off" or expires is zero/past |
| T-13-03-05 | Elevation of Privilege | NetBird server process running as root | mitigate | nb-proc-user: FAIL on Linux if systemctl User is root; macOS defers to container runtime via nb-runtime |
| T-13-03-06 | Tampering | Unverified netbird-server binary version | mitigate | nb-version: hard FAIL if below v0.57.0 floor (CVE-2025-10678 fix boundary) |
| T-13-03-07 | Spoofing | TLS disabled or invalid cert | mitigate | nb-tls: FAIL if no cert configured or cert expired/mismatched; mirrors hs-tls strict behavior |
| T-13-03-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
1. `go build ./internal/backend/...` exits 0
2. `go test ./internal/backend/... -run TestMergeNetBird` — all pass
3. `go test ./internal/backend/... -run 'TestNb|TestNbRuntime|TestNetBirdDoctor'` — all pass
4. nb-zitadel SKIP, PASS, FAIL test cases all present and passing
5. `grep -v '^//' internal/backend/netbird_doctor.go | grep -c 'WARN: SSHCheck not available on NetBird'` returns >= 1
6. nb-runtime check: macOS PASS/FAIL/Linux-SKIP tests all passing (TestNbRuntime)
7. NetBirdDoctorChecks() returns exactly 9 findings (length check in test)
8. setNestedKey has one canonical implementation in config_helpers.go (no duplicate in netbird_config.go)
9. `make lint test` exits 0
</verification>

<success_criteria>
- MergeNetBirdConfig() surgical merge idempotent; tested for all key scenarios; uses shared setNestedKey from config_helpers.go
- NetBirdDoctorChecks() returns exactly 9 DoctorFindings in this order: (0) nb-sshcheck WARN (D-03, unconditional), (1-7) the 7 canonical nb-* checks from ROADMAP SC-5 (nb-tls, nb-version, nb-zitadel, nb-mgmt-bind, nb-key-type, nb-api-auth, nb-proc-user), (8) nb-runtime (PR-A post-research amendment)
- nb-sshcheck message contains verbatim: `WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled`
- nb-version: FAIL below v0.57.0 (tested)
- nb-zitadel: SKIP on Dex deployments (tested); FAIL on live default-creds (tested)
- nb-runtime: macOS PASS/FAIL tested; Linux SKIP tested (TestNbRuntime covers all three paths)
- make lint test exits 0
</success_criteria>

<output>
Create `.planning/phases/13-netbird-backend/13-03-SUMMARY.md` when done
</output>
