---
phase: 13-netbird-backend
plan: "05"
type: execute
wave: 4
depends_on:
  - "13-02"
  - "13-04"
files_modified:
  - internal/cli/cmd_init.go
  - internal/cli/cmd_up.go
  - internal/cli/cmd_init_backend_test.go
autonomous: true
requirements:
  - NB-01
  - NB-02
  - NB-03

must_haves:
  truths:
    - "`abysslink init` wizard offers `backend.type: netbird` as a selection option + prompts for server_url"
    - "`abysslink up --apply` on a netbird backend refuses unless cfg.Server.NetBird.AcceptNoSSHCheck==true OR --accept-no-sshcheck flag is present (D-04)"
    - "First run with netbird backend without --accept-no-sshcheck: exits with clear error explaining the degradation and how to accept it"
    - "After --accept-no-sshcheck is given: AcceptNoSSHCheck=true is persisted to abysslink.yaml via audit.WriteFile"
    - "Subsequent runs with AcceptNoSSHCheck=true in config: proceed without requiring flag again (persisted once)"
    - "The refusal error message contains the exact string `WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled` and instructs to re-run with `--accept-no-sshcheck`"
    - "cmd_init.go runInitForm wizard backend type select includes netbird option (alongside tailscale and headscale)"
    - "init wizard netbird path: prompts for server_url; writes backend.type: netbird + server.netbird.server_url to abysslink.yaml"
  artifacts:
    - path: "internal/cli/cmd_init.go"
      provides: "runInitForm netbird backend selection + server_url prompt"
      contains: "netbird"
    - path: "internal/cli/cmd_up.go"
      provides: "--accept-no-sshcheck gate for netbird backend (D-04)"
      contains: "accept-no-sshcheck"
  key_links:
    - from: "internal/cli/cmd_up.go"
      to: "internal/config/config.go"
      via: "AcceptNoSSHCheck field persisted via audit.WriteFile after --accept-no-sshcheck flag accepted"
      pattern: "AcceptNoSSHCheck"
    - from: "internal/cli/cmd_init.go"
      to: "internal/config/config.go"
      via: "runInitForm writes Backend.Type=netbird + Server.NetBird.ServerURL to abysslink.yaml"
      pattern: "netbird"
---

<objective>
Complete the Phase 13 user-facing integration: extend the init wizard to offer the
netbird backend, and add the --accept-no-sshcheck D-04 gate to `abysslink up`.

Purpose: Without the init wizard extension, users have no interactive path to
configure a netbird backend. Without the D-04 up gate, the SSHCheck degradation
(missing checkPeriod enforcement for NetBird) is silent — violating the requirement
to never silently weaken a v1 default. The accept flag persisted once in config
closes the friction loop after the first acknowledgment.

Output: Updated cmd_init.go (wizard netbird case), updated cmd_up.go (D-04 gate),
updated cmd_init_backend_test.go. `make lint test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-netbird-backend/13-CONTEXT.md
@.planning/phases/13-netbird-backend/13-RESEARCH.md
@.planning/phases/13-netbird-backend/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: init wizard netbird backend option + --accept-no-sshcheck up gate (NB-01, NB-02, D-04)</name>
  <read_first>
    - internal/cli/cmd_init.go — full file: runInitForm function structure, backend type selection, headscale path (server_url prompt) as direct template to mirror for netbird
    - internal/cli/cmd_init_backend_test.go — existing backend selection tests to extend
    - internal/cli/cmd_up.go — up command structure; where --apply flag is checked; where modules run; location to insert the D-04 gate BEFORE the module runner executes
    - internal/config/config.go — cfg.Server.NetBird.AcceptNoSSHCheck field (added in Plan 01); cfg.Backend.Type; how config is persisted (the save/write pattern)
    - internal/audit — WriteFile for persisting AcceptNoSSHCheck=true back to abysslink.yaml
    - .planning/phases/13-netbird-backend/13-CONTEXT.md §Decisions D-04 — gate semantics: refuse unless flag present OR config field true; persist on acceptance
    - .planning/phases/13-netbird-backend/13-CONTEXT.md §post_research_resolutions PR-C — D-05 is DROPPED; do NOT set peer_login_expiration; only D-04 WARN+accept-flag
    - internal/cli/cmd_server_headscale.go — for the Phase 12 --accept-checkperiod-extension precedent (naming + persistence pattern)
  </read_first>
  <files>
    internal/cli/cmd_init.go,
    internal/cli/cmd_up.go,
    internal/cli/cmd_init_backend_test.go
  </files>
  <action>
    In `cmd_init.go`, locate `runInitForm` (the function that drives the interactive wizard). Find the existing backend type selection step (a huh.Select or similar where "tailscale" and "headscale" are options). Add `"netbird"` as a third option with description "Self-hosted NetBird control plane (REST-only, SSHCheck degradation — see docs)". When the user selects "netbird": prompt for server_url (non-empty string, must start with "https://"). Write `backend.type: netbird` and `server.netbird.server_url: <url>` to the config struct before the yaml write step. Mirror the exact headscale path (which also prompts for server_url) — the two paths are structurally identical at the YAML level, differing only in the backend type key and the specific sub-struct field.

    Print a Note (tui.Note or p.Warning) after the netbird selection that states: "NetBird does not support SSH checkPeriod enforcement. You will need to run `abysslink up --accept-no-sshcheck` on first use." This is the wizard-time degradation disclosure (D-04 UX requirement).

    In `cmd_up.go`, add a persistent (or up-local) flag `--accept-no-sshcheck` (type bool, default false). In the `up` command's RunE, BEFORE the module runner is called: if `cfg.Backend.Type == "netbird"` AND `cfg.Server.NetBird.AcceptNoSSHCheck == false` AND the `--accept-no-sshcheck` flag is not set → return an error:
    ```
    WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled.
    NetBird offers no SSH checkPeriod enforcement for setup-key-enrolled peers.
    To acknowledge this degradation and proceed, re-run with: --accept-no-sshcheck
    This flag will be persisted in abysslink.yaml so you only need it once.
    ```
    (The exact first line `WARN: SSHCheck not available on NetBird — checkPeriod enforcement disabled` must appear verbatim.)

    When `--accept-no-sshcheck` IS set AND `cfg.Server.NetBird.AcceptNoSSHCheck == false`: set `cfg.Server.NetBird.AcceptNoSSHCheck = true` in the in-memory config struct, then persist by marshaling the full config back to abysslink.yaml via `audit.WriteFile`. Use the existing config write pattern from cmd_init.go (find the function that serializes and writes abysslink.yaml, reuse it). Log via `slog.Info("netbird: AcceptNoSSHCheck acknowledged; persisting to config")` — do NOT include any secret values in this log line.

    When `--accept-no-sshcheck` IS set AND `cfg.Server.NetBird.AcceptNoSSHCheck == true`: it is already persisted — no re-write needed. Proceed to module runner.

    Do NOT implement D-05 (peer_login_expiration) — PR-C explicitly drops this. The up command must NOT call any /api/accounts REST endpoint for expiration management.

    In `cmd_init_backend_test.go`, add test cases:
    - Scripted inputs for netbird backend selection: confirm cfg.Backend.Type=="netbird" and cfg.Server.NetBird.ServerURL is populated after runInitForm
    - Verify the wizard prints the SSHCheck degradation note during netbird selection
    Use the existing headscale test case as the template (it already does scripted huh form input).

    All file writes via audit.WriteFile. No fmt.Println — slog + Printer only. Apache-2.0 header already present on both files (do not duplicate).
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go build ./internal/cli/... && grep -c 'accept-no-sshcheck' internal/cli/cmd_up.go && grep -c 'netbird' internal/cli/cmd_init.go</automated>
  </verify>
  <done>`go build ./internal/cli/...` exits 0; cmd_up.go has `accept-no-sshcheck` flag; cmd_init.go has `netbird` in the backend selection; `go test ./internal/cli/... -run TestInitBackend` passes; `make lint test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: end-to-end source audit + make lint test green (NB-01, NB-02, NB-03, NB-04)</name>
  <read_first>
    - All files modified in Plans 01–05 (do a grep scan first, listed below)
    - .planning/phases/13-netbird-backend/13-CONTEXT.md §post_research_resolutions — verify PR-A/PR-B/PR-C are fully implemented, no D-05 code present
    - internal/backend/netbird.go — confirm no fmt.Println, no sh -c, no os.WriteFile, no secrets on argv
    - internal/backend/netbird_editor.go — same hard-rule audit
    - internal/cli/cmd_server_netbird.go — same hard-rule audit
    - .golangci.yml — confirm no-netbird-agpl rule present and correct format
  </read_first>
  <files>
    internal/backend/netbird.go,
    internal/backend/netbird_editor.go,
    internal/backend/netbird_config.go,
    internal/backend/netbird_doctor.go,
    internal/cli/cmd_server_netbird.go,
    internal/cli/cmd_init.go,
    internal/cli/cmd_up.go
  </files>
  <action>
    Perform a final source-level audit across all 7 NetBird files created in this phase. This is a code-review task — read each file in the context window and verify:

    1. Apache-2.0 header present at top of every .go file
    2. No `fmt.Println` calls in any file (slog + cli Printer only)
    3. No direct `os.WriteFile` calls outside audit package — all writes via `audit.WriteFile`
    4. No `sh -c` exec patterns — all runner calls use explicit argv (verify no strings that look like shell arguments)
    5. No secrets (API key, setup key) appear as args to runner.Run or as slog fields — secrets only in env vars
    6. No `peer_login_expiration` code present (PR-C: D-05 must be completely absent)
    7. `context.Context` is the first argument on all exported methods with I/O
    8. `--accept-no-sshcheck` flag present in cmd_up.go and the exact WARN text appears verbatim
    9. D-08 Validate() is called after every policy push in netbird_editor.go
    10. `case "netbird"` present in factory.go

    Fix any issues found during the audit. Then run `make lint test` and fix all errors until it exits 0.

    Additionally: scan for any file in the codebase that now imports `github.com/netbirdio/netbird/management` or `combined` or `relay` or `signal` — this must return zero results. Run: `grep -r 'netbirdio/netbird/management\|netbirdio/netbird/combined\|netbirdio/netbird/relay\|netbirdio/netbird/signal' --include='*.go' .` — must return empty.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && make lint test 2>&1 | tail -10 && grep -r 'netbirdio/netbird/management' --include='*.go' . | wc -l</automated>
  </verify>
  <done>`make lint test` exits 0; grep for netbirdio/netbird/management returns 0 matches; no fmt.Println in netbird_*.go files; no os.WriteFile outside audit package; no peer_login_expiration code anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → init wizard | Server URL input; must not prompt for secrets |
| cmd_up → config write | AcceptNoSSHCheck persisted via audit (backup + log); no secret values in config |
| AGPLv3 boundary | Verified by final grep scan in Task 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Info Disclosure | Secrets in init wizard prompts | mitigate | Wizard only prompts for non-secret values (backend type, server URL); API key and setup key are never prompted in init — keychain and env-var instructions provided |
| T-13-05-02 | Tampering | D-05 code silently present (peer_login_expiration) | mitigate | Task 2 audit explicitly checks for peer_login_expiration and must return zero matches; PR-C drop is verified at code level |
| T-13-05-03 | Elevation of Privilege | SSHCheck degradation silently accepted | mitigate | D-04 gate in cmd_up.go refuses without explicit --accept-no-sshcheck; exact WARN text appears before refusal error; persisted once so friction is minimal after first acknowledgment |
| T-13-05-04 | Tampering | AGPLv3 imports creeping in | mitigate | Task 2 grep scan + depguard CI rule both confirm zero netbird/management imports; belt-and-suspenders |
| T-13-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
1. `go build ./internal/cli/...` exits 0
2. `grep -c 'accept-no-sshcheck' internal/cli/cmd_up.go` returns >= 1
3. `grep -c 'netbird' internal/cli/cmd_init.go` returns >= 1 (wizard option)
4. `grep -c 'AcceptNoSSHCheck' internal/cli/cmd_up.go` returns >= 1 (persistence)
5. `grep -r 'netbirdio/netbird/management' --include='*.go' . | wc -l` returns 0
6. `grep -r 'peer_login_expiration' --include='*.go' . | wc -l` returns 0 (PR-C)
7. `go test ./internal/cli/... -run TestInitBackend` passes
8. `make lint test` exits 0
</verification>

<success_criteria>
- `abysslink init` wizard offers netbird backend with server_url prompt and SSHCheck degradation note
- `abysslink up --apply` with netbird backend refuses without --accept-no-sshcheck (first run)
- After --accept-no-sshcheck: AcceptNoSSHCheck=true persisted to abysslink.yaml via audit
- Subsequent up runs with AcceptNoSSHCheck=true in config: proceed without flag
- No D-05 / peer_login_expiration code anywhere in the codebase
- No AGPLv3 imports (grep + depguard both confirm)
- make lint test exits 0
</success_criteria>

<output>
Create `.planning/phases/13-netbird-backend/13-05-SUMMARY.md` when done
</output>
