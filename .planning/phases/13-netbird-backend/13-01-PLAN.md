---
phase: 13-netbird-backend
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .golangci.yml
  - internal/config/config.go
  - internal/backend/capabilities.go
  - internal/backend/contract_test.go
  - internal/backend/testdata/netbird/config_minimal.yaml
  - internal/backend/testdata/netbird/config_zitadel.yaml
  - internal/backend/testdata/netbird/policy_deny_all.json
  - docs/RELEASE-CHECKLIST.md
autonomous: true
requirements:
  - NB-04
  - NB-01
  - NB-02

must_haves:
  truths:
    - "golangci-lint depguard rule `no-netbird-agpl` rejects any import of `github.com/netbirdio/netbird/management`, `combined`, `relay`, or `signal` packages"
    - "config.Config.Server.NetBird struct compiles with fields for ServerURL, MgmtBindAddr, SetupKeyExpiry, BinaryPath, and VersionFloor"
    - "Capabilities struct SSHCheck field is already present (confirmed existing)"
    - "testdata/netbird/ fixture directory exists with minimal config, zitadel config, and deny-all policy golden files"
    - "docs/RELEASE-CHECKLIST.md contains explicit AGPLv3 review item for v2.0.0"
  artifacts:
    - path: ".golangci.yml"
      provides: "AGPLv3 depguard deny rule (no-netbird-agpl)"
      contains: "no-netbird-agpl"
    - path: "internal/config/config.go"
      provides: "NetBird server provisioning config stanza"
      contains: "NetBird"
    - path: "internal/backend/testdata/netbird/config_minimal.yaml"
      provides: "Minimal NetBird config.yaml fixture for surgical-merge tests"
    - path: "internal/backend/testdata/netbird/policy_deny_all.json"
      provides: "Golden deny-all policy fixture for Validate() tests"
    - path: "docs/RELEASE-CHECKLIST.md"
      provides: "v2.0.0 AGPLv3 review item"
      contains: "AGPLv3"
  key_links:
    - from: ".golangci.yml"
      to: "github.com/netbirdio/netbird/management"
      via: "depguard no-netbird-agpl deny rule"
      pattern: "no-netbird-agpl"
    - from: "internal/config/config.go"
      to: "internal/backend/netbird.go"
      via: "cfg.Server.NetBird fields consumed by netbirdAdapter"
      pattern: "NetBird struct"
---

<objective>
Lay the foundation that all NetBird plans build on: the AGPLv3 CI import guard,
the config schema extension for NetBird server provisioning, the testdata fixture
directory, and an updated contract_test.go to accept a future `netbird` case.
Also add the v2.0.0 AGPLv3 review item to the release checklist.

Purpose: NB-04 requires a CI-enforced depguard rule before any netbird code lands;
the config schema and test fixtures must exist before the adapter and doctor code
can reference them. This wave-1 plan has no dependencies and unblocks all others.

Output: Updated .golangci.yml, extended config.Config, testdata fixtures, release
checklist entry, and a verified `make lint test` green baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-netbird-backend/13-CONTEXT.md
@.planning/phases/13-netbird-backend/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGPLv3 depguard rule + release checklist entry (NB-04)</name>
  <read_first>
    - .golangci.yml — existing depguard.rules format to extend (no-concrete-backend rule as template)
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §RF-6 — exact depguard YAML to add
    - docs/RELEASE-CHECKLIST.md — location to insert AGPLv3 review item (create file if absent)
  </read_first>
  <files>.golangci.yml, docs/RELEASE-CHECKLIST.md</files>
  <action>
    Extend .golangci.yml depguard.rules with a new `no-netbird-agpl` rule (per NB-04 + RF-6). The rule uses `list-mode: lax` and denies four prefixes: `github.com/netbirdio/netbird/management`, `github.com/netbirdio/netbird/combined`, `github.com/netbirdio/netbird/relay`, `github.com/netbirdio/netbird/signal`. Each deny entry has a `desc` field naming the AGPLv3 reason. The rule has NO `files:` scope restriction — it applies repo-wide (unlike no-concrete-backend which has a files: scope). Do NOT add `github.com/netbirdio/netbird` itself to the deny list — the netbird CLIENT packages are permissively licensed; only the server/management packages are AGPLv3.

    Create or update docs/RELEASE-CHECKLIST.md with a v2.0.0 milestone section that includes: "[ ] AGPLv3 legal review: confirm no github.com/netbirdio/netbird/management/... package is transitively imported in the final binary (run `go mod why github.com/netbirdio/netbird/management` — must return 'does not need'); engage legal review if any AGPLv3 transitive import is found." Apache-2.0 header NOT required in RELEASE-CHECKLIST.md (it is a docs file, not a Go source file).

    Verification: run `golangci-lint run ./...` — must exit 0 on the current codebase (no new violations introduced yet, since netbird packages are not imported anywhere).
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && golangci-lint run ./... 2>&1 | tail -5 && grep -c 'no-netbird-agpl' .golangci.yml</automated>
  </verify>
  <done>golangci-lint exits 0; `grep -c 'no-netbird-agpl' .golangci.yml` returns >= 1; docs/RELEASE-CHECKLIST.md contains "AGPLv3 legal review" line.</done>
</task>

<task type="auto">
  <name>Task 2: Config schema extension for NetBird server provisioning (NB-01, NB-02)</name>
  <read_first>
    - internal/config/config.go — existing Config struct, Server sub-struct, Headscale fields as template
    - .planning/phases/13-netbird-backend/13-CONTEXT.md §Canonical Refs — config.Server.NetBird stanza fields needed
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §RF-5 — setup key fields, mgmt bind
    - internal/backend/capabilities.go — confirm SSHCheck field already present (it is; no change needed)
  </read_first>
  <files>internal/config/config.go</files>
  <action>
    Add a `NetBirdServer` struct and a `NetBird NetBirdServer` field to the existing `Server` struct in config.go. The `NetBirdServer` struct requires these fields:
    - `ServerURL string` — management server base URL (e.g. `https://nb.example.com`)
    - `MgmtBindAddr string` — management listen address; paranoid default `127.0.0.1:443`
    - `MetricsAddr string` — metrics bind address; paranoid default `127.0.0.1:9090`
    - `BinaryPath string` — path to user-supplied netbird-server binary (PR-B); empty = not configured
    - `SetupKeyExpiry string` — duration string for setup key TTL; paranoid default `24h`
    - `AcceptNoSSHCheck bool` — persisted one-time opt-in for D-04 (`--accept-no-sshcheck`); default false
    - `TLSCertFile string` — BYO TLS cert path
    - `TLSKeyFile string` — BYO TLS key path
    - `ConfigPath string` — path to netbird-server config.yaml; default `/etc/netbird/config.yaml`

    YAML struct tags must use `yaml:"..."` with snake_case names matching the existing Headscale convention. Add a `Defaults()` method update (or inline if Defaults is already inline) so that empty-string fields get their paranoid defaults: MgmtBindAddr `127.0.0.1:443`, MetricsAddr `127.0.0.1:9090`, SetupKeyExpiry `24h`, ConfigPath `/etc/netbird/config.yaml`.

    Add strict-mode YAML validation guard: if `Backend.Type == "netbird"` and `Server.NetBird.ServerURL == ""`, the `Validate()` function returns an error.

    Apache-2.0 header must already be present on config.go — do not duplicate it, only add the new struct.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go build ./internal/config/... && grep -c 'NetBirdServer' internal/config/config.go</automated>
  </verify>
  <done>`go build ./internal/config/...` exits 0; `grep -c 'NetBirdServer' internal/config/config.go` >= 1; all existing config tests still pass (`go test ./internal/config/...`).</done>
</task>

<task type="auto">
  <name>Task 3: testdata fixtures + contract_test.go netbird placeholder (NB-01, NB-02)</name>
  <read_first>
    - internal/backend/testdata/ — existing testdata structure for headscale (directory layout to mirror)
    - internal/backend/contract_test.go — existing contract test structure; add netbird skip/placeholder
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §RF-5 — policy rule body JSON shape for deny_all.json golden
    - .planning/phases/13-netbird-backend/13-RESEARCH.md §RF-7 — config_zitadel.yaml shape (server.auth.issuer contains "zitadel")
  </read_first>
  <files>
    internal/backend/testdata/netbird/config_minimal.yaml,
    internal/backend/testdata/netbird/config_zitadel.yaml,
    internal/backend/testdata/netbird/policy_deny_all.json,
    internal/backend/contract_test.go
  </files>
  <action>
    Create `internal/backend/testdata/netbird/` directory with three fixture files:

    1. `config_minimal.yaml` — a minimal valid netbird-server config.yaml with fields: `disableAnonymousMetrics: true`, `disableGeoliteUpdate: true`, `metricsPort: 9090`, `server.listenAddress: "127.0.0.1:443"`, `server.auth.issuer: "http://localhost:5556/dex"` (Dex — NOT zitadel). No TLS keys (tests can inject them).

    2. `config_zitadel.yaml` — same as minimal but with `server.auth.issuer: "https://zitadel.example.com"` to trigger nb-zitadel detection in tests.

    3. `policy_deny_all.json` — golden fixture matching the deny-all policy POST body shape from RESEARCH.md RF-5: `{"name":"abysslink-deny-all","description":"Abysslink deny-all baseline — pushed before first node admission","enabled":true,"rules":[{"name":"deny-all","enabled":true,"action":"drop","protocol":"all","bidirectional":true,"sources":["__ALL_GROUP_ID__"],"destinations":["__ALL_GROUP_ID__"],"ports":[],"port_ranges":[]}]}`. Use the placeholder `__ALL_GROUP_ID__` so tests can inject the actual group ID.

    In `contract_test.go`, add a `TestNetBirdAdapterPlaceholder` function (or update the existing `TestBackendFactory` table if one exists) that skips with `t.Skip("netbird adapter not yet implemented — wave 2")`. This ensures the test file compiles and the skip message is clear. Do NOT yet instantiate `newNetBirdAdapter` (it does not exist yet).

    Apache-2.0 header on any new Go test files; YAML/JSON fixtures do not need headers.
  </action>
  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go test ./internal/backend/... 2>&1 | grep -v "^ok\|^---\|SKIP\|no test" | head -20 && ls internal/backend/testdata/netbird/</automated>
  </verify>
  <done>All three fixture files exist in `testdata/netbird/`; `go test ./internal/backend/...` exits 0 (netbird placeholder test shows SKIP); `make lint test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD → go compiler | depguard rule enforces no AGPLv3 imports at build time |
| developer → config.go | NetBirdServer config struct must not expose secret fields in YAML (API key, setup key are keychain-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | .golangci.yml depguard rule | mitigate | Rule is version-controlled; CI enforces it on every PR; prefix match blocks entire `netbird/management` subtree (RF-6 confirmed) |
| T-13-01-02 | Info Disclosure | config.Config.Server.NetBird YAML fields | mitigate | ServerURL, paths, bind addresses are non-secret; API key and setup key are NOT fields in this struct — they live in OS keychain only |
| T-13-01-03 | Tampering | AGPLv3 license contamination | mitigate | depguard no-netbird-agpl rule; release checklist explicit review item added in Task 1 |
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — stdlib + existing go.mod only |
</threat_model>

<verification>
1. `golangci-lint run ./...` exits 0 — no new lint errors from config.go additions
2. `go build ./...` exits 0 — new config struct compiles cleanly
3. `go test ./internal/config/...` exits 0 — existing config tests pass
4. `go test ./internal/backend/...` exits 0 — netbird placeholder test skips cleanly
5. `grep 'no-netbird-agpl' .golangci.yml` returns a match
6. `ls internal/backend/testdata/netbird/` shows three fixture files
7. `grep 'AGPLv3' docs/RELEASE-CHECKLIST.md` returns a match
8. `make lint test` exits 0
</verification>

<success_criteria>
- .golangci.yml has a repo-wide no-netbird-agpl depguard rule that denies management/combined/relay/signal prefixes
- config.Config.Server.NetBird struct has all required provisioning fields with paranoid defaults
- testdata/netbird/ has config_minimal.yaml, config_zitadel.yaml, policy_deny_all.json
- contract_test.go has a netbird placeholder test that skips
- docs/RELEASE-CHECKLIST.md has explicit AGPLv3 review item
- make lint test exits 0
</success_criteria>

<output>
Create `.planning/phases/13-netbird-backend/13-01-SUMMARY.md` when done
</output>
