---
phase: 19-web-ui-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - .goreleaser.yaml
  - go.mod
  - go.sum
  - abysslink.yaml.example
autonomous: true
requirements:
  - WEB-01
  - WEB-02

must_haves:
  truths:
    - "config.Validate rejects webui.bind_addr set to 0.0.0.0 or :: (D-01, WEB-02)"
    - "config.Validate rejects webui.read_only: false (D-01, WEB-02)"
    - "goreleaser snapshot produces a separate abysslinkd-webui binary with -tags webui (D-01, WEB-01)"
    - "go.mod contains tailscale.com at v1.98.5 or later"
    - "abysslink.yaml.example has webui block with enabled: false default"
  artifacts:
    - path: "internal/config/config.go"
      provides: "WebUI config struct + ValidateWebUI function"
      contains: "type WebUI struct"
    - path: ".goreleaser.yaml"
      provides: "abysslinkd-webui build entry"
      contains: "abysslinkd-webui"
  key_links:
    - from: "internal/config/config.go"
      to: "config.Validate"
      via: "call to ValidateWebUI"
      pattern: "ValidateWebUI"
    - from: ".goreleaser.yaml"
      to: "cmd/abysslinkd"
      via: "tags: [webui]"
      pattern: "tags:.*webui"
---

<objective>
Add the WebUI config block to internal/config/config.go with schema-level
validation (WEB-02 HARD FLOOR: reject 0.0.0.0/:: bind AND read_only:false),
and add the separate abysslinkd-webui goreleaser build entry (WEB-01). Also
add tailscale.com to go.mod so the webui-tagged build can import
tailscale.com/client/local and tailscale.com/safeweb.

Purpose: Establishes the schema contract that ALL later webui plans rely on.
ValidateWebUI is the config-layer half of the two-layer read-only gate (the
HTTP-layer half is in Plan 04). The goreleaser entry is a prerequisite for WEB-01.

Output: Updated config.go (WebUI struct + ValidateWebUI), updated .goreleaser.yaml
(abysslinkd-webui build + archive entry), updated go.mod/go.sum, updated
abysslink.yaml.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-web-ui-dashboard/19-CONTEXT.md
@.planning/phases/19-web-ui-dashboard/19-RESEARCH.md
@internal/config/config.go
@.goreleaser.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WebUI config struct and ValidateWebUI to config.go</name>
  <files>internal/config/config.go</files>
  <read_first>
    - internal/config/config.go — full file; copy the ObservabilityMetrics struct and
      ValidateObservability + IsUnspecifiedBindAddr pattern exactly (lines 56-514);
      the new WebUI struct mirrors ObservabilityMetrics; ValidateWebUI mirrors
      ValidateObservability with TWO extra checks: reject read_only:false.
    - .planning/phases/19-web-ui-dashboard/19-CONTEXT.md — decisions section,
      especially the "schema rejects 0.0.0.0/:: bind AND read_only: false" contract.
    - .planning/phases/19-web-ui-dashboard/19-RESEARCH.md — WEB-02 row in
      Phase Requirements table; IsUnspecifiedBindAddr reuse note.
  </read_first>
  <behavior>
    - Test: ValidateWebUI returns nil when webui.enabled is false (zero value is safe disabled state).
    - Test: ValidateWebUI returns nil when webui.enabled is true with a valid tailnet IP bind addr.
    - Test: ValidateWebUI returns a non-nil error when webui.bind_addr is "0.0.0.0:8443" (WEB-02).
    - Test: ValidateWebUI returns a non-nil error when webui.bind_addr is "::".
    - Test: ValidateWebUI returns a non-nil error when webui.read_only is false (WEB-02).
    - Test: Validate (top-level) propagates ValidateWebUI errors.
  </behavior>
  <action>
    Add the WebUI config struct and ValidateWebUI function to internal/config/config.go.

    Add to the Config struct after the Observability field (per D-01):
      WebUI WebUIConfig yaml:"webui"

    Define WebUIConfig struct (all fields yaml-tagged for KnownFields strict mode):
      Enabled   bool   yaml:"enabled"          // default false — listener never starts unless explicitly opted in (WEB-01)
      BindAddr  string yaml:"bind_addr,omitempty"
      Port      int    yaml:"port,omitempty"   // default 0 → use 8443
      ReadOnly  bool   yaml:"read_only"        // MUST be true; false is rejected by ValidateWebUI (WEB-02)
      AllowNotify bool yaml:"allow_notify,omitempty" // scaffolded, default false (WEB-05)

    Add Defaults() initialization for WebUI:
      WebUI: WebUIConfig{ReadOnly: true, Port: 8443}

    Add ValidateWebUI exported function (mirrors ValidateObservability from line 494):
    - When webui.bind_addr is non-empty and IsUnspecifiedBindAddr returns true, return
      fmt.Errorf with message: "config: webui.bind_addr %q must not be 0.0.0.0 or :: — webui must bind to the tailnet IP only (WEB-02)"
    - When webui.enabled is true and webui.read_only is false, return
      fmt.Errorf with message: "config: webui.read_only must be true — mutations are disabled in the web UI (WEB-02)"
    - Otherwise return nil.

    Wire ValidateWebUI into Validate() after ValidateObservability (same call pattern
    at config.go:458). The call is unconditional — ValidateWebUI is a no-op when
    webui.enabled is false except for the bind_addr check (which applies even when
    disabled, same as metrics pattern).

    License header (Apache-2.0 short form) is already present — do not duplicate.

    Write tests in internal/config/validate_webui_test.go (new file, standard
    package config_test + testify assertions). Cover all six behavior cases above.
  </action>
  <verify>
    <automated>go test ./internal/config/... -run TestValidateWebUI -count=1 -timeout 30s</automated>
  </verify>
  <done>
    ValidateWebUI rejects 0.0.0.0/:: bind and read_only:false. Tests pass.
    WebUIConfig struct is in config.Config. Validate() calls ValidateWebUI.
    Defaults() sets ReadOnly:true and Port:8443.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add abysslinkd-webui goreleaser build + archive entry and update go.mod</name>
  <files>.goreleaser.yaml, go.mod, go.sum, abysslink.yaml.example</files>
  <read_first>
    - .goreleaser.yaml — full file; understand the existing abysslinkd build entry
      (id: abysslinkd, lines 35-55) and archives section (lines 57-72); the new
      build must mirror abysslinkd exactly but add tags: [webui], id: abysslinkd-webui,
      binary: abysslinkd-webui; the new archive entry uses ids: [abysslinkd-webui].
    - .planning/phases/19-web-ui-dashboard/19-RESEARCH.md — Pattern 7 (goreleaser
      second artifact, lines 477-506) — exact YAML field layout. mod_timestamp must
      be "{{ .CommitTimestamp }}" (reproducible builds per SCH-06 / Phase 16 decision).
      The ldflags block must match the existing abysslinkd ldflags exactly (same
      -X flags, same -s -w, same bundleURL/slsaURL).
    - .planning/phases/19-web-ui-dashboard/19-CONTEXT.md — "goreleaser: SEPARATE
      abysslinkd-webui build (-tags webui) + SEPARATE archives entry" critical locked fact.
  </read_first>
  <action>
    Step 1 — Update go.mod: Run go get tailscale.com@v1.98.5 from the repo root.
    This adds tailscale.com to go.mod and resolves transitive deps (gorilla/csrf
    and others) into go.sum. Verify with: go mod tidy.

    Step 2 — Add abysslinkd-webui build to .goreleaser.yaml:
    Append a new builds entry after the existing abysslinkd entry (after line 55).
    Copy the abysslinkd build block verbatim and change:
      id: abysslinkd-webui
      binary: abysslinkd-webui
    Add inside the entry (after the existing flags block):
      tags:
        - webui
    Keep all ldflags identical to abysslinkd including the bundleURL and slsaURL
    -X flags. Keep mod_timestamp: "{{ .CommitTimestamp }}". Keep CGO_ENABLED=0.
    Keep goos/goarch: darwin/linux x amd64/arm64.

    Step 3 — Add abysslinkd-webui archive entry to .goreleaser.yaml:
    Append a new archives entry after the existing default archive:
      - id: webui
        ids:
          - abysslinkd-webui
        name_template: "{{ .ProjectName }}-webui_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
        format_overrides:
          - goos: windows
            formats:
              - zip
        files:
          - LICENSE
          - README.md
          - NOTICE
          - abysslink.yaml.example

    Step 4 — Update abysslink.yaml.example: add a webui block after the
    observability block:
      webui:
        enabled: false
        bind_addr: ""   # resolved to tailnet IP at runtime; never set to 0.0.0.0 or ::
        port: 8443
        read_only: true # MUST be true — mutations are disabled in the web UI (WEB-02)

    Do NOT add allow_notify to the example (it is a scaffolded capability not yet
    exposed to users in Phase 19).
  </action>
  <verify>
    <automated>goreleaser check --config .goreleaser.yaml 2>&amp;1 | grep -v "^WARN" | grep -c "^" ; echo $?</automated>
  </verify>
  <done>
    goreleaser check passes (exit 0, no ERR lines). abysslinkd-webui build entry
    with tags:[webui] present. Separate archive entry with ids:[abysslinkd-webui] present.
    go.mod contains tailscale.com. abysslink.yaml.example has webui block.
    go test ./internal/config/... still passes (no regressions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config → config.Validate | Untrusted user-supplied YAML values cross here; ValidateWebUI is the gate |
| go.sum integrity | Dependency graph integrity; tailscale.com module hash must be verified by go mod tidy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | config.Validate / ValidateWebUI | mitigate | ValidateWebUI rejects 0.0.0.0/:: bind and read_only:false at config load time; errors are fatal |
| T-19-02 | Information Disclosure | .goreleaser.yaml webui build | mitigate | Build tag `-tags webui` is the only artifact that links Tailscale SDK; base abysslinkd remains SDK-free |
| T-19-03 | Tampering | go.mod / go.sum | mitigate | go mod tidy + go.sum GONOSUMCHECK='' ensures the module hash is recorded; CI verifies go.sum is not dirty |
| T-19-SC | Tampering | tailscale.com Go module add | mitigate | tailscale.com is the official Tailscale SDK monorepo (Package Legitimacy Audit in RESEARCH.md, Approved); no slopcheck checkpoint required |
</threat_model>

<verification>
Run after both tasks complete:

  go test ./internal/config/... -count=1 -timeout 60s
  goreleaser check --config .goreleaser.yaml

Base binary must still build cleanly (webui package does not exist yet — no symbols to check):
  go build -o /dev/null ./cmd/abysslinkd

Config validates correctly:
  go test ./internal/config/... -run TestValidateWebUI -v -count=1
</verification>

<success_criteria>
- go test ./internal/config/... green (including TestValidateWebUI: 6 sub-cases)
- goreleaser check exits 0 with no ERR lines
- go build ./cmd/abysslinkd exits 0 (no regressions)
- go.mod contains tailscale.com at v1.98.5 or later
- .goreleaser.yaml has two builds (abysslinkd, abysslinkd-webui) and two archives (default, webui)
- abysslink.yaml.example contains webui block with enabled: false and read_only: true
</success_criteria>

<output>
Create .planning/phases/19-web-ui-dashboard/19-01-SUMMARY.md when done
</output>
