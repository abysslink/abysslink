---
phase: 18-observability-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metrics/doc.go
  - internal/metrics/registry.go
  - internal/metrics/labels.go
  - internal/metrics/registry_test.go
  - internal/metrics/labels_test.go
  - internal/modules/deps.go
  - internal/config/config.go
  - internal/config/config_test.go
  - .golangci.yml
autonomous: true
requirements: [OBS-01, OBS-03, OBS-04]

must_haves:
  truths:
    - "internal/metrics.Registry interface exists with Counter(), Gauge(), and Snapshot() methods; NoopRegistry satisfies it and is nil-safe (all methods no-op, never panic)"
    - "labelAllowlist compile-time set permits only {severity, check_id, backend, result, rig}; sanitizeLabel maps any name not in the set to the string 'other'"
    - "opaqueRigLabel hashes the rig name via SHA-256 truncated to 8 bytes (16 hex chars) — deterministic, never returns hostname/IP/node_id"
    - "modules.Deps has a Registry field of type metrics.Registry; zero-value Deps has NoopRegistry wired"
    - "config.Config has an Observability field with nested Metrics{Enabled, BindAddr, Port} and Digest{Enabled, Hour, NtfyTopic} sub-structs; Defaults() keeps all OFF"
    - "config.Validate rejects observability.metrics.bind_addr containing '0.0.0.0' or '::' with an error message containing 'OBS-03'"
    - "depguard no-prometheus-client rule in .golangci.yml uses list-mode: lax + deny entry for github.com/prometheus/client_golang; make lint fails on any import of that package"
  artifacts:
    - path: "internal/metrics/registry.go"
      provides: "Registry interface, Counter interface, Gauge interface, MetricFamily, Sample, memRegistry, NoopRegistry"
      exports: ["Registry", "Counter", "Gauge", "MetricFamily", "Sample", "NoopRegistry", "NewMemRegistry"]
    - path: "internal/metrics/labels.go"
      provides: "labelAllowlist, sanitizeLabel, opaqueRigLabel"
      exports: ["SanitizeLabel", "OpaqueRigLabel"]
    - path: "internal/metrics/doc.go"
      provides: "package doc"
    - path: "internal/config/config.go"
      provides: "Observability, ObservabilityMetrics, ObservabilityDigest config structs; validateObservability"
      contains: "Observability"
    - path: ".golangci.yml"
      provides: "no-prometheus-client depguard rule"
      contains: "no-prometheus-client"
  key_links:
    - from: "internal/modules/deps.go"
      to: "internal/metrics/registry.go"
      via: "Deps.Registry field"
      pattern: "Registry.*metrics\\.Registry"
    - from: "internal/config/config.go"
      to: "Validate"
      via: "validateObservability called from Validate"
      pattern: "validateObservability"
---

<objective>
Lay the metrics foundation: define the hand-rolled Registry interface (OBS-01 per D-03), add the Observability config block (OBS-03), register the depguard total ban on prometheus/client_golang (OBS-01), and wire NoopRegistry into modules.Deps (OBS-01, OBS-04).

Purpose: Every subsequent plan (daemon metrics_server, digest, report) depends on these types being defined. Get the compile-time contracts and lint gates right first.

Output: internal/metrics package (registry.go, labels.go, doc.go + tests), config.Observability struct + validateObservability, .golangci.yml no-prometheus-client rule, modules.Deps.Registry field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-observability-metrics/18-CONTEXT.md
@.planning/phases/18-observability-metrics/18-RESEARCH.md

Key source files (read before writing):
@internal/modules/deps.go
@internal/config/config.go
@.golangci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal/metrics package — Registry interface, NoopRegistry, memRegistry</name>
  <files>internal/metrics/doc.go, internal/metrics/registry.go, internal/metrics/registry_test.go</files>

  <read_first>
    Read internal/modules/module.go lines 1-75 to understand the existing interface pattern (Severity, Finding, Module).
    Read internal/audit/audit.go lines 1-50 for the Apache-2.0 license header short-form used by every file.
    Read internal/modules/deps.go for the full Deps struct before adding the Registry field.
    Read .planning/phases/18-observability-metrics/18-RESEARCH.md sections "Registry Interface", "NoopRegistry", and "Code Examples" for the exact method signatures.
  </read_first>

  <behavior>
    - TestNoopRegistryNilSafe: call NoopRegistry{}.Counter("c","help",nil).Inc(), .Add(5), .Value() — all no-ops, no panic
    - TestNoopRegistryGauge: NoopRegistry{}.Gauge("g","help",nil).Set(3.14), .Value() returns 0.0
    - TestNoopRegistrySnapshot: NoopRegistry{}.Snapshot() returns nil (zero length), no panic
    - TestMemRegistryCounter: NewMemRegistry().Counter("req","help",map[string]string{"result":"ok"}).Inc() x3; Snapshot contains one MetricFamily named "req" with Type "counter" and Sample Value 3.0
    - TestMemRegistryGauge: NewMemRegistry().Gauge("up","help",nil).Set(1.0); Snapshot MetricFamily Type "gauge", Sample Value 1.0
    - TestMemRegistryConcurrent: 20 goroutines each calling Inc() 50 times on the same counter; after WaitGroup, Value() == 1000.0 (no race with -race flag)
    - TestMemRegistrySameKeyReturnsIdentical: calling Counter twice with same name+labels returns the same logical counter (Inc on first, value visible on second)
  </behavior>

  <action>
    Create internal/metrics/doc.go with the Apache-2.0 license header (short form matching existing files) and the package doc comment: "Package metrics defines the Registry interface and hand-rolled implementations. No prometheus/client_golang import is permitted anywhere in this module (OBS-01 depguard rule in .golangci.yml)."

    Create internal/metrics/registry.go with the Apache-2.0 license header and the following types, all exported:

    Registry interface with three methods: Counter(name, help string, labels map[string]string) Counter; Gauge(name, help string, labels map[string]string) Gauge; Snapshot() []MetricFamily.

    Counter interface: Inc(); Add(delta int64); Value() float64.

    Gauge interface: Set(v float64); Value() float64.

    MetricFamily struct: Name string, Help string, Type string (values "counter" or "gauge"), Samples []Sample.

    Sample struct: Labels map[string]string, Value float64.

    NoopRegistry struct (zero value, no fields). Implement Registry. Counter returns noopCounter{}; Gauge returns noopGauge{}; Snapshot returns nil. noopCounter and noopGauge are unexported zero-value structs satisfying Counter and Gauge respectively (all methods no-op; Value() returns 0).

    NewMemRegistry() Registry — returns a *memRegistry. The memRegistry holds a sync.RWMutex and a map[string]*metricEntry (keyed by a canonical key: name + sorted label pairs). metricEntry stores: name string, help string, metricType string, labels map[string]string, value atomic int64 (use sync/atomic.Int64). Counter.Inc() calls Add(1). Counter.Add(delta) does atomic add. Counter.Value() returns float64(atomic load). Gauge.Set(v) converts v to int64 bits via math.Float64bits and stores atomically. Gauge.Value() loads and converts back via math.Float64frombits. Snapshot() acquires RLock, iterates map, builds []MetricFamily grouped by name (sort families by Name for determinism). The canonical key for a metricEntry is built by sorting the label map keys and formatting as "name|k1=v1|k2=v2".

    IMPORTANT: No fenced code blocks here — see the reference patterns in 18-RESEARCH.md "Code Examples" section. Do NOT import github.com/prometheus/client_golang. Do NOT use fmt.Println anywhere. Use log/slog for any diagnostic logging. Every file needs the Apache-2.0 license header.

    Add the Registry field to internal/modules/deps.go: Registry metrics.Registry — import "github.com/abysslink/abysslink/internal/metrics". Wire the default: in cmd/abysslinkd/main.go (Plan 04) and CLI Deps builder (Plan 03); Plan 01 only adds the field. The Registry field comment must say: "Registry is the metrics sink for all modules. Use NoopRegistry when metrics are disabled or unavailable — modules must call Registry methods unconditionally (never nil-check before calling)."
  </action>

  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go test ./internal/metrics/... -run TestNoop -count=1 -timeout 30s && go test ./internal/metrics/... -run TestMem -count=1 -race -timeout 30s</automated>
  </verify>

  <done>All TestNoop* and TestMem* pass with -race; internal/metrics compiles with go build ./internal/metrics/...; modules.Deps.Registry field exists and the package compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Label allowlist, sanitizeLabel, opaqueRigLabel, config.Observability, .golangci.yml depguard rule</name>
  <files>internal/metrics/labels.go, internal/metrics/labels_test.go, internal/config/config.go, internal/config/config_test.go, .golangci.yml</files>

  <read_first>
    Read internal/config/config.go lines 1-50 and 396-465 (Validate + validateBackend) for the exact error format and call structure.
    Read .golangci.yml in full to understand the existing depguard rule shapes (no-concrete-backend, no-netbird-agpl use list-mode: lax + deny).
    Read .planning/phases/18-observability-metrics/18-RESEARCH.md sections "Label Allowlist and sanitizeLabel", "Pattern 3: config.Validate Bind-Address Rejection", and "Pattern 7: depguard Total Ban".
  </read_first>

  <behavior>
    - TestSanitizeLabelAllowed: "severity", "check_id", "backend", "result", "rig" each return themselves unchanged
    - TestSanitizeLabelRejected: "hostname", "topic", "user", "node_id", "ip", "password", "" each return "other"
    - TestOpaqueRigLabelDeterministic: opaqueRigLabel("laptop-01") called twice returns the same 16-char hex string
    - TestOpaqueRigLabelNonReversible: the returned string does not contain "laptop", "01", or any substring of the input
    - TestOpaqueRigLabelLength: always returns exactly 16 hex characters (8 bytes SHA-256 prefix)
    - TestValidateObservabilityRejects0000: config.Validate on a valid base config with Observability.Metrics.BindAddr="0.0.0.0:9090" returns non-nil error containing "OBS-03"
    - TestValidateObservabilityRejectsDoubleColon: BindAddr="::" returns error containing "OBS-03"
    - TestValidateObservabilityAcceptsEmpty: BindAddr="" (empty, runtime-resolved) passes validation
    - TestValidateObservabilityAcceptsTailnetIP: BindAddr="100.64.0.1" passes validation
    - TestDefaultsObservabilityOff: config.Defaults().Observability.Metrics.Enabled == false; config.Defaults().Observability.Digest.Enabled == false
  </behavior>

  <action>
    Create internal/metrics/labels.go with the Apache-2.0 license header. Define the unexported labelAllowlist as map[string]struct{} containing exactly: "severity", "check_id", "backend", "result", "rig". Export SanitizeLabel(name string) string — returns name if in allowlist, otherwise "other". Export OpaqueRigLabel(rigName string) string — computes sha256.Sum256([]byte(rigName)), returns fmt.Sprintf("%x", sum[:8]). Import: crypto/sha256, fmt.

    In internal/config/config.go:

    Add the Observability field to the Config struct: Observability Observability `yaml:"observability"`. Add the Observability struct with yaml tags after the Hardening struct: type Observability struct { Metrics ObservabilityMetrics `yaml:"metrics"`; Digest ObservabilityDigest `yaml:"digest"` }. type ObservabilityMetrics struct { Enabled bool `yaml:"enabled"`; BindAddr string `yaml:"bind_addr,omitempty"`; Port int `yaml:"port,omitempty"` }. type ObservabilityDigest struct { Enabled bool `yaml:"enabled"`; Hour int `yaml:"hour,omitempty"`; NtfyTopic string `yaml:"ntfy_topic,omitempty"` }. Do NOT set Observability in Defaults() (zero value is correct: all false, empty, zero). Add a call to validateObservability(cfg) at the end of Validate, before the return nil, after validateBackend. Implement validateObservability(cfg *Config) error: if addr := cfg.Observability.Metrics.BindAddr; addr != "" && (strings.Contains(addr, "0.0.0.0") || strings.Contains(addr, "::")) { return fmt.Errorf("config: observability.metrics.bind_addr %q must not be 0.0.0.0 or :: — metrics must bind to the tailnet IP only (OBS-03)") }. Return nil otherwise.

    In .golangci.yml depguard.rules section, add a new rule after no-netbird-agpl named no-prometheus-client: list-mode: lax; deny: [{pkg: "github.com/prometheus/client_golang", desc: "OBS-01: metrics are hand-rolled in internal/daemon/metrics_server.go — no prometheus client lib permitted anywhere in this module"}]. Do NOT add a files: scope — list-mode: lax with no files scope applies to all files globally (matching the no-netbird-agpl pattern).

    In internal/config/config_test.go, add table-driven tests for TestValidateObservability (the file already exists; append the new test function). Import strings only if not already imported.
  </action>

  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go test ./internal/metrics/... -run TestSanitize -run TestOpaque -count=1 -timeout 30s && go test ./internal/config/... -run TestValidateObservability -run TestDefaultsObservability -count=1 -timeout 30s && make lint</automated>
  </verify>

  <done>All label + opaque-rig tests pass; all config.Validate tests pass; make lint is green with the new depguard rule active; attempting to add "import prometheus/client_golang" to any file causes make lint to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → config.Validate | User-supplied YAML bind_addr crosses into daemon network binding |
| metrics package → all modules | Registry methods accept label names from module call sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | config.Observability.Metrics.BindAddr | mitigate | validateObservability rejects 0.0.0.0 and :: with error class matching Funnel rejection; same FATAL class as OBS-03 requires |
| T-18-02 | Denial of Service | metrics.Registry label cardinality | mitigate | labelAllowlist compile-time set; SanitizeLabel maps unknown to "other"; memRegistry keyed by canonical name+labels so unknown labels collapse to the "other" key — cardinality is bounded |
| T-18-03 | Information Disclosure | metrics label values containing secrets | mitigate | Label VALUES are module-supplied strings not from user input; HELP/TYPE docstrings are hardcoded constants; modules never pass keychain handles or tokens as label values |
| T-18-04 | Tampering | prometheus/client_golang import ban | mitigate | depguard no-prometheus-client rule (list-mode: lax, no files scope) — make lint fails on any import; prevents accidental re-introduction of the library |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in this plan (all stdlib + existing go.mod) |
</threat_model>

<verification>
Overall phase-01 checks:

1. go build ./internal/metrics/... exits 0
2. go build ./internal/modules/... exits 0 (Deps.Registry field compiles)
3. go build ./internal/config/... exits 0
4. go test ./internal/metrics/... -race exits 0
5. go test ./internal/config/... exits 0
6. make lint exits 0 with no-prometheus-client rule active
7. grep -r "prometheus/client_golang" ./internal returns no matches (baseline)
</verification>

<success_criteria>
- internal/metrics package exists with Registry, Counter, Gauge, MetricFamily, Sample, NoopRegistry, NewMemRegistry exported
- modules.Deps.Registry field is type metrics.Registry
- config.Observability struct is in Config; Defaults() produces Enabled:false for both Metrics and Digest
- config.Validate rejects 0.0.0.0 and :: bind_addr with message containing "OBS-03"
- labelAllowlist is {severity, check_id, backend, result, rig}; SanitizeLabel("hostname") returns "other"
- OpaqueRigLabel returns 16-char hex, deterministic, non-reversible
- .golangci.yml has no-prometheus-client depguard rule; make lint passes
- All new tests pass with -race
</success_criteria>

<output>
Create `.planning/phases/18-observability-metrics/18-01-SUMMARY.md` when done.
</output>
