---
phase: 18-observability-metrics
plan: 04
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - cmd/abysslinkd/main.go
  - internal/modules/deps.go
  - .planning/phases/18-observability-metrics/18-VALIDATION.md
autonomous: true
requirements: [OBS-02, OBS-05, OBS-07, OBS-08]

must_haves:
  truths:
    - "cmd/abysslinkd/main.go calls startMetricsServer(ctx, cfg, reg, backendClient) and startDigestScheduler(ctx, cfg, directNotifier, runner) at daemon startup, after startAnchorWriter and before srv.Run"
    - "modules.Deps.Registry is populated with metrics.NewMemRegistry() when observability.metrics.enabled is true, and metrics.NoopRegistry{} when disabled; this is wired in the buildDeps helper in internal/cli so all module users get the correct registry"
    - "bc (the backend.Client passed to startMetricsServer) may be nil if the backend is not yet configured; startMetricsServer handles nil gracefully (Plan 02 nil-backend guard) — no nil-check is needed in main.go beyond logging the bcErr"
    - "make lint test exits 0 with all Phase 18 code present — depguard no-prometheus-client rule does not produce false positives; all new tests pass with -race"
    - "abysslink report --help shows the command is registered in the CLI root (verified via temp binary build, not PATH)"
    - "The Phase 18 VALIDATION.md file is filled in with accurate per-task test map, wave-0 list resolved, and nyquist_compliant: true set in frontmatter"
  artifacts:
    - path: "cmd/abysslinkd/main.go"
      provides: "startMetricsServer and startDigestScheduler calls at daemon startup"
      contains: "startMetricsServer"
    - path: ".planning/phases/18-observability-metrics/18-VALIDATION.md"
      provides: "completed per-task test map for all OBS-01..OBS-08"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "cmd/abysslinkd/main.go"
      to: "internal/daemon/metrics_server.go"
      via: "startMetricsServer(ctx, cfg, reg, backendClient)"
      pattern: "startMetricsServer"
    - from: "cmd/abysslinkd/main.go"
      to: "internal/daemon/digest.go"
      via: "startDigestScheduler(ctx, cfg, directNotifier, runner)"
      pattern: "startDigestScheduler"
    - from: "internal/modules/deps.go"
      to: "internal/metrics/registry.go"
      via: "Deps.Registry = metrics.NewMemRegistry() or metrics.NoopRegistry{}"
      pattern: "NoopRegistry\|NewMemRegistry"
---

<objective>
Wire the Phase 18 features into the daemon entrypoint: startMetricsServer + startDigestScheduler called at startup; NoopRegistry vs NewMemRegistry selected based on config; VALIDATION.md completed; make lint test green (OBS-02, OBS-05, OBS-07, OBS-08 per D-03).

Purpose: Plans 02 and 03 built all the components. This plan connects them so `abysslinkd` actually starts the metrics listener and digest scheduler on the next daemon restart. It also closes the testing loop (make lint test must be fully green).

Output: Updated cmd/abysslinkd/main.go, updated buildDeps helper, completed VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-observability-metrics/18-02-SUMMARY.md
@.planning/phases/18-observability-metrics/18-03-SUMMARY.md
@.planning/phases/18-observability-metrics/18-CONTEXT.md
@.planning/phases/18-observability-metrics/18-RESEARCH.md

Key source files (read before writing):
@cmd/abysslinkd/main.go
@internal/modules/deps.go
@internal/cli/cmd_doctor.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire startMetricsServer + startDigestScheduler into cmd/abysslinkd/main.go; wire Registry into buildDeps</name>
  <files>cmd/abysslinkd/main.go, internal/modules/deps.go</files>

  <read_first>
    Read cmd/abysslinkd/main.go in full — startAnchorWriter is called at line 79; the new calls go immediately after it, before srv.Run(ctx).
    Read internal/cli/cmd_doctor.go lines 264-380 to find the buildDeps helper and understand where deps.Registry should be set.
    Read internal/modules/deps.go to confirm the Registry field exists (from Plan 01).
    Read .planning/phases/18-observability-metrics/18-02-SUMMARY.md and 18-03-SUMMARY.md for the exact function signatures of startMetricsServer and startDigestScheduler as implemented.
  </read_first>

  <action>
    In cmd/abysslinkd/main.go:

    1. Import "github.com/abysslink/abysslink/internal/backend" (for the backend client) and "github.com/abysslink/abysslink/internal/metrics". Check whether a backend client is already constructed in main(); if not, construct one as: bc, bcErr := backend.NewClient(ctx, cfg, runner); if bcErr != nil { slog.Warn("abysslinkd: backend client unavailable; metrics binding degraded", "err", bcErr) }. bc may be nil if the backend is not yet configured — do NOT nil-check bc before passing it to startMetricsServer. The nil-backend guard lives inside startMetricsServer (Plan 02 implemented it as the very first line of the goroutine body: if b == nil { slog.Error("abysslinkd: metrics: no backend client; metrics listener disabled"); return }). No panic possible.

    2. Construct the registry: var reg metrics.Registry = metrics.NoopRegistry{}; if cfg.Observability.Metrics.Enabled { reg = metrics.NewMemRegistry() }.

    3. After startAnchorWriter(ctx, kc), add: startMetricsServer(ctx, cfg, reg, bc) — pass bc even if nil; startMetricsServer handles nil by logging and returning.

    4. After startMetricsServer, add: startDigestScheduler(ctx, cfg, directNotifier{m: nm}, runner).

    5. If startMetricsServer's signature takes backend.Client (not a pointer), adjust per the actual implementation from Plan 02's SUMMARY. Never add a nil panic; the guard is inside startMetricsServer.

    In internal/cli/cmd_doctor.go (or wherever buildDeps is defined — search for func buildDeps):

    Update the Deps struct initialization to set Registry: if cc.cfg.Observability.Metrics.Enabled { deps.Registry = metrics.NewMemRegistry() } else { deps.Registry = metrics.NoopRegistry{} }. Import "github.com/abysslink/abysslink/internal/metrics".

    IMPORTANT: No new fields added to modules.Deps beyond what Plan 01 already added. No fmt.Println anywhere. No panics. context.Context propagated through all new calls.

    After wiring, confirm abysslink report is registered by verifying it appears in the Cobra command tree. If the root command registration was not completed in Plan 03, add newReportCmd() to rootCmd.AddCommand here. Search cmd/abysslink/main.go or internal/cli/root.go for the pattern used by newDoctorCmd, newStatusCmd, etc. and add newReportCmd() in the same location.
  </action>

  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && go build ./... && go build -o /tmp/abysslink-test ./cmd/abysslink && /tmp/abysslink-test report --help 2>&1 | grep -q report && echo "report registered OK"</automated>
  </verify>

  <done>go build ./... exits 0; /tmp/abysslink-test report --help shows the report command description (verified via temp build, not PATH); cmd/abysslinkd/main.go calls startMetricsServer and startDigestScheduler; deps.Registry is populated appropriately in buildDeps.</done>
</task>

<task type="auto">
  <name>Task 2: make lint test green + fill VALIDATION.md</name>
  <files>.planning/phases/18-observability-metrics/18-VALIDATION.md</files>

  <read_first>
    Read .planning/phases/18-observability-metrics/18-VALIDATION.md to understand the template structure to fill.
    Run make lint test and read all error output before making fixes. Fix any issues in the files they originate from (not by suppressing lint rules unless there is a documented reason from RESEARCH.md).
  </read_first>

  <action>
    Run make lint test. Fix all errors. Common issues to anticipate and how to handle each:

    - "forbidigo: fmt.Println" — replace with slog.Info/slog.Warn/slog.Error in lib code; use printerInfo in cli code.
    - "gocyclo: complexity > 15" — split the offending function into helpers; countMetricSeries(reg) for the cardinality check is the likely candidate.
    - "unused: exported function" — check if the function is actually called; if it is a public API for test use, add a blank import or a test that uses it.
    - "errcheck: unchecked error" — add _ = or proper error handling.
    - "revive: exported ..." — ensure all exported types and functions have doc comments.
    - "staticcheck: SA..." — fix as indicated.
    - "depguard: no-prometheus-client" — this should produce zero findings (no prometheus imports in the new code). If it fires unexpectedly, remove the violating import immediately.
    - "gosec: G104 (errors unhandled)" — handle the net.DialTimeout conn.Close error in the met-disabled-listener check.

    After make lint test exits 0, fill in .planning/phases/18-observability-metrics/18-VALIDATION.md:

    Update frontmatter: set nyquist_compliant: true, wave_0_complete: true, status: complete.

    Fill "Test Infrastructure" table with actual values: Framework = "go test", Config file = "none", Quick run command = "go test ./internal/metrics/... ./internal/daemon/... ./internal/cli/... -timeout 30s", Full suite command = "make lint test".

    Fill the per-task verification map with one row per task (18-01-01 through 18-04-02), including actual automated commands, actual test file paths (with exists status ✅), and actual threat refs from the threat_model blocks above.

    Fill Wave 0 requirements as all checked (they were created in Plans 01-03).

    Set Manual-Only Verifications to "All phase behaviors have automated verification." if true (the met-disabled-listener check is automated via net.DialTimeout in tests).

    Mark Validation Sign-Off checklist as all checked.
  </action>

  <verify>
    <automated>cd /Users/mohandoz/u01/innovate/abysslink && make lint test</automated>
  </verify>

  <done>make lint test exits 0 with zero lint findings and all tests passing; VALIDATION.md has nyquist_compliant: true in frontmatter and all per-task rows filled; the phase is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon entrypoint → metrics goroutine | startMetricsServer receives a backend.Client that may be nil if Tailscale not yet configured |
| buildDeps → modules.Registry | All module callers get a Registry; NoopRegistry prevents nil-deref even if daemon not running |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16 | Denial of Service | Daemon startup with nil backend.Client | mitigate | startMetricsServer checks if b == nil as first goroutine line; logs slog.Error and returns without panicking (Plan 02 nil-backend guard; main.go passes bc unconditionally) |
| T-18-17 | Information Disclosure | Registry.Snapshot exposed to /metrics when Noop is active | accept | NoopRegistry.Snapshot() returns nil (empty families); GET /metrics returns empty body with correct Content-Type — no data leak possible |
| T-18-18 | Tampering | lint suppression of depguard rule | mitigate | No //nolint:depguard annotations are added in this phase; if lint fails on prometheus import, that is a real violation requiring fix, not suppression |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; all stdlib or existing go.mod |
</threat_model>

<verification>
Overall plan-04 checks (final phase gate):

1. make lint test exits 0 — full green
2. go build ./... exits 0 — all packages build
3. go build -o /tmp/abysslink-test ./cmd/abysslink && /tmp/abysslink-test report --help shows the report command (temp build, not PATH)
4. grep "startMetricsServer" cmd/abysslinkd/main.go confirms call is present
5. grep "startDigestScheduler" cmd/abysslinkd/main.go confirms call is present
6. grep "no-prometheus-client" .golangci.yml confirms rule exists
7. go test ./... -race -timeout 60s exits 0 — no data races
8. VALIDATION.md has nyquist_compliant: true
9. All 8 OBS requirement IDs covered across plans 01-04:
   - OBS-01: Plan 01 (Registry interface, NoopRegistry, depguard rule)
   - OBS-02: Plans 02 + 04 (metrics TCP listener + daemon wiring)
   - OBS-03: Plan 01 (config.Validate 0.0.0.0/:: rejection)
   - OBS-04: Plans 01 + 03 (label allowlist, sanitizeLabel, doctor checks)
   - OBS-05: Plans 02 + 03 + 04 (OBS-05 metrics wired to real data sources + listener lifecycle + met-disabled-listener check + daemon wiring)
   - OBS-06: Plan 03 (abysslink report command)
   - OBS-07: Plans 02 + 04 (GET /status handler + daemon wiring)
   - OBS-08: Plans 03 + 04 (digest scheduler + daemon wiring)
</verification>

<success_criteria>
- cmd/abysslinkd/main.go calls startMetricsServer and startDigestScheduler after startAnchorWriter
- deps.Registry in buildDeps uses NewMemRegistry when enabled, NoopRegistry when not
- abysslink report is registered in the CLI command tree (verified via /tmp/abysslink-test temp build)
- make lint test exits 0 (all 8 linters pass; all tests pass)
- VALIDATION.md nyquist_compliant: true with full per-task coverage
- All 8 OBS-01..OBS-08 requirements are addressed across the four plans
</success_criteria>

<output>
Create `.planning/phases/18-observability-metrics/18-04-SUMMARY.md` when done.
</output>
