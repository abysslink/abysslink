---
phase: 17-tamper-evident-audit-log-fuzzing
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - internal/audit/interface.go
  - internal/modules/deps.go
  - internal/cli/cmd_enroll.go
  - internal/cli/cmd_audit.go
  - internal/cli/cmd_backup.go
  - internal/cli/cmd_doctor.go
  - internal/cli/context.go
  - cmd/abysslinkd/main.go
autonomous: true
requirements:
  - AUD-03
  - AUD-05

must_haves:
  truths:
    - "abysslink audit verify exits 0 on a clean chain and exits 2 with stderr message 'CHAIN BROKEN at entry N' on any gap, fork, or HMAC mismatch"
    - "abysslink audit tail outputs last 20 entries (default) via Printer with --json support"
    - "abysslink audit ls outputs all entries in a human-readable table; --json emits []Entry"
    - "abysslink audit export outputs raw JSONL entries; --json emits the same JSONL-wrapped output"
    - "abysslink backup verify checks chain integrity and exits 2 on truncation or chain break"
    - "doctor check audit-anchor-age is WARN when newest anchor.json is older than 24 hours"
    - "doctor check audit-count-vs-anchor is FATAL when current entry count < anchor.EntryCount"
    - "doctor check audit-keychain is FATAL when audit.NewSigned fails (keychain unavailable)"
    - "internal/audit/interface.go declares AuditWriter interface with WriteFile(path string, content []byte, perm os.FileMode, dryRun bool) error; modules.Deps.Audit is typed audit.AuditWriter; *audit.Audit and *audit.SignedAudit both satisfy it"
    - "buildDeps in context.go and abysslinkd/main.go use audit.NewSigned(logPath, kc) instead of audit.New(logPath) when kc is non-nil; existing audit.New callers (nil-kc path) remain for backward compat"
  artifacts:
    - path: "internal/audit/interface.go"
      provides: "AuditWriter interface with WriteFile method signature matching *Audit.WriteFile exactly (no ctx — drop-in compatible)"
      exports: ["AuditWriter"]
    - path: "internal/modules/deps.go"
      provides: "Deps.Audit field type changed from *audit.Audit to audit.AuditWriter"
      contains: "AuditWriter"
    - path: "internal/cli/cmd_enroll.go"
      provides: "deps.Audit.WriteFile caller — unchanged call site; compiles against AuditWriter interface"
      contains: "deps.Audit.WriteFile"
    - path: "internal/cli/cmd_audit.go"
      provides: "audit verify/tail/ls/export Cobra subcommand tree"
      exports: ["newAuditCmd"]
    - path: "internal/cli/cmd_backup.go"
      provides: "backup verify subcommand added to existing newBackupCmd()"
      contains: "backup verify"
    - path: "internal/cli/cmd_doctor.go"
      provides: "audit-anchor-age WARN, audit-count-vs-anchor FATAL, audit-keychain FATAL findings"
      contains: "audit-anchor-age"
    - path: "internal/cli/context.go"
      provides: "buildDeps wires audit.NewSigned when keychain available; Deps.Audit assigned as audit.AuditWriter"
      contains: "NewSigned"
    - path: "cmd/abysslinkd/main.go"
      provides: "daemon uses SignedAudit for write path and calls WriteAnchor on 1h ticker"
      contains: "NewSigned"
  key_links:
    - from: "internal/audit/interface.go"
      to: "internal/modules/deps.go"
      via: "Deps.Audit typed as audit.AuditWriter; both *Audit and *SignedAudit satisfy this interface"
      pattern: "AuditWriter"
    - from: "internal/cli/cmd_audit.go"
      to: "internal/audit.Verify"
      via: "auditVerifyCmd.RunE calls audit.Verify(ctx, logPath, kc) and maps VerifyResult to exit code 2"
      pattern: "audit.Verify"
    - from: "internal/cli/cmd_audit.go"
      to: "internal/audit.ReadLog"
      via: "tail/ls/export subcommands call audit.ReadLog(logPath)"
      pattern: "audit.ReadLog"
    - from: "internal/cli/cmd_doctor.go"
      to: "internal/audit.ReadAnchor"
      via: "auditDoctorFindings calls ReadAnchor to get EntryCount and Time for anchor-age and count-vs-anchor checks"
      pattern: "audit.ReadAnchor"
    - from: "internal/cli/context.go"
      to: "internal/audit.NewSigned"
      via: "buildDeps: if kc != nil, use NewSigned; else fall back to audit.New for backward compat"
      pattern: "audit.NewSigned"
---

<objective>
Wire the Phase 17 audit library into the CLI and daemon: (Task 1) add the `abysslink audit` command surface; (Task 2a) introduce the AuditWriter interface and refactor all callers so *SignedAudit can be injected via Deps.Audit; (Task 2b) extend `backup verify`, add three doctor checks, update buildDeps and abysslinkd to use SignedAudit when a keychain is available, and add daemon's hourly anchor writer.

Purpose: Exposes the tamper-evident audit capabilities to users and operators. The interface refactor (2a) is a prerequisite for buildDeps wiring (2b) — without it, assigning *SignedAudit to Deps.Audit fails to compile. The doctor checks provide the observable security posture signals. The daemon wiring ensures every mutation path under `up --apply` produces signed, chained entries from this phase forward.

Output: interface.go (new), deps.go (refactored), cmd_enroll.go (caller unchanged but recompiled against interface), cmd_audit.go (new), cmd_backup.go (extended), cmd_doctor.go (extended with 3 checks), context.go (buildDeps updated), abysslinkd/main.go (SignedAudit wiring + hourly anchor ticker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-tamper-evident-audit-log-fuzzing/17-CONTEXT.md
@.planning/phases/17-tamper-evident-audit-log-fuzzing/17-RESEARCH.md
@.planning/phases/17-tamper-evident-audit-log-fuzzing/17-01-SUMMARY.md

@internal/audit/audit.go
@internal/audit/signed.go
@internal/audit/anchor.go
@internal/audit/verify.go
@internal/audit/reverse.go
@internal/modules/deps.go
@internal/cli/cmd_enroll.go
@internal/cli/cmd_backup.go
@internal/cli/cmd_doctor.go
@internal/cli/context.go
@cmd/abysslinkd/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add abysslink audit command surface (verify, tail, ls, export)</name>
  <files>internal/cli/cmd_audit.go</files>

  <read_first>
    - internal/cli/cmd_backup.go — Cobra subcommand tree pattern (newBackupCmd registers newBackupLsCmd + newBackupRestoreCmd); newPrinter(cmd) pattern; printerInfo + printerError usage; logPath via audit.DefaultLogPath()
    - internal/cli/cmd_doctor.go — exit-code 2 pattern via exitError type or cobra RunE returning a specific error; loadCmdContext pattern for kc access
    - internal/cli/context.go — loadCmdContext signature, cmdContext fields (cfg, runner, dryRun, apply, yes, jsonOut); buildDeps is NOT needed here (audit commands are read-only, no Deps bundle)
    - internal/audit/verify.go — Verify(ctx, logPath, kc) signature; VerifyResult fields (OK, At, Reason, TruncationDetected)
    - internal/audit/reverse.go — ReadLog(logPath) ([]Entry, error)
    - .planning/phases/17-tamper-evident-audit-log-fuzzing/17-CONTEXT.md — audit command surface decisions: tail default 20, export JSONL, verify exit 2 with "CHAIN BROKEN at entry N"
  </read_first>

  <behavior>
    - Test: newAuditCmd() returns a cobra.Command with Use="audit" and four subcommands: verify, tail, ls, export
    - Test: audit verify on a log with no break exits 0; on a log with corrupted prev_hash at entry 3 exits 2 and Printer.Error contains "CHAIN BROKEN at entry 3"
    - Test: audit tail --n=2 on a 5-entry log prints exactly the last 2 entries; --json emits valid JSON array
    - Test: audit ls on a 3-entry log prints a table with 3 rows; --json emits a JSON array with 3 elements
    - Test: audit export on a 2-entry log emits 2 JSONL lines; --json flag emits the same (JSONL format is the canonical output for export)
  </behavior>

  <action>
    Create internal/cli/cmd_audit.go with Apache-2.0 license header. Package cli. Imports: context, fmt, os, strconv plus internal/audit, internal/secrets, github.com/spf13/cobra.

    newAuditCmd() *cobra.Command: creates parent command Use="audit", Short="Inspect and verify the tamper-evident audit log". Registers four subcommands: newAuditVerifyCmd(), newAuditTailCmd(), newAuditLsCmd(), newAuditExportCmd(). Register newAuditCmd() in root.go (or wherever the backup command is registered — mirror that pattern exactly). Note: the root.go registration line must be added; do not leave it unregistered.

    newAuditVerifyCmd() *cobra.Command: Use="verify". RunE: (1) cc, err := loadCmdContext(cmd); (2) logPath via audit.DefaultLogPath(); (3) kc, _ := secrets.NewStore(ctx, cc.runner) — if kc nil, emit warning via slog.Warn and set kc to nil (Verify can proceed with nil kc for chain-only check, skipping HMAC); (4) call audit.Verify(ctx, logPath, kc); (5) if !result.OK: printerError(p, fmt.Sprintf("CHAIN BROKEN at entry %d: %s", result.At, result.Reason)); if result.TruncationDetected: printerError(p, "TRUNCATION DETECTED: current entry count is less than anchor's recorded count"); return exitErr(2); (6) if result.TruncationDetected (but chain OK): warn; (7) printerInfo(p, "Chain OK — N entries verified").

    exitErr(code int) error: return a private type that carries the exit code and implements error; the root command's Execute loop should inspect this type. Look at how the existing codebase surfaces exit code 2 (check cmd_doctor.go for hasFatal → os.Exit(2) pattern) and mirror it exactly. Do NOT call os.Exit in RunE directly — use the established pattern.

    newAuditTailCmd() *cobra.Command: Use="tail", persistent flag --n int (default 20). RunE: ReadLog; take last min(n, len(entries)) entries; if --json emit p.PrintJSON(entries); else render a table row per entry (Time, Op, Target, DryRun columns) via Printer.

    newAuditLsCmd() *cobra.Command: Use="ls". RunE: ReadLog; if --json emit p.PrintJSON(entries); else render table of all entries.

    newAuditExportCmd() *cobra.Command: Use="export". RunE: ReadLog; regardless of --json flag, emit raw JSONL (one json.Marshal(entry)+"\n" per line) to cmd.OutOrStdout(). Export's purpose is machine consumption so --json flag is redundant; if --json is set, emit the same JSONL output wrapped as a JSON array for consistency.

    All four subcommands: must use Printer for output (never fmt.Println or os.Stdout direct writes); accept context.Context propagation; handle missing log file gracefully (ReadLog returns empty slice for absent file — emit "No audit entries found" and exit 0).
  </action>

  <verify>
    <automated>go build ./internal/cli/... && go test -run 'TestAudit' ./internal/cli/... 2>&1 || go test -count=1 ./internal/cli/... 2>&1 | tail -20</automated>
  </verify>

  <done>
    - abysslink audit verify/tail/ls/export subcommands compile and register correctly in the command tree
    - verify exits 2 with "CHAIN BROKEN at entry N" on corruption
    - tail default 20 entries; ls all entries; export JSONL — all via Printer, --json supported
    - No fmt.Println in cmd_audit.go; all output via Printer; context.Context propagated
  </done>
</task>

<task type="auto">
  <name>Task 2a: Introduce AuditWriter interface and refactor Deps.Audit + all callers</name>
  <files>internal/audit/interface.go, internal/modules/deps.go, internal/cli/cmd_enroll.go</files>

  <read_first>
    - internal/modules/deps.go — current Deps struct; Audit field is typed *audit.Audit (line 41); all imports
    - internal/cli/cmd_enroll.go — the one caller of deps.Audit.WriteFile (line 592); confirm the exact call signature used: WriteFile(path string, content []byte, perm os.FileMode, dryRun bool) error
    - internal/audit/write.go — *Audit.WriteFile existing signature: WriteFile(path string, content []byte, perm os.FileMode, dryRun bool) error — this is the interface method that must be matched exactly
    - internal/audit/signed.go (from Plan 01) — *SignedAudit; confirm WriteFile method does not yet exist and must be added here
  </read_first>

  <action>
    PART A — Create internal/audit/interface.go with Apache-2.0 license header. Package audit.

    Declare the AuditWriter interface:

      AuditWriter is the interface satisfied by both *Audit (unsigned, backward-compat) and *SignedAudit (HMAC-chained). The interface uses the existing WriteFile signature with no ctx parameter, which is acceptable here because *Audit.WriteFile also has no ctx, making this a drop-in-compatible refactor. *SignedAudit.WriteFile uses context.Background() internally (per RESEARCH.md Open Questions Q1 RESOLVED) — this is explicitly justified because WriteFile is a convenience wrapper over Append; the mutation ordering (append-to-log BEFORE the physical write) is enforced inside the implementation, not the interface.

      Method: WriteFile(path string, content []byte, perm os.FileMode, dryRun bool) error

    PART B — Add WriteFile to *SignedAudit in internal/audit/signed.go. This method must be added to signed.go (not interface.go). Import os if not already imported.

    SignedAudit.WriteFile(path string, content []byte, perm os.FileMode, dryRun bool) error: Write order MUST be append-to-log BEFORE the physical write, matching the existing *Audit.WriteFile pattern and the audit-then-write project convention. Implementation: (1) compute diffHash := sha256.Sum256(content); (2) call a.Append(context.Background(), SignInput{Title: "write", DiffHash: diffHash}, path, dryRun) — this records intent in the log first; (3) if dryRun is true, return after Append (no physical write for dry-run); (4) if file exists at path, call Backup(path) — return error if backup fails; (5) write atomically: os.WriteFile to path+".abysslink.tmp" then os.Rename; (6) return nil or error. Use context.Background() internally — WriteFile is not a hot path and the constructor pattern already uses context.Background() for keychain calls.

    PART C — Update internal/modules/deps.go: change the Audit field type from `*audit.Audit` to `audit.AuditWriter`. No other changes to the file.

    PART D — Verify internal/cli/cmd_enroll.go compiles without changes. The call `deps.Audit.WriteFile(path, []byte(body), 0o600, false)` must compile against the AuditWriter interface. If cmd_enroll.go references any type assertion or type-specific method beyond WriteFile, update only those lines. Do not alter any business logic.

    Run go build ./... after all edits to confirm the interface refactor compiles cleanly before proceeding to Task 2b.
  </action>

  <verify>
    <automated>go build ./... && grep -c "AuditWriter" internal/audit/interface.go internal/modules/deps.go</automated>
  </verify>

  <done>
    - internal/audit/interface.go exists with AuditWriter interface declaring WriteFile(path, content, perm, dryRun) — no ctx parameter
    - *SignedAudit.WriteFile appends to log (records intent) BEFORE physical write; uses context.Background() internally
    - modules.Deps.Audit is typed audit.AuditWriter — both *Audit and *SignedAudit satisfy it
    - cmd_enroll.go compiles unchanged; deps.Audit.WriteFile call works against the interface
    - go build ./... exits 0 — no import cycles, no compile errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Extend backup verify, add audit doctor checks, wire SignedAudit into daemon and buildDeps</name>
  <files>internal/cli/cmd_backup.go, internal/cli/cmd_doctor.go, internal/cli/context.go, cmd/abysslinkd/main.go</files>

  <read_first>
    - internal/cli/cmd_backup.go — existing newBackupCmd() tree; newBackupLsCmd/newBackupRestoreCmd pattern; add newBackupVerifyCmd() and register it in newBackupCmd()
    - internal/cli/cmd_doctor.go — supplyChainFindings function pattern (returns []modules.Finding); how findings are appended in newDoctorCmd().RunE (line ~196-200 area); modules.Finding struct fields (Module, Check, Severity, Message); modules.SeverityWarning, modules.SeverityFatal constants
    - internal/cli/context.go — buildDeps function (lines 141-175 area); audit.New(logPath) call; kc variable (may be nil); import of internal/audit; Deps.Audit is now typed audit.AuditWriter (after Task 2a)
    - cmd/abysslinkd/main.go — existing kc construction (secrets.NewStore); no current audit wiring; daemon.Run(ctx, ...) call structure; how to add a goroutine for the hourly ticker
    - internal/audit/anchor.go — WriteAnchor(ctx, logPath, kc) signature (written in Plan 01)
    - internal/audit/verify.go — Verify(ctx, logPath, kc) signature
    - internal/audit/reverse.go — ReadLog(logPath) for entry count
    - .planning/phases/17-tamper-evident-audit-log-fuzzing/17-CONTEXT.md — doctor check names: audit-anchor-age WARN (>24h), audit-count-vs-anchor FATAL (count < anchor), audit-keychain FATAL (NewSigned unavailable)
  </read_first>

  <behavior>
    - Test: newBackupCmd() has a "verify" subcommand; backup verify on a clean log exits 0; on a corrupted log exits 2 with "CHAIN BROKEN at entry N"
    - Test: auditDoctorFindings with a fresh anchor (< 24h old) produces no audit-anchor-age finding; with anchor older than 24h produces a WARN finding with Check="audit-anchor-age"
    - Test: auditDoctorFindings with current count < anchor.EntryCount produces a FATAL finding with Check="audit-count-vs-anchor"
    - Test: auditDoctorFindings when audit.NewSigned returns error (kc=nil) produces a FATAL finding with Check="audit-keychain"
    - Test: buildDeps with a non-nil kc returns Deps.Audit as a *SignedAudit (satisfying audit.AuditWriter); buildDeps with nil kc returns Deps.Audit as *Audit (backward compat, also satisfying audit.AuditWriter)
  </behavior>

  <action>
    PART A — backup verify (cmd_backup.go):

    Add newBackupVerifyCmd() *cobra.Command to cmd_backup.go. Use="verify", Short="Verify the integrity of the audit log chain". RunE: mirrors auditVerifyCmd.RunE exactly — calls audit.Verify(ctx, logPath, kc) and maps result to exit 0 or exit 2 with "CHAIN BROKEN at entry N". Register in newBackupCmd() alongside newBackupLsCmd() and newBackupRestoreCmd().

    PART B — doctor checks (cmd_doctor.go):

    Add a new private function auditDoctorFindings(ctx context.Context, logPath string, kc secrets.KeychainStore) []modules.Finding. This function runs three checks:

    Check 1 — audit-keychain (FATAL): attempt audit.NewSigned(logPath, kc) where kc may be nil or unavailable; if err != nil (or kc == nil), append modules.Finding{Module: "audit", Check: "audit-keychain", Severity: modules.SeverityFatal, Message: "HMAC signing key unavailable — keychain not accessible; signed entries cannot be written (AUD-02)"}.

    Check 2 — audit-anchor-age (WARN): call audit.ReadAnchor(logPath); if anchor != nil, parse anchor.Time (RFC3339); if time.Since(anchorTime) > 24*time.Hour, append Finding{Module: "audit", Check: "audit-anchor-age", Severity: modules.SeverityWarning, Message: fmt.Sprintf("Audit anchor is %s old (threshold 24h) — daemon may not be running or anchor writes are failing", humanDuration)}. If anchor == nil (no anchor yet), emit the WARN too (message: "No audit anchor found — run `abysslink up --apply` or start the daemon to create one").

    Check 3 — audit-count-vs-anchor (FATAL): call audit.ReadAnchor(logPath) and audit.ReadLog(logPath) (for count); if anchor != nil and int64(len(entries)) < anchor.EntryCount, append Finding{Module: "audit", Check: "audit-count-vs-anchor", Severity: modules.SeverityFatal, Message: fmt.Sprintf("Audit log has %d entries but anchor records %d — truncation detected", len(entries), anchor.EntryCount)}.

    Wire auditDoctorFindings into newDoctorCmd().RunE: after the existing `findings = append(findings, supplyChainFindings(...))` line, add `findings = append(findings, auditDoctorFindings(ctx, logPath, cc.keychain_or_kc)...)`. Determine how to pass kc by looking at how supplyChainFindings gets cc.runner — mirror the same pattern for kc. logPath via audit.DefaultLogPath() called inside auditDoctorFindings (error → empty findings with slog.Warn).

    PART C — buildDeps wiring (context.go):

    In buildDeps, after the existing `kc, err := secrets.NewStore(...)` block and after `logPath, err := audit.DefaultLogPath()`, replace `Audit: audit.New(logPath)` with: if kc != nil, attempt sa, err := audit.NewSigned(logPath, kc); if err == nil assign sa to Deps.Audit (it satisfies audit.AuditWriter after Task 2a). If kc is nil or NewSigned fails, fall back to audit.New(logPath) and emit slog.Warn. The Deps.Audit field is now typed audit.AuditWriter so both assignments compile without type assertion.

    PART D — abysslinkd daemon wiring (cmd/abysslinkd/main.go):

    In main.go, after kc is constructed: call logPath, err := audit.DefaultLogPath(); handle error via slog.Error + return (do not panic at startup — return error instead per CLAUDE.md no-panics rule). Then if kc != nil and err == nil: attempt sa, err2 := audit.NewSigned(logPath, kc); if err2 == nil, launch a goroutine for the hourly WriteAnchor ticker:

      go func() {
        ticker := time.NewTicker(1 * time.Hour)
        defer ticker.Stop()
        for {
          select {
          case <-ticker.C:
            writeCtx, cancel := context.WithTimeout(ctx, 30*time.Second)
            if err := audit.WriteAnchor(writeCtx, logPath, kc); err != nil {
              slog.Warn("abysslinkd: anchor write failed", "err", err)
            }
            cancel()
          case <-ctx.Done():
            return
          }
        }
      }()

    The goroutine uses a 30-second timeout context per WriteAnchor call (per T-17-11 threat mitigation). The goroutine respects ctx.Done() for clean shutdown. If NewSigned fails, log slog.Warn and skip goroutine launch — daemon continues operating without signed auditing (degraded mode, doctor check will flag it).
  </action>

  <verify>
    <automated>go build ./... && go test -race -count=1 -run 'TestBackupVerify|TestAuditDoctor|TestBuildDeps' ./internal/cli/... && go test -race -count=1 ./internal/cli/... 2>&1 | tail -20</automated>
  </verify>

  <done>
    - backup verify subcommand registered; exits 2 with "CHAIN BROKEN at entry N" on corrupt chain
    - audit-anchor-age WARN and audit-count-vs-anchor FATAL doctor checks fire correctly
    - audit-keychain FATAL doctor check fires when NewSigned fails or kc is nil
    - buildDeps uses SignedAudit (assigned as audit.AuditWriter) when kc is available; falls back to audit.New when kc is nil
    - abysslinkd launches hourly WriteAnchor goroutine with 30s timeout; goroutine respects context cancellation
    - go build ./... exits 0 — no import cycles, no compile errors
    - make lint test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user input → audit verify exit code | Exit code 2 must only be set on genuine chain break, not on file-not-found or parse errors |
| daemon → anchor write cadence | Hourly anchor goroutine must not block the daemon's shutdown path; context cancellation must propagate |
| doctor findings → secrets (kc) | Doctor checks call NewSigned to probe keychain availability; must not log the key value |
| AuditWriter interface → callers | Interface has no ctx on WriteFile; *SignedAudit.WriteFile uses context.Background() internally — justified because WriteFile is a convenience wrapper, not a hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering | `abysslink audit verify` exit code | mitigate | Only Verify()'s VerifyResult.OK==false or TruncationDetected sets exit 2; parse errors return exit 1 (generic error), not 2 |
| T-17-10 | Information Disclosure | audit-keychain doctor check error message | mitigate | Finding.Message describes the failure class (keychain unavailable), not the key material; NewSigned error is wrapped, not the raw key |
| T-17-11 | Denial of Service | Hourly anchor goroutine blocking on stuck keychain | mitigate | WriteAnchor call uses ctx with 30s timeout (context.WithTimeout derived from parent ctx); slog.Warn on timeout and continue |
| T-17-12 | Repudiation | backup verify bypasses chain check | mitigate | backup verify calls audit.Verify (same function as audit verify) — identical verification logic, not a separate weaker path |
| T-17-13 | Spoofing | audit-count-vs-anchor FATAL with zero-entry new log | accept | New installations have no anchor yet; check only fires when anchor exists AND count < anchor.EntryCount; zero-anchor state emits WARN not FATAL |
| T-17-14 | Tampering | *SignedAudit.WriteFile write order | mitigate | Append-to-log (records intent) happens BEFORE physical write — if the process crashes between log and write, the operator sees intent without effect; consistent with *Audit.WriteFile ordering |
| T-17-SC | Tampering | No new package installs in this plan | accept | No npm/pip/cargo/go-get new packages; all stdlib + existing deps |
</threat_model>

<verification>
After all tasks complete:
1. abysslink audit verify --help — lists verify, tail, ls, export subcommands
2. abysslink backup verify --help — present
3. abysslink doctor (dry-run) — includes audit-anchor-age, audit-count-vs-anchor, audit-keychain in output when conditions met
4. go build ./... — exits 0
5. go test -race -count=1 ./... — all tests pass
6. golangci-lint run ./... — exits 0
7. grep -c "AuditWriter" internal/audit/interface.go internal/modules/deps.go — both return >= 1
8. grep "context.Background" internal/audit/signed.go — present in WriteFile implementation (justified no-ctx interface compatibility)
</verification>

<success_criteria>
- AuditWriter interface declared in internal/audit/interface.go; WriteFile signature has no ctx parameter for drop-in *Audit compatibility; *SignedAudit.WriteFile uses context.Background() internally with append-before-write order (AUD-02, AUD-05)
- Deps.Audit typed as audit.AuditWriter; cmd_enroll.go caller compiles unchanged (AUD-03)
- audit verify exits 2 with exact string "CHAIN BROKEN at entry N" on chain break (AUD-03)
- backup verify registered and functional (AUD-03)
- audit-anchor-age WARN, audit-count-vs-anchor FATAL, audit-keychain FATAL doctor checks wired (AUD-05)
- buildDeps uses SignedAudit when kc != nil; abysslinkd daemon writes anchors every 1h with 30s timeout context (AUD-05, AUD-02)
- make lint test exits 0
</success_criteria>

<output>
Create `.planning/phases/17-tamper-evident-audit-log-fuzzing/17-02-SUMMARY.md` when done
</output>
