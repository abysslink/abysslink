---
phase: 26
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - internal/cli/journey.go
  - internal/cli/journey_test.go
autonomous: true
requirements:
  - PHASE-26-B2
  - PHASE-26-GATED-JOURNEY
  - PHASE-26-ACL-STAGE
must_haves:
  truths:
    - "Stage 2 does NOT call ensureTailscale or runSecurityFixes (B2 fix — no duplicate work)"
    - "Stage 2 uses tui.Pause with the captured autoYes — never hardcodes true"
    - "All stages 1–7 gate on autoYes/non-TTY before any huh call (T-10-16 invariant)"
    - "Stages 3, 4, 5, 6 each offer to run their underlying command via huh.NewConfirm"
    - "New ACL stage (index 7) prints guidance and offers to run abysslink acl push --apply"
    - "Done stage is index 8 (shifted from 7)"
    - "journeyLabels() returns 8 labels including 'ACL'"
    - "TestJourneyStageCount asserts 8 stages (updated from 7)"
    - "TestJourneyStageLabels asserts 8 labels including 'ACL'"
    - "TestJourneyStage2_NoDuplicateCalls: Stage 2 run makes zero subprocess calls"
    - "TestRunJourney_NonTTY: journey completes without hanging under non-TTY stdin"
    - "Headless autoYes=true path never hangs — T-10-16 regression unchanged"
    - "make lint test green"
  artifacts:
    - path: "internal/cli/journey.go"
      provides: "8-stage gated journey with ACL stage, B2 fix, doc comment update"
      contains: "ACL"
    - path: "internal/cli/journey_test.go"
      provides: "Updated stage count/label tests + B2 no-duplicate test + non-TTY test"
      contains: "TestJourneyStageCount"
  key_links:
    - from: "journey.go Stage 2 run closure"
      to: "tui.Pause"
      via: "autoYes captured from journeyStages params"
      pattern: "tui\\.Pause"
    - from: "journey.go Stages 3-7 run closures"
      to: "huh.NewConfirm"
      via: "autoYes/interactive() gate"
      pattern: "interactive\\(autoYes, jsonOut\\)"
    - from: "journey.go Stage 7 ACL run closure"
      to: "runner.RunInteractive(ctx, \"abysslink\", \"acl\", \"push\", \"--apply\")"
      via: "user confirms huh prompt"
      pattern: "RunInteractive"
---

<objective>
Fix B2 (Stage 2 hardcoded autoYes=true duplicating RunE work) and deliver the full gated interactive journey: per-stage confirm gates, offer-to-run in stages 3–6, new ACL stage (index 7), Done shifts to index 8, and reconcile the "NON-BLOCKING" doc comment.

**B2** — Stage 2 calls `ensureTailscale` and `runSecurityFixes` with literal `true`, duplicating the calls already made in `cmd_init.go:101–104` before `runJourney` is called. Fix: convert Stage 2 to a summary/gate stage that emits the security note, prints "Prerequisites verified.", and calls `tui.Pause` with captured `autoYes`.

**Gated journey** — Each stage ends with a gate:
- Stages 1, 2: `tui.Pause(ctx, msg, autoYes)` (no yes/no choice)
- Stages 3–7: `huh.NewConfirm` offering to run the underlying command, gated by `autoYes || !interactive(autoYes, jsonOut)` so headless paths never block

**ACL stage** — New Stage 7 (before Done, which shifts to 8): prints `abysslink acl push --apply` guidance + Tailscale admin console URL fallback; in interactive mode offers to run the command via `runner.RunInteractive`

**Doc comment** — Replace "journey is NON-BLOCKING" package doc with the accurate formulation: "interactive stages pause for user confirmation; headless path (autoYes=true or non-TTY) remains non-blocking per T-10-16"

Purpose: The stage gates turn a scrolling printout into a real interactive wizard where the user confirms each step and can optionally execute it inline. Headless behavior is unchanged.

Output: Updated journey.go with 8 stages (B2 fix + gates + ACL), updated journey_test.go.
</objective>

<execution_context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/26-init-journey-gating-first-run-fixes/26-PATTERNS.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/26-init-journey-gating-first-run-fixes/26-RESEARCH.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/PROJECT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/journey.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/journey_test.go
@/Users/mohandoz/u01/innovate/abysslink/internal/tui/forms.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/term.go
@/Users/mohandoz/u01/innovate/abysslink/internal/shell/mock.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix B2 + add per-stage gates + ACL stage in journey.go</name>
  <files>internal/cli/journey.go</files>
  <read_first>
    - internal/cli/journey.go — full source: current package doc comment (lines 16–31), imports block (lines 35–47), journeyLabels() (lines 67–77), journeyStages() structure (lines 87–204), runJourney (lines 216–248); read the complete file before editing
    - internal/tui/forms.go — tui.Pause signature: `func Pause(ctx context.Context, msg string, yes bool) error`; its internal yes||!stdinIsTTY() guard
    - internal/cli/term.go — interactive() definition: `func interactive(yes, jsonOut bool) bool`
    - 26-PATTERNS.md §"internal/cli/journey.go" — B2 fix pattern, per-stage gate pattern, ACL stage pattern, journeyLabels() update, Done index update, doc comment text
    - 26-RESEARCH.md §Pattern 4 and §Pattern 5 — gate guard order and ACL stage wiring
  </read_first>
  <behavior>
    - Stage 2 run closure: calls emitSecurityNote for "sudo-notice", prints "Prerequisites verified.", calls tui.Pause(ctx, "Stage 2 complete — prerequisites ready", autoYes) — NO calls to ensureTailscale or runSecurityFixes
    - Stage 1 run closure: after ensureTailscaleAccount returns, adds tui.Pause(ctx, "Stage 1 complete — account ready", autoYes)
    - Stage 3 run closure: emits security notes, prints guidance, then `if autoYes || !interactive(autoYes, jsonOut) { return nil }` before huh.NewConfirm offering "Run `abysslink up --apply` now?"; if confirmed calls runner.RunInteractive(ctx, "abysslink", "up", "--apply") and on error prints warning (does not propagate error)
    - Stage 4 run closure: same gate pattern; offers "Run `abysslink lock init --apply` now?"; if confirmed calls runner.RunInteractive(ctx, "abysslink", "lock", "init", "--apply")
    - Stage 5 run closure: same gate pattern; offers "Run `abysslink enroll phone` now?"; if confirmed calls runner.RunInteractive(ctx, "abysslink", "enroll", "phone")
    - Stage 6 run closure: same gate pattern; offers "Run `abysslink doctor` now?"; if confirmed calls runner.RunInteractive(ctx, "abysslink", "doctor")
    - Stage 7 (ACL, NEW): prints ACL guidance + URL "https://login.tailscale.com/admin/acls/file"; gate pattern; if confirmed calls runner.RunInteractive(ctx, "abysslink", "acl", "push", "--apply")
    - Stage 8 (Done, was 7): unchanged security notes + success box; index: 8
    - journeyLabels() returns ["Account","Prerequisites","Converge","Lock","Enroll","Verify","ACL","Done"] — 8 labels
    - Package doc comment updated: "NON-BLOCKING" removed; new text: "interactive stages pause for user confirmation; headless path (autoYes=true or non-TTY) remains non-blocking per T-10-16"
    - autoYes=true: all tui.Pause calls return nil immediately (already guards); all interactive() guards fire → stages 3–7 return nil without huh
    - non-TTY (go test): interactive() returns false → stages 3–7 return nil without huh
  </behavior>
  <action>
    Edit internal/cli/journey.go:

    1. **Package doc comment (lines 16–31):** Replace the sentence "The journey itself is NON-BLOCKING: each stage prints guidance and returns — the §6-sanctioned external-action waits live inside the underlying commands..." with: "Under --yes/--json/non-TTY the journey runs headless (no JourneyHeader) so automated invocations never hang (T-10-16). With a TTY, each stage pauses for user confirmation; stages 3–7 offer to invoke the underlying command directly. The headless path (autoYes=true or non-TTY) remains non-blocking in all cases." Keep all other doc text.

    2. **Imports:** Add `"github.com/charmbracelet/huh"` to the import block (existing: context, encoding/json, errors, os, path/filepath, backend, config, platformauto, shell, tui).

    3. **journeyLabels():** Add `"ACL"` between `"Verify"` and `"Done"` — returns 8 labels.

    4. **journeyStages() Stage 1** (index 1, label "Account"): Change the run closure body to: call emitSecurityNote for "sso-hardening" and "dry-run-default", then call ensureTailscaleAccount using the two-step error-then-pause pattern. Specifically: first `if err := ensureTailscaleAccount(p, autoYes); err != nil { return err }`, then `return tui.Pause(ctx, "Stage 1 complete — account ready", autoYes)`. This ensures ensureTailscaleAccount errors surface immediately and are never discarded by the tui.Pause return — do NOT collapse into a single `return tui.Pause(...)` which would discard the ensureTailscaleAccount error.

    5. **journeyStages() Stage 2** (index 2, label "Prerequisites"): Replace the entire run closure body with:
       - `emitSecurityNote(p, jsonOut, "sudo-notice")`
       - `printerInfo(p, styleSuccess.Render("Prerequisites verified."))`
       - `return tui.Pause(ctx, "Stage 2 complete — prerequisites ready", autoYes)`
       - Remove the local `runner := &shell.ExecRunner{}`, `plat, err := platformauto.New(runner)`, `toolStatus := runToolCheck(...)`, `ensureTailscale(...)`, and `runSecurityFixes(...)` calls entirely.

    6. **journeyStages() Stage 3** (index 3, label "Converge"): After existing emitSecurityNote calls and printerInfo guidance, add the gate+offer block: guard `if autoYes || !interactive(autoYes, jsonOut) { return nil }`, then huh.NewConfirm with Title "Run `abysslink up --apply` now?", Description "Converges your system to match abysslink.yaml.", Affirmative "Yes, run it", Negative "No, I'll run it later". If confirmed, call `runner.RunInteractive(ctx, "abysslink", "up", "--apply")` and on error print warning with iconWarnStr(). Always return nil from the stage (non-fatal run failure). Change function signature from `func(_ context.Context, ...)` to `func(ctx context.Context, ...)` to pass ctx.

    7. **journeyStages() Stage 4** (index 4, label "Lock"): After existing lock guidance (the if-locker check + printerInfo lines), add gate+offer block: guard `if autoYes || !interactive(autoYes, jsonOut) { return nil }`, then huh.NewConfirm with Title "Run `abysslink lock init --apply` now?", Description "Enables Tailnet Lock — disablement secrets printed ONCE.", Affirmative "Yes, run it", Negative "No, I'll run it later". If confirmed, `runner.RunInteractive(ctx, "abysslink", "lock", "init", "--apply")`.

    8. **journeyStages() Stage 5** (index 5, label "Enroll"): After existing printerInfo guidance, add gate+offer block: guard `if autoYes || !interactive(autoYes, jsonOut) { return nil }`, then huh.NewConfirm with Title "Run `abysslink enroll phone` now?", Description "Shows a QR code for the Tailscale app.", Affirmative "Yes, run it", Negative "No, I'll run it later". If confirmed, `runner.RunInteractive(ctx, "abysslink", "enroll", "phone")`. NOTE: If the existing Stage 5 closure signature uses `func(_ context.Context, p Printer)`, rename the blank identifier to `ctx` — i.e. `func(ctx context.Context, p Printer)` — so `ctx` is available for the RunInteractive call. Failing to do this causes a compile error.

    9. **journeyStages() Stage 6** (index 6, label "Verify"): After existing printerInfo guidance, add gate+offer block: guard `if autoYes || !interactive(autoYes, jsonOut) { return nil }`, then huh.NewConfirm with Title "Run `abysslink doctor` now?", Description "Checks all modules for misconfigurations.", Affirmative "Yes, run it", Negative "No, I'll run it later". If confirmed, `runner.RunInteractive(ctx, "abysslink", "doctor")`. NOTE: If the existing Stage 6 closure signature uses `func(_ context.Context, p Printer)`, rename the blank identifier to `ctx` — i.e. `func(ctx context.Context, p Printer)` — so `ctx` is available for the RunInteractive call. Failing to do this causes a compile error.

    10. **Insert Stage 7 (ACL, NEW)** — append before the existing Done stage (currently index 7). The new ACL stage has index: 7, label: "ACL". Run closure: prints `styleBold.Render("Configure the tailnet ACL:")`, `styleCode.Render("abysslink acl push --apply")`, `styleMuted.Render("Requires tailnet.admin OAuth config — if absent, manage at:")`, `styleCode.Render("https://login.tailscale.com/admin/acls/file")`. Then gate+offer block: guard `if autoYes || !interactive(autoYes, jsonOut) { return nil }`, huh.NewConfirm with Title "Run `abysslink acl push --apply` now?", Description "Pushes the abysslink ACL to your tailnet. Requires OAuth config.", Affirmative "Yes, push ACL", Negative "No, I'll manage it manually". If confirmed, `runner.RunInteractive(ctx, "abysslink", "acl", "push", "--apply")` with warning on error.

    11. **Stage 8 (Done)**: Change existing Done stage index from 7 to 8. All other content unchanged.

    12. **Remove unused import**: After removing the ensureTailscale/runSecurityFixes duplicate from Stage 2, `platformauto` and potentially `shell` may become unused. Check: `shell` is still used (runner shell.Runner is a param of journeyStages); `platformauto` was only used inside Stage 2's local runner creation — remove the `platformauto` import. Verify `go build ./internal/cli/...` exits 0.

    Critical invariants:
    - NEVER pass literal `true` or `false` as the autoYes argument in any stage closure — always use the captured `autoYes` variable
    - The `if autoYes || !interactive(autoYes, jsonOut)` guard MUST appear before every huh.NewConfirm call
    - runner.RunInteractive failures must print a warning, not propagate as errors (non-fatal)
    - No `sh -c` usage (CLAUDE.md hard rule)
    - No reimplementation of command logic inline
  </action>
  <verify>
    <automated>go build ./internal/cli/... && go test ./internal/cli/... -run 'TestJourneyStageCount|TestJourneyStageLabels|TestRunJourney_AutoYes' -v</automated>
  </verify>
  <done>
    - `go build ./internal/cli/...` exits 0 (no unused imports, no type errors)
    - `TestJourneyStageCount` passes asserting 8 stages
    - `TestJourneyStageLabels` passes with 8 expected labels including "ACL"
    - `TestRunJourney_AutoYes` still passes (regression)
    - Stage 2 run closure has no call to ensureTailscale or runSecurityFixes
    - journeyLabels() returns exactly 8 labels
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update journey_test.go with stage count, labels, B2 no-dup, and non-TTY tests</name>
  <files>internal/cli/journey_test.go</files>
  <read_first>
    - internal/cli/journey_test.go — full current source: TestJourneyStageCount (asserts 7), TestJourneyStageLabels (7 labels without "ACL"), TestRunJourney_AutoYes (existing pattern to model non-TTY test from)
    - internal/shell/mock.go — NewMockRunner, Done() method (reports all scripted calls consumed), RecordedCalls()
    - 26-PATTERNS.md §"internal/cli/journey_test.go" — exact update patterns for count/labels, TestJourneyStage2_NoDuplicateCalls, TestRunJourney_NonTTY
    - internal/cli/journey.go — journeyStages() signature: `func journeyStages(jsonOut bool, cfg *config.Config, runner shell.Runner, autoYes bool) []journeyStage`
  </read_first>
  <behavior>
    - TestJourneyStageCount: asserts Len(stages, 8, "journey must have exactly 8 stages")
    - TestJourneyStageLabels: expects 8 labels; assert.Equal checks for "ACL" at index 6 and "Done" at index 7
    - TestJourneyStage2_NoDuplicateCalls: uses shell.NewMockRunner() with zero scripted calls; calls journeyStages(false, config.Defaults(), runner, true) and runs stage index 1 (stages[1].run); asserts runner.Done() (no calls consumed); additionally asserts stage output contains "Prerequisites verified." to distinguish the new summary Stage 2 from the old re-run Stage 2 (old code would emit ensureTailscale output, not this message)
    - TestRunJourney_NonTTY: uses autoYes=false but runs under go test (non-TTY stdin); all stage run stubs complete without blocking; asserts stagesRan == 2
  </behavior>
  <action>
    Edit internal/cli/journey_test.go:

    1. **TestJourneyStageCount**: Change `assert.Len(t, stages, 7, ...)` to `assert.Len(t, stages, 8, "journey must have exactly 8 stages")`.

    2. **TestJourneyStageLabels**: Change `require.Len(t, stages, 7)` to `require.Len(t, stages, 8)`. Add `"ACL"` as the 7th element in `expectedLabels` (index 6) and keep `"Done"` as the 8th (index 7). Update the loop bound to 8 (it already uses `for i, expected := range expectedLabels` so this is automatic).

    3. **Add TestJourneyStage2_NoDuplicateCalls** (new test): Creates a `shell.NewMockRunner()` with zero scripted calls. Calls `journeyStages(false, config.Defaults(), runner, true)` — autoYes=true ensures no huh prompts. Requires `len(stages) >= 2`. Runs `stage2 := stages[1]; err := stage2.run(ctx, p)` where ctx is context.Background() and p is a `&testPrinter{out: &buf}`. Asserts `require.NoError(t, err)`. Asserts `runner.Done()` — meaning MockRunner's internal idx == 0 because no calls were scripted AND zero calls were consumed (Done() returns m.idx == len(m.calls) which is 0==0 → true; this proves no subprocess calls were made). ALSO assert `assert.Contains(t, buf.String(), "Prerequisites verified.")` — this is a positive behavioral check that distinguishes the fixed Stage 2 (which emits this message) from the old Stage 2 (which emits ensureTailscale output). The runner.Done() check alone is vacuously true because the old Stage 2 creates its own &shell.ExecRunner{} and never uses the passed runner — the output assertion catches that regression. Add a comment: "runner.Done() proves no calls went through the injected runner; output assertion proves Stage 2 is the new summary path, not the old re-run path."

    4. **Add TestRunJourney_NonTTY** (new test): Builds a 2-stage stub journey with counters (stagesRan++). Calls `runJourney(ctx, p, stages, 0, stateFile, false)` with autoYes=false. Asserts no error and stagesRan == 2. Under go test, stdin is not a TTY so tui.Pause and interactive() will be no-ops. Add comment: "Under go test, stdin is never a TTY — stdinIsTTY() returns false, so all stage gates are no-ops regardless of autoYes=false."

    Ensure all required imports are present: `context`, `bytes`, `path/filepath`, `testing`, `github.com/abysslink/abysslink/internal/config`, `github.com/abysslink/abysslink/internal/shell`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`. Do not add a second license header — the existing one at lines 1–15 is correct.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run 'TestJourneyStageCount|TestJourneyStageLabels|TestJourneyStage2_NoDuplicateCalls|TestRunJourney_NonTTY|TestRunJourney_AutoYes|TestRunJourney_ResumesFromStage' -v</automated>
  </verify>
  <done>
    - `TestJourneyStageCount` passes asserting 8 stages
    - `TestJourneyStageLabels` passes with 8 labels including "ACL" at index 6
    - `TestJourneyStage2_NoDuplicateCalls` passes (runner.Done() == true AND output contains "Prerequisites verified." — both assertions required)
    - `TestRunJourney_NonTTY` passes (2 stages ran, no hang)
    - Existing resume and state tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three first-run bugs fixed (B1 browser probe, B2 Stage 2 autoYes, B3 pmset parser) and journey upgraded to 8-stage gated interactive wizard with per-stage confirm gates and new ACL stage. `make lint test` is green.
  </what-built>
  <how-to-verify>
    Run `make lint test` and confirm it exits 0.

    Then smoke-test the non-interactive path:
    ```
    abysslink init --yes 2>&1 | head -40
    ```
    Expected: wizard runs through all 8 stages without hanging or errors; output includes stage names Account → Prerequisites → Converge → Lock → Enroll → Verify → ACL → Done.

    Optional interactive verification (requires TTY):
    ```
    abysslink init
    ```
    Expected: each stage pauses with a "Stage N of 8" header and a confirm/continue prompt; stages 3–7 offer to run the underlying command.

    Verify the pmset fix:
    ```
    go test ./internal/cli/... -run TestCheckACSleepDisabled -v
    ```
    Expected: 5 sub-tests all PASS.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage closures → huh prompt | huh prompts are gated by interactive()/autoYes — never fire in headless/non-TTY |
| Stage closures → runner.RunInteractive | RunInteractive is gated by the same interactive() check — never called in headless/non-TTY; user confirms first |
| journey-state.json | Contains only {"last_stage": N} — no secrets, no executable content (T-10-17 invariant preserved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | Stage 2 B2 fix — sudo mutation without confirm | mitigate | Stage 2 no longer calls ensureTailscale/runSecurityFixes at all; duplicate sudo calls eliminated; original calls in RunE still use real autoYes |
| T-26-05 | Elevation of Privilege | journey Stage 3–7 RunInteractive | mitigate | Gate: `if autoYes || !interactive(autoYes, jsonOut) { return nil }` must fire before any RunInteractive call; user sees huh confirm before any command runs; --apply flag required by each underlying command (dry-run default per CLAUDE.md) |
| T-26-06 | Spoofing | ACL stage printing fallback URL | accept | URL is a hardcoded constant "https://login.tailscale.com/admin/acls/file" — not user-supplied; no injection risk |
| T-26-07 | Information Disclosure | journey-state.json stage index | accept | File contains {"last_stage": N} only — no PII, no tokens; T-10-17 invariant documented |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Phase 26 installs zero external packages |
</threat_model>

<verification>
After all tasks complete:

```bash
make lint test
go test ./internal/cli/... -run 'TestJourneyStageCount|TestJourneyStageLabels|TestJourneyStage2_NoDuplicateCalls|TestRunJourney|TestCheckACSleepDisabled|TestEnsureTailscaleAccount' -v
```

Expected:
- `TestJourneyStageCount` asserts 8 stages: PASS
- `TestJourneyStageLabels` includes "ACL": PASS
- `TestJourneyStage2_NoDuplicateCalls` shows runner.Done() == true: PASS
- `TestRunJourney_NonTTY` completes without hanging: PASS
- `TestRunJourney_AutoYes`, `TestRunJourney_ResumesFromStage`: PASS (regression)
- `make lint test`: exits 0
</verification>

<success_criteria>
- Stage 2 never calls ensureTailscale or runSecurityFixes
- journeyLabels() returns 8 labels with "ACL" at position 7 (index 6)
- All stages 1–7 gate on `autoYes || !interactive(autoYes, jsonOut)` before huh calls
- New ACL stage at index 7 offers `abysslink acl push --apply`
- Done stage index is 8
- Package doc comment no longer says "NON-BLOCKING" unconditionally
- TestJourneyStageCount asserts 8
- TestJourneyStage2_NoDuplicateCalls passes (runner.Done() == true AND output contains "Prerequisites verified.")
- `make lint test` exits 0
</success_criteria>

<output>
Create `.planning/phases/26-init-journey-gating-first-run-fixes/26-02-SUMMARY.md` when done
</output>
