---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/shell/exec.go
  - internal/cli/cmd_init.go
  - internal/cli/cmd_init_headless_test.go
autonomous: true
requirements:
  - PHASE-26-B1
  - PHASE-26-B3
must_haves:
  truths:
    - "openURL uses shell.LookPath to detect browser opener — no subprocess exit-code probe"
    - "openURL returns a non-nil error when no opener is found (tried: open, xdg-open)"
    - "openURL returns a non-nil error when the opener exits non-zero"
    - "checkACSleepDisabled returns true for annotated pmset output: 'sleep  0 (sleep prevented by powerd)'"
    - "checkACSleepDisabled returns false for annotated pmset output: 'sleep  1 (sleep prevented by caffeinate)'"
    - "TestCheckACSleepDisabled table-driven tests pass (5 cases)"
    - "TestOpenURL_NoOpener asserts error when neither open nor xdg-open is on PATH"
    - "make lint test green"
  artifacts:
    - path: "internal/shell/exec.go"
      provides: "shell.LookPath — package-level PATH probe, not a Runner method"
      contains: "func LookPath(binary string) bool"
    - path: "internal/cli/cmd_init.go"
      provides: "Fixed openURL + deleted isCommandAvailable + fixed checkACSleepDisabled"
      contains: "shell.LookPath"
    - path: "internal/cli/cmd_init_headless_test.go"
      provides: "B3 regression tests: TestCheckACSleepDisabled (5 cases) + TestOpenURL_NoOpener"
  key_links:
    - from: "internal/cli/cmd_init.go openURL"
      to: "internal/shell/exec.go LookPath"
      via: "package-level function call"
      pattern: "shell\\.LookPath"
---

<objective>
Fix the two silent first-run bugs in `abysslink init`:

**B1** — `openURL` on macOS silently does nothing and returns nil (reports "Browser opened" when no browser opened). Root cause: `isCommandAvailable` probes with `--version`; `open --version` exits 1; `ExecRunner.Run` converts ExitError → `(Result, nil)`; so `err == nil` even though the binary rejected the probe. Fix: replace `isCommandAvailable` with `shell.LookPath` (PATH probe — no subprocess) and delete the broken helper. Also fix `openURL` to return an error when the opener exits non-zero, and to return an explicit error when no opener is found (instead of silent `return nil`).

**B3** — `checkACSleepDisabled` never returns true on real macOS output because `pmset -g` emits lines like `sleep  0 (sleep prevented by powerd)` — more than 2 fields — but the parser requires exactly 2. Fix: change `len(fields) == 2` to `len(fields) >= 2`.

Purpose: These bugs cause `abysslink init` to print false-success messages on first run: browser never opens for Tailscale auth, and `sudo pmset` re-runs on every init because sleep is never detected as already disabled.

Output: `shell.LookPath` function, patched `openURL`, deleted `isCommandAvailable`, patched `checkACSleepDisabled`, and regression tests covering both fixes.
</objective>

<execution_context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/26-init-journey-gating-first-run-fixes/26-PATTERNS.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/26-init-journey-gating-first-run-fixes/26-RESEARCH.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/PROJECT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/abysslink/internal/shell/exec.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_init.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_init_headless_test.go
@/Users/mohandoz/u01/innovate/abysslink/internal/shell/mock.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shell.LookPath and fix openURL + checkACSleepDisabled</name>
  <files>internal/shell/exec.go, internal/cli/cmd_init.go</files>
  <read_first>
    - internal/shell/exec.go — existing LookPath function absence confirmed (grep for LookPath); existing import block (os/exec already imported); append LookPath after RunWithStdin
    - internal/cli/cmd_init.go — openURL at lines 227–246 (current broken form with isCommandAvailable); checkACSleepDisabled at lines 593–606 (len == 2 bug); existing import block (shell package already imported)
    - 26-PATTERNS.md §"internal/shell/exec.go" and §"internal/cli/cmd_init.go" — exact replacement patterns and design notes
  </read_first>
  <behavior>
    - shell.LookPath("open") returns true when `open` is on PATH (macOS), false otherwise
    - shell.LookPath("xdg-open") returns true when `xdg-open` is on PATH (Linux), false otherwise
    - shell.LookPath of a non-existent binary returns false
    - openURL with "open" on PATH and exit 0: returns nil
    - openURL with "open" on PATH and exit non-zero: returns error containing "exit"
    - openURL with neither "open" nor "xdg-open" on PATH: returns error containing "no browser opener found"
    - checkACSleepDisabled given "sleep 0\n": returns true
    - checkACSleepDisabled given "sleep  0 (sleep prevented by powerd)\n": returns true
    - checkACSleepDisabled given "sleep  1 (sleep prevented by caffeinate)\n": returns false
    - checkACSleepDisabled given "disksleep 0\n": returns false
    - checkACSleepDisabled given "": returns false
  </behavior>
  <action>
    In internal/shell/exec.go, append after the RunWithStdin function (before the closing of the file) a package-level function `LookPath(binary string) bool` that calls `exec.LookPath(binary)` and returns `err == nil`. No import changes needed — `os/exec` is already imported. Add doc comment explaining: it is a PATH probe (no subprocess spawned), use instead of running `binary --version` since ExecRunner.Run normalises ExitError to (Result, nil) making exit-code-based availability checks unreliable. The function must NOT be added to the Runner interface and must NOT be a method on ExecRunner.

    In internal/cli/cmd_init.go:
    1. Delete the `isCommandAvailable` function entirely (lines ~243–246).
    2. Replace the `openURL` function body (lines ~228–240) with:
       - `switch` on `shell.LookPath("open")` and `shell.LookPath("xdg-open")`
       - For the matched opener: call `runner.Run(ctx, <opener>, url)` where runner is `&shell.ExecRunner{}`; check err != nil → return err; check `res.ExitCode != 0` → return `fmt.Errorf("%s %s: exit %d", opener, url, res.ExitCode)` (per D-open from research); return nil on success
       - Default case: return `fmt.Errorf("no browser opener found (tried: open, xdg-open)")`
       - Remove the silent `return nil` that was the original default

    3. In `checkACSleepDisabled` (lines ~594–606), change the condition `len(fields) == 2` to `len(fields) >= 2`. No other changes to this function.

    License header is already present in both files — do not add a second one.
  </action>
  <verify>
    <automated>go test ./internal/shell/... -run TestLookPath -v 2>/dev/null || echo "no TestLookPath yet — covered in task 2"; go build ./internal/shell/... ./internal/cli/...</automated>
  </verify>
  <done>
    - `shell.LookPath` exists in internal/shell/exec.go as a package-level function (not a Runner method)
    - `isCommandAvailable` is deleted from cmd_init.go
    - `openURL` uses `shell.LookPath` for detection and returns error when opener exits non-zero or no opener found
    - `checkACSleepDisabled` uses `len(fields) >= 2`
    - `go build ./internal/shell/... ./internal/cli/...` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write B1 + B3 regression tests</name>
  <files>internal/cli/cmd_init_headless_test.go</files>
  <read_first>
    - internal/cli/cmd_init_headless_test.go — existing test pattern (testPrinter, mock runner usage, require.NoError style)
    - internal/shell/mock.go — shell.NewMockRunner, shell.Call, shell.Result, MockRunner.RecordedCalls() and Done()
    - 26-PATTERNS.md §"cmd_init_headless_test.go" — TestCheckACSleepDisabled table-driven pattern (5 cases)
    - 26-RESEARCH.md §Validation Architecture — test map for B1 and B3
  </read_first>
  <behavior>
    - TestCheckACSleepDisabled/"exact two fields": checkACSleepDisabled(" sleep 0\n") == true
    - TestCheckACSleepDisabled/"annotated sleep 0": checkACSleepDisabled(" sleep  0 (sleep prevented by powerd)\n") == true
    - TestCheckACSleepDisabled/"sleep enabled annotated": checkACSleepDisabled(" sleep  1 (sleep prevented by caffeinate)\n") == false
    - TestCheckACSleepDisabled/"sleep key absent": checkACSleepDisabled(" disksleep 0\n") == false
    - TestCheckACSleepDisabled/"empty output": checkACSleepDisabled("") == false
    - TestOpenURL_NoOpener: openURL("https://example.com") returns a non-nil error when neither "open" nor "xdg-open" are available on PATH — use the actual PATH absence (go test runs on a real OS; if "open" exists on macOS the test cannot run this path — test only the "no opener" error string from the error message output or skip gracefully when on macOS where "open" exists)
  </behavior>
  <action>
    Add to internal/cli/cmd_init_headless_test.go (after the existing two tests):

    1. `TestCheckACSleepDisabled` — table-driven test with 5 cases (exact two fields / annotated sleep 0 / sleep enabled annotated / sleep key absent / empty output). Each case creates a `shell.NewMockRunner(shell.Call{Result: shell.Result{Stdout: tt.output}})` and calls `checkACSleepDisabled(context.Background(), runner)`. Use `assert.Equal(t, tt.want, got, "pmset output: %q", tt.output)`. Import `context` and `github.com/abysslink/abysslink/internal/shell` if not already present.

    2. `TestOpenURL_NoOpenerError` — calls `openURL("https://example.com")` and asserts the result contains the string "no browser opener found" when neither "open" nor "xdg-open" is on PATH. On macOS where "open" exists, the function will use "open" — skip this specific assertion with `t.Skip("open is on PATH; can't test no-opener path on this machine")` or test only with a mock. Since openURL uses `shell.LookPath` (PATH-based, not mockable via Runner), the cleanest approach: make TestOpenURL_NoOpenerError use t.Skip when `shell.LookPath("open") || shell.LookPath("xdg-open")` returns true, and only assert the error case on machines where neither binary is found. Add a note comment explaining why.

    Ensure the file still has its Apache-2.0 license header. Do not duplicate the existing header — add only the new test functions.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run 'TestCheckACSleepDisabled|TestOpenURL|TestEnsureTailscaleAccount' -v</automated>
  </verify>
  <done>
    - `TestCheckACSleepDisabled` passes all 5 table cases
    - `TestOpenURL_NoOpenerError` is present and either passes or skips cleanly (never fails)
    - `go test ./internal/cli/... -run TestCheckACSleepDisabled -v` exits 0 with 5 sub-tests reported
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → OS PATH | shell.LookPath probes OS PATH for known binaries only; no user input determines the binary name |
| openURL → browser process | URL value is a hardcoded constant (`https://login.tailscale.com/start`), not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | openURL binary selection | mitigate | shell.LookPath checks only two known-safe binaries: "open" (macOS) and "xdg-open" (Linux). No user-supplied binary name. No sh -c. Argv exec direct. |
| T-26-02 | Information Disclosure | openURL false success message | mitigate | B1 fix: openURL now returns error when opener is absent or exits non-zero; caller prints "Could not open browser — visit manually" instead of false "Browser opened" |
| T-26-03 | Elevation of Privilege | checkACSleepDisabled re-running sudo pmset | mitigate | B3 fix ensures `sudo pmset` is not re-invoked when sleep is already disabled; reduces unnecessary sudo calls on each init |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Phase 26 installs zero external packages — no slopcheck required |
</threat_model>

<verification>
After both tasks complete:

```
go test ./internal/shell/... ./internal/cli/... -v -count=1 2>&1 | grep -E 'PASS|FAIL|TestCheckAC|TestOpenURL'
make lint test
```

Expected: `TestCheckACSleepDisabled` shows 5 sub-tests passing; `make lint test` exits 0; no new golangci-lint warnings.
</verification>

<success_criteria>
- `shell.LookPath` exists as a package-level function in internal/shell/exec.go (not on Runner interface)
- `isCommandAvailable` is deleted from cmd_init.go
- `openURL` uses `shell.LookPath` for detection, returns error on no-opener and on non-zero exit
- `checkACSleepDisabled` uses `len(fields) >= 2` — passes annotated pmset output
- `TestCheckACSleepDisabled` reports 5 sub-tests all PASS
- `make lint test` exits 0
</success_criteria>

<output>
Create `.planning/phases/26-init-journey-gating-first-run-fixes/26-01-SUMMARY.md` when done
</output>
