---
phase: 12-headscale-backend
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified:
  - internal/backend/headscale_config.go
  - internal/backend/headscale_doctor.go
autonomous: true
requirements:
  - HS-02
  - HS-03
must_haves:
  truths:
    - "MergeHeadscaleConfig writes six required hardened keys to config.yaml via internal/audit (backup+log on every write)"
    - "MergeHeadscaleConfig preserves user-owned keys (oidc.*, dns.*, node_update_check_interval) outside the hardened set"
    - "MergeHeadscaleConfig uses derp.server.verify_clients: true (not the non-existent verify_client_url_fail_open — R-02)"
    - "HeadscaleDoctorChecks returns a Finding for each of the nine hs-* check names"
    - "hs-lock finding is always SeverityWarning with exact ROADMAP text"
    - "hs-lock can never return SeverityPass — it is a permanent, unconditional WARN"
    - "hs-key-expiry returns SeverityFatal for any active key with Expiration == 0001-01-01T00:00:00Z"
    - "hs-oidc-filter returns PASS/SKIP when oidc.issuer is absent; FAIL only when OIDC is configured AND all filter lists are empty"
    - "hs-proc-user checks platform-correctly: systemctl on Linux, launchctl+ps on macOS"
    - "All nine hs-* checks return correct PASS/WARN/FAIL on the testdata fixtures"
  artifacts:
    - path: "internal/backend/headscale_config.go"
      provides: "MergeHeadscaleConfig() surgical YAML merge function"
      exports: ["MergeHeadscaleConfig"]
      contains: "MergeHeadscaleConfig"
    - path: "internal/backend/headscale_doctor.go"
      provides: "HeadscaleDoctorChecks() returning []modules.Finding for all nine hs-* checks"
      exports: ["HeadscaleDoctorChecks"]
      contains: "HeadscaleDoctorChecks"
  key_links:
    - from: "headscale_config.go MergeHeadscaleConfig()"
      to: "internal/audit WriteFile"
      via: "audit.New(logPath).WriteFile(path, data, 0o600, false)"
      pattern: "audit.*WriteFile"
    - from: "headscale_doctor.go HeadscaleDoctorChecks()"
      to: "modules.Finding{}"
      via: "append findings for each hs-* check"
      pattern: "modules.Finding"
    - from: "headscale_doctor.go hs-lock"
      to: "permanent WARN text"
      via: "unconditional append; no condition ever returns PASS"
      pattern: "server-trust model only"
---

<objective>
Implement the two backend-only service files that handle Headscale server configuration hardening and doctor check reporting.

Purpose: headscale_config.go provides the surgical YAML merge used by Wave 4's server init/upgrade commands to harden config.yaml without overwriting user customizations (D-03). headscale_doctor.go provides HeadscaleDoctorChecks() returning all nine hs-* findings that Wave 4 wires into cmd_doctor.go.

Output: headscale_config.go (MergeHeadscaleConfig), headscale_doctor.go (HeadscaleDoctorChecks with all nine checks), plus test coverage for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-headscale-backend/12-CONTEXT.md
@.planning/phases/12-headscale-backend/12-RESEARCH.md
@.planning/phases/12-headscale-backend/12-PATTERNS.md
@internal/backend/headscale.go
@internal/backend/headscale_test.go
@internal/config/config.go
@internal/audit/audit.go
@internal/modules/deps.go
@.planning/phases/12-headscale-backend/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: headscale_config.go — surgical YAML merge of Headscale config.yaml</name>
  <files>internal/backend/headscale_config.go</files>
  <read_first>
    - .planning/phases/12-headscale-backend/12-PATTERNS.md §"internal/backend/headscale_config.go" (MergeHeadscaleConfig func signature, lenient decoder, required hardened key set)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §"Hardened config.yaml Key Set" (exact YAML keys and values: grpc_allow_insecure, metrics_listen_addr, derp.server.verify_clients, policy.mode, logtail.enabled, database.type/path)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §"Common Pitfalls" Pitfall 1 (NEVER write verify_client_url_fail_open — use derp.server.verify_clients: true per R-02)
    - internal/config/config.go lines 295-311 (audit.WriteFile pattern — only sanctioned write path)
    - internal/audit/audit.go (WriteFile signature)
    - CLAUDE.md (all file mutations via internal/audit; context.Context everywhere; Apache-2.0 header)
  </read_first>
  <behavior>
    - MergeHeadscaleConfig(ctx, cfgPath, hs HeadscaleServer, dryRun bool) reads existing YAML with lenient decoder (KnownFields false)
    - Sets exactly these keys (does NOT overwrite others): grpc_allow_insecure=false, metrics_listen_addr="127.0.0.1:9090", derp.server.enabled=true, derp.server.verify_clients=true, policy.mode="database", logtail.enabled=false, database.type="sqlite", database.sqlite.path=hs.DBPath, server_url=hs.ServerURL
    - NEVER writes verify_client_url_fail_open (does not exist in Headscale config — R-02)
    - In BYO mode (hs.ACME==false): sets tls_cert_path=hs.TLSCertPath, tls_key_path=hs.TLSKeyPath
    - In ACME mode (hs.ACME==true): sets tls_letsencrypt_hostname, tls_letsencrypt_challenge_type="HTTP-01", tls_letsencrypt_listen=":http"; does NOT set tls_cert_path or tls_key_path
    - Uses gopkg.in/yaml.v3 decode→modify struct→re-encode (no regex/string replace)
    - All writes go through audit.New(logPath).WriteFile(path, data, 0o600, false)
    - In dryRun mode: logs the diff but writes nothing; returns nil
    - Re-runs are idempotent: merging already-hardened config returns same bytes
    - Test: TestMergeHeadscaleConfig_SetsRequiredKeys, TestMergeHeadscaleConfig_PreservesUserKeys, TestMergeHeadscaleConfig_Idempotent, TestMergeHeadscaleConfig_NoVerifyClientURLFailOpen
  </behavior>
  <action>
    Create internal/backend/headscale_config.go (package backend, Apache-2.0 header).

    The hardened config key set is implemented via a loosely-typed Go struct mirroring Headscale's config.yaml structure (only the keys abysslink needs to control — use map[string]any for nested structures not in the required set, or a struct with yaml:",inline" for preservation). Recommended pattern: load into a map[string]any, apply required keys, re-encode. This is safer for preservation than a partial struct because yaml.v3 handles unknown keys naturally in maps.

    Struct for the hardened subkeys (inline the nested keys):
    - Top-level: grpc_allow_insecure, metrics_listen_addr, server_url, tls_cert_path, tls_key_path
    - Nested logtail: logtail.enabled
    - Nested policy: policy.mode
    - Nested database: database.type, database.sqlite.path
    - Nested derp.server: derp.server.enabled, derp.server.verify_clients

    Implementation approach: decode YAML into map[string]any; call setNestedKey(m, []string{"derp","server","verify_clients"}, true) for each required key; re-encode. setNestedKey creates intermediate maps as needed. This preserves all user keys not in the required set.

    For dryRun: compute the diff string (old bytes vs. new bytes), emit via slog.Info("headscale config merge [dry-run]"), return nil without writing.

    All writes: audit.New(logPath).WriteFile(path, data, 0o600, false).

    Include tests in a _test.go file (headscale_config_test.go) that use a temp dir: TestMergeHeadscaleConfig_SetsRequiredKeys verifies grpc_allow_insecure=false, derp.server.verify_clients=true, policy.mode=database in the output YAML. TestMergeHeadscaleConfig_NoVerifyClientURLFailOpen reads the output and asserts the string "verify_client_url_fail_open" never appears. TestMergeHeadscaleConfig_PreservesUserKeys starts with a config containing oidc.issuer="https://idp.example.com" and verifies it is preserved after merge.
  </action>
  <verify>
    <automated>go test ./internal/backend/... -run "TestMergeHeadscaleConfig" -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    All TestMergeHeadscaleConfig_* tests PASS; grep confirms "verify_clients" appears in headscale_config.go output path; grep confirms "verify_client_url_fail_open" does NOT appear anywhere in headscale_config.go; make lint exits 0 on this file
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: headscale_doctor.go — all nine hs-* checks with correct PASS/WARN/FAIL signals</name>
  <files>internal/backend/headscale_doctor.go</files>
  <read_first>
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §"All Nine hs-* Doctor Checks — Concrete Signals" (exact PASS/WARN/FAIL signals for each check)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §RF-5 (hs-db-perms: os.Stat perm check; hs-bind: parse config, metrics_listen_addr; hs-proc-user: platform-conditional systemctl vs launchctl+ps)
    - .planning/phases/12-headscale-backend/12-PATTERNS.md §"internal/backend/headscale_doctor.go" (Finding construction pattern, hs-lock verbatim text, hs-db-perms pattern, hs-api-auth pattern, hs-tls pattern)
    - internal/modules/deps.go or internal/modules/module.go (modules.Finding struct, SeverityFatal, SeverityWarning, SeverityInfo constants)
    - internal/backend/headscale.go (headscaleAdapter.doRequest — used by hs-api-auth and hs-key-expiry REST probes)
    - CLAUDE.md (no exec outside shell.Runner, context.Context, Apache-2.0 header, no fmt.Println)
  </read_first>
  <behavior>
    - HeadscaleDoctorChecks(ctx, adapter *headscaleAdapter, runner shell.Runner) returns []modules.Finding for all nine checks
    - hs-lock: ALWAYS appends SeverityWarning with EXACT text "WARN: server-trust model only — Tailnet Lock (TKA) is not available on Headscale"; no condition can ever produce SeverityPass for this check
    - hs-tls (BYO mode): uses crypto/tls.LoadX509KeyPair + crypto/x509.ParseCertificate; FAIL if expired; WARN if within 30 days (cfg.Server.Headscale.CertExpiryWarnDays); FAIL if SAN mismatch (leaf.VerifyHostname); FAIL if key file perms != 0600
    - hs-tls (ACME mode, cfg.Server.Headscale.ACME==true): HTTP GET https://server_url, inspect TLS cert presented; WARN if within 30 days of expiry
    - hs-bind: parse config.yaml via lenient YAML decode; FAIL if metrics_listen_addr contains "0.0.0.0"; FAIL if grpc_allow_insecure==true; WARN if non-standard listen_addr port (not :443)
    - hs-api-auth: GET /api/v1/apikey via adapter.doRequest; PASS=200; FAIL=401/403 or unreachable; WARN if any key expires within 14 days
    - hs-key-expiry: GET /api/v1/preauthkey; for each key where used==false: FAIL if expiration=="0001-01-01T00:00:00Z"; FAIL if expiration <= now; WARN if no active keys
    - hs-db-perms: os.Stat(cfg.DBPath); PASS if mode.Perm()==0o600; WARN if 0o640; FAIL if 0o644 or broader; FAIL if not found
    - hs-oidc-filter: parse config.yaml; SKIP/PASS if no oidc.issuer configured; FAIL if oidc.issuer non-empty AND all of allowed_domains/allowed_users/allowed_groups empty; WARN if issuer set but email_verified_required==false
    - hs-proc-user (Linux): runner.Run(ctx, "systemctl", "show", "headscale", "--property=User") → check output for "User=headscale"; FAIL if User=root or service not found
    - hs-proc-user (macOS): runner.Run(ctx, "launchctl", "list", "net.abysslink.headscale") then runner.Run(ctx, "ps", "-p", pid, "-o", "user=") → check equals "_headscale"; FAIL if root or not running
    - hs-derp-failclosed: parse config.yaml; PASS if derp.server.enabled AND derp.server.verify_clients; WARN if derp.server.enabled==false (external DERP only); FAIL if enabled but verify_clients==false
    - Test: TestHsDoctorChecks_AllNine asserts all nine check names appear in results; TestHsLockPermanentWarn asserts hs-lock is always Warning, never empty, never Pass; TestHsKeyExpiry_ZeroExpiry asserts FAIL on fixture key with expiry "0001-01-01T00:00:00Z"
  </behavior>
  <action>
    Create internal/backend/headscale_doctor.go (package backend, Apache-2.0 header).

    Function signature: HeadscaleDoctorChecks(ctx context.Context, cfg *config.Config, runner shell.Runner, httpClient func(ctx context.Context, method, path string, body any) (*http.Response, error)) ([]modules.Finding, error). Accept the doRequest func to avoid circular dependency on the adapter struct.

    Check ordering: config-parse checks first (no network, no running server: hs-bind, hs-oidc-filter, hs-derp-failclosed, hs-db-perms), then filesystem checks (hs-tls, hs-proc-user via runner), then REST checks (hs-api-auth, hs-key-expiry). hs-lock is always the first finding appended (unconditional, no error).

    hs-lock: do NOT wrap in any if/switch. Always append verbatim:
      modules.Finding{Check: "hs-lock", Module: "headscale", Severity: modules.SeverityWarning, Message: "WARN: server-trust model only — Tailnet Lock (TKA) is not available on Headscale"}
    This is the exact ROADMAP text. Never modify it.

    hs-tls BYO: use crypto/tls and crypto/x509 per PATTERNS.md excerpt. Check file perms on TLSKeyPath with os.Stat + mode.Perm()&0o077 != 0 → FAIL (world/group read). Use leaf.VerifyHostname(fqdn) for SAN check.

    hs-proc-user: platform detection via runtime.GOOS == "darwin" vs. "linux". On linux: runner.Run for systemctl. On darwin: runner.Run for launchctl then runner.Run for ps -p. Never use os/exec directly — all via shell.Runner (CLAUDE.md hard rule).

    All checks: on error reading config or I/O failure, emit SeverityWarning with message "hs-X: could not complete check: <err>" — do not silently swallow errors.

    Include headscale_doctor_test.go with:
    - TestHsDoctorChecks_AllNinePresent: runs HeadscaleDoctorChecks with a minimal config and mock REST/runner; collects check names; asserts all nine names are in the result set.
    - TestHsLockPermanentWarn: asserts the hs-lock finding has Severity==SeverityWarning and Message contains the exact ROADMAP text.
    - TestHsKeyExpiry_ZeroExpiry: mock returns headscale_preauthkeys.json (contains zero-expiry key); assert hs-key-expiry finding has SeverityFatal.
    - TestHsDerpFailclosed_OpenRelay: config with derp.server.enabled=true, verify_clients=false; assert SeverityFatal.
    - TestHsOidcFilter_SkipWhenAbsent: no oidc section; assert no FAIL finding for hs-oidc-filter.
    - TestHsOidcFilter_FailWhenOpen: oidc.issuer set, empty filter lists; assert SeverityFatal.
  </action>
  <verify>
    <automated>go test ./internal/backend/... -run "TestHs" -v -count=1 2>&1 | tail -50</automated>
  </verify>
  <done>
    All TestHs* tests PASS; TestHsLockPermanentWarn confirms exact ROADMAP text; TestHsKeyExpiry_ZeroExpiry confirms FAIL on zero-expiry key; make lint test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headscale_config.go → headscale config.yaml | Config file written via internal/audit only; lenient YAML decode; never writes unknown keys |
| headscale_doctor.go → Headscale REST API | Bearer key via doRequest; REST probes are read-only (GET only for doctor) |
| headscale_doctor.go → OS process info | shell.Runner for systemctl/launchctl/ps — never os/exec directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | headscale_config.go surgical merge | mitigate | All writes via audit.WriteFile (backup + log); dryRun mode writes nothing; idempotent re-runs; KnownFields-false lenient decode preserves user keys |
| T-12-03-02 | Information Disclosure | headscale config.yaml tls_key_path perms | mitigate | hs-tls check: FAIL if key file perm group/world readable (mode & 0o077 != 0); FAIL if SAN mismatch; explicit warning days threshold |
| T-12-03-03 | Elevation of Privilege | Open OIDC registration | mitigate | hs-oidc-filter: FAIL when oidc.issuer set with empty filter lists (D-12); SKIP when OIDC absent (no false-positive rate) |
| T-12-03-04 | Information Disclosure | Embedded DERP as open relay | mitigate | hs-derp-failclosed: FAIL when verify_clients=false and enabled=true (R-02 correct key); NEVER uses verify_client_url_fail_open |
| T-12-03-05 | Information Disclosure | Metrics endpoint exposed | mitigate | hs-bind: FAIL if metrics_listen_addr contains 0.0.0.0 |
| T-12-03-06 | Elevation of Privilege | Headscale running as root | mitigate | hs-proc-user: FAIL if UID=0; platform-conditional runner calls |
| T-12-03-07 | Information Disclosure | SQLite DB world-readable | mitigate | hs-db-perms: FAIL if perm >= 0o644; also checks WAL/SHM files |
| T-12-03-08 | Elevation of Privilege | Pre-auth key without expiry (#1579) | mitigate | hs-key-expiry: FAIL if expiration=="0001-01-01T00:00:00Z" or expired; closes upstream #1579 vector |
| T-12-03-09 | Elevation of Privilege | TKA absent on Headscale | accept | hs-lock: permanent WARN (cannot be mitigated — users must understand server-trust model; text is verbatim from ROADMAP; can never PASS) |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run after both tasks complete:
1. `make lint test` exits 0
2. `go test ./internal/backend/... -run TestMergeHeadscaleConfig -v` — all PASS
3. `go test ./internal/backend/... -run TestHs -v` — all PASS
4. `grep -c "verify_client_url_fail_open" internal/backend/headscale_config.go` — returns 0
5. `grep -c "verify_clients" internal/backend/headscale_config.go` — returns >= 1
6. `grep -c "server-trust model only" internal/backend/headscale_doctor.go` — returns >= 1 (exact ROADMAP text)
7. `grep -c "audit.*WriteFile" internal/backend/headscale_config.go` — returns >= 1
8. All nine check names present: `grep -c "hs-tls\|hs-bind\|hs-api-auth\|hs-key-expiry\|hs-db-perms\|hs-lock\|hs-oidc-filter\|hs-proc-user\|hs-derp-failclosed" internal/backend/headscale_doctor.go` — returns 9
</verification>

<success_criteria>
- `make lint test` is green
- MergeHeadscaleConfig writes all six required hardened keys and never writes verify_client_url_fail_open
- HeadscaleDoctorChecks returns findings for all nine hs-* check names
- hs-lock is always SeverityWarning with exact ROADMAP text; test asserts it can never be empty or Pass
- hs-key-expiry returns SeverityFatal for zero-expiry key (issue #1579 closure)
- hs-oidc-filter SKIP when no OIDC; FAIL only on open OIDC with empty filter lists
- hs-derp-failclosed uses derp.server.verify_clients (not verify_client_url_fail_open)
- All writes to config.yaml go through audit.WriteFile — no bare os.WriteFile
</success_criteria>

<output>
Create `.planning/phases/12-headscale-backend/12-03-SUMMARY.md` when done
</output>
