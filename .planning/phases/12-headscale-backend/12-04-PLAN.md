---
phase: 12-headscale-backend
plan: "04"
type: execute
wave: 4
depends_on:
  - "12-03"
files_modified:
  - internal/cli/cmd_server_headscale.go
  - internal/cli/root.go
  - internal/cli/cmd_doctor.go
autonomous: false
requirements:
  - HS-01
  - HS-02
  - HS-03
user_setup:
  - service: headscale
    why: "macOS launchd service-account creation uses dscl (privileged), requires sudo on target machine; confirmed MEDIUM confidence path (R-03)"
    dashboard_config:
      - task: "Review and confirm macOS _headscale service account creation"
        location: "abysslink server headscale init --apply (macOS only) — triggers checkpoint:human-verify before dscl commands"
must_haves:
  truths:
    - "abysslink server headscale init --apply refuses BEFORE any mutation if TLS cert is invalid — gate calls backend.ValidateHeadscaleTLS (from internal/backend, Plan 12-03); no circular import (D-05/D-06 / SC-2)"
    - "abysslink server headscale init --apply service is installed and health-checked BEFORE any REST admin call (API key bootstrap, D-13 user-ensure, pre-auth key)"
    - "abysslink server headscale init --apply creates/ensures the 'abysslink' Headscale user via REST before minting any pre-auth key (D-13)"
    - "abysslink server headscale init --apply mints an API key via one-time shell bootstrap (first install only); steady-state re-init uses REST POST /api/v1/apikey (D-08)"
    - "abysslink server headscale init --apply downloads Headscale binary, verifies SHA-256 checksum (NOT cosign — R-01), writes hardened config.yaml via internal/audit, installs systemd (Linux) or launchd (macOS) service as non-root, creates pre-auth key with explicit expiry (D-01) (D-02)"
    - "abysslink server headscale upgrade --apply verifies new binary checksum, refuses downgrade, stops service, swaps binary via internal/audit, restarts service"
    - "abysslink server headscale backup runs abysslink backup scope that includes the SQLite DB path"
    - "abysslink server headscale status reports service state, Headscale version, and API reachability"
    - "abysslink server headscale init --dry-run prints plan but mutates nothing"
    - "All nine hs-* checks appear in abysslink doctor output when backend.type is headscale"
    - "hs-lock WARN appears in doctor output with exact ROADMAP text regardless of configuration"
    - "macOS dscl service-account creation task has checkpoint:human-verify gate (R-03)"
    - "Headscale binary is downloaded from github.com/juanfont/headscale/releases/download/v{ver}/ over HTTPS; artifact is a bare binary (no tarball); artifact name: headscale_{ver}_{os}_{arch}"
    - "Pinned version v0.28.0 floor; init refuses if headscale version reports < v0.23.0"
  artifacts:
    - path: "internal/cli/cmd_server_headscale.go"
      provides: "abysslink server headscale {init,status,upgrade,backup} commands"
      exports: ["newServerCmd"]
      contains: "newServerCmd"
    - path: "internal/cli/root.go"
      provides: "server command registered in opsCmds"
      contains: "newServerCmd()"
    - path: "internal/cli/cmd_doctor.go"
      provides: "findingFix map extended with nine hs-* entries"
      contains: "hs-lock"
  key_links:
    - from: "internal/cli/root.go opsCmds"
      to: "internal/cli/cmd_server_headscale.go newServerCmd()"
      via: "append to opsCmds slice"
      pattern: "newServerCmd"
    - from: "cmd_server_headscale.go init command — TLS gate (step 0, before any mutation)"
      to: "internal/backend.ValidateHeadscaleTLS(ctx, cfg)"
      via: "direct call from cli to backend; dependency direction is cli→backend (no cycle); function exported from internal/backend/headscale_doctor.go in Plan 12-03"
      pattern: "backend\\.ValidateHeadscaleTLS"
    - from: "cmd_server_headscale.go init command — service health-check (step 7)"
      to: "GET /api/v1/apikey or /api/v1/node"
      via: "poll loop via net/http until 200 or 30s timeout; must succeed before REST admin calls begin"
      pattern: "healthCheck\|waitReady"
    - from: "cmd_server_headscale.go init command — user ensure (D-13, step 9)"
      to: "REST GET/POST /api/v1/user"
      via: "doRequest pattern; idempotent check-then-create; called AFTER service is running"
      pattern: "api/v1/user"
    - from: "cmd_server_headscale.go init command — API key mint"
      to: "REST POST /api/v1/apikey (re-init) or shell headscale apikeys create (first bootstrap)"
      via: "ensureAPIKey helper; shell path one-time only with inline comment per D-08 exception"
      pattern: "api/v1/apikey\|apikeys.*create"
    - from: "cmd_server_headscale.go init command"
      to: "internal/backend.MergeHeadscaleConfig()"
      via: "call after binary download, before service install"
      pattern: "MergeHeadscaleConfig"
    - from: "cmd_server_headscale.go init command"
      to: "internal/backend.HeadscaleDoctorChecks()"
      via: "cmd_doctor.go findingFix map + doctor pipeline"
      pattern: "HeadscaleDoctorChecks"
    - from: "cmd_server_headscale.go upgrade"
      to: "verifyChecksum() in cmd_upgrade.go"
      via: "same cli package — direct function call reuse"
      pattern: "verifyChecksum"
---

<objective>
Implement the abysslink server headscale CLI subtree, wire it into root.go's opsCmds, and extend cmd_doctor.go with hs-* fix entries. This plan covers the full HS-02 server provisioning command surface and the HS-03 doctor integration point.

CIRCULAR IMPORT FIX: ValidateHeadscaleTLS lives in internal/backend (exported by Plan 12-03). The cli init command calls backend.ValidateHeadscaleTLS(ctx, cfg). The dependency direction is cli→backend throughout. internal/backend NEVER imports internal/cli.

INIT SEQUENCE FIX: The correct security-safe sequence ensures the Headscale HTTP server is running before any REST call:
  Step 0: TLS gate — backend.ValidateHeadscaleTLS (fail-closed, before any mutation)
  Step 1-2: Download binary + verify SHA-256 checksum (R-01)
  Step 3: macOS xattr quarantine clear (ignore error if absent)
  Step 4: Version floor check (>= v0.23.0) — headscale version on tmp binary
  Step 5: Write binary via audit.WriteFile mode 0o755
  Step 6: MergeHeadscaleConfig (hardened config.yaml)
  Step 7: Service install (systemd/launchd) + start
  Step 8: Health-check — poll GET /api/v1/node or /api/v1/apikey until 200 or 30s timeout; fail closed on timeout; NO REST admin calls before this passes
  Step 9: API key bootstrap — ensureAPIKey helper (first install: one-time shell exception per D-08; re-init: REST POST /api/v1/apikey)
  Step 10: D-13 user-ensure — GET/POST /api/v1/user (REST, called AFTER service is running and API key exists)
  Step 11: Pre-auth key mint — POST /api/v1/preauthkey

Task 1a covers provisioning foundation (steps 0-6 + upgrade command).
Task 1b-init covers service bring-up + secrets (steps 7-11 + init tests).
Task 1c covers status + backup + cmd_doctor.go + root.go wiring.
The macOS launchd path carries a checkpoint:human-verify (Task 2) due to MEDIUM confidence (R-03).

Purpose: Wave 4 is the user-visible output of Phase 12. The commands abysslink server headscale init/status/upgrade/backup complete HS-02. The doctor fix-map extension completes HS-03.

Output: cmd_server_headscale.go (four subcommands), root.go (server registered), cmd_doctor.go (nine hs-* fix entries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-headscale-backend/12-CONTEXT.md
@.planning/phases/12-headscale-backend/12-RESEARCH.md
@.planning/phases/12-headscale-backend/12-PATTERNS.md
@internal/cli/cmd_upgrade.go
@internal/cli/cmd_lock.go
@internal/cli/cmd_doctor.go
@internal/cli/root.go
@internal/backend/headscale_config.go
@internal/backend/headscale_doctor.go
@.planning/phases/12-headscale-backend/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: cmd_server_headscale.go — provisioning foundation: TLS gate call, download/verify, version floor, write binary, config merge, upgrade command</name>
  <files>
    internal/cli/cmd_server_headscale.go
  </files>
  <read_first>
    - internal/cli/cmd_upgrade.go (verifyChecksum, downloadFile functions — reuse directly in same package; SHA-256 logic; do NOT use cosign for Headscale per R-01)
    - internal/cli/cmd_lock.go (newLockCmd parent command pattern, dry-run gate, print-once secret via Printer only)
    - internal/backend/headscale_doctor.go (ValidateHeadscaleTLS exported function signature — call as backend.ValidateHeadscaleTLS(ctx, cfg) from cli; dependency direction is cli→backend, NEVER backend→cli)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §RF-3 "Confirmed REST endpoints" (endpoint signatures for later tasks)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §"Headscale Release Artifact Naming" (download URL, checksums.txt URL, pinned v0.28.0, v0.23.0 minimum)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §RF-2 macOS launchd (dscl idempotency, plist content, port 443 binding)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §"Open Questions" Q3 (macOS Gatekeeper xattr removal)
    - .planning/phases/12-headscale-backend/12-CONTEXT.md §D-05/D-06 (TLS gate: BYO strict validation, ACME mode assertion, fail-closed before any mutation)
    - .planning/phases/12-headscale-backend/12-CONTEXT.md §D-04 (upgrade: verify → backup DB → swap; refuse downgrade)
    - CLAUDE.md (all exec via shell.Runner; all file mutations via internal/audit; no sh -c; no secrets on argv; --dry-run default; Apache-2.0 header; no fmt.Println)
  </read_first>
  <behavior>
    - newServerCmd() + newServerHeadscaleCmd() parent group pattern follows cmd_lock.go exactly
    - Create the file and command skeleton (newServerCmd, newServerHeadscaleCmd, four subcommand stubs); Task 1b-init fills in the service + secrets steps
    - init RunE steps 0-6 implemented here (the pre-service steps):
        0. TLS gate (FAIL-CLOSED, before any mutation): call backend.ValidateHeadscaleTLS(ctx, cfg); if non-nil error, return it immediately — no mutation has occurred. This call imports internal/backend from internal/cli (cli→backend direction, correct, no cycle).
        1. Download headscale_{ver}_{os}_{arch} binary + checksums.txt via HTTPS (reuse downloadFile from cmd_upgrade.go)
        2. Verify SHA-256 via verifyChecksum() — fail-closed on mismatch (R-01, NOT cosign)
        3. On macOS: runner.Run(ctx, "xattr", "-d", "com.apple.quarantine", tmpBinaryPath) — ignore error if attr not set
        4. Check version floor: runner.Run(ctx, tmpBinaryPath, "version") → parse; if < v0.23.0, return error "headscale: version %s is below minimum supported v0.23.0"
        5. Write binary to cfg.Server.Headscale.BinaryPath via audit.WriteFile mode 0o755
        6. Call MergeHeadscaleConfig(ctx, configPath, cfg.Server.Headscale, dryRun) for hardened config
    - Steps 7-11 (service + secrets) are implemented in Task 1b-init; the init RunE calls a helper completeInit(ctx, cfg, cc, runner, p) for those steps so the two tasks edit disjoint logical units
    - server headscale upgrade RunE: download new binary + checksums.txt; verifyChecksum; check version; refuse downgrade (compare semver, strip "v" prefix); backup DB first via audit.Backup; stop service via runner.Run systemctl/launchctl; write new binary via audit.WriteFile; start service; health-check via GET /api/v1/node returning 200
    - dryRun gate: cc.dryRun checked before any mutation (steps 5-6); in dryRun mode log plan via slog.Info and return nil; TLS gate (step 0) still runs in dry-run (pre-flight, not a mutation)
    - Constants at top: headscaleDefaultVersion = "v0.28.0", headscaleMinVersion = "v0.23.0", headscaleBaseURL = "https://github.com/juanfont/headscale/releases/download"
    - Tests: TestServerHeadscaleUpgradeRefusesDowngrade; TestServerHeadscaleInitDryRunMutatesNothing (steps 0-6 only, service steps stubbed); TestServerHeadscaleInitTLSGateRejects (BYO mode: init returns error when cert not found, before any download); TestServerHeadscaleInitTLSGateRejectsACMEMissingHostname (ACME mode: init returns error when ACMEHostname empty)
  </behavior>
  <action>
    Create internal/cli/cmd_server_headscale.go (package cli, Apache-2.0 header).

    Constants at top: headscaleDefaultVersion = "v0.28.0", headscaleMinVersion = "v0.23.0", headscaleBaseURL = "https://github.com/juanfont/headscale/releases/download". Artifact name pattern: fmt.Sprintf("headscale_%s_%s_%s", strings.TrimPrefix(ver,"v"), runtime.GOOS, runtime.GOARCH).

    newServerCmd() returns a cobra.Command with Use="server" following cmd_lock.go's parent-group pattern. newServerHeadscaleCmd() adds four subcommands: init, status, upgrade, backup. Register in root.go opsCmds (done in Task 1c).

    init RunE: implement steps 0-6 in sequence. Step 0 must be the very first line after argument parsing — call backend.ValidateHeadscaleTLS(ctx, cfg) and return immediately on error. Import path: "github.com/abysslink/abysslink/internal/backend". This is the correct import direction (cli→backend); internal/backend never imports internal/cli, so there is no cycle.

    After step 6 (MergeHeadscaleConfig), the init RunE calls completeInit(ctx, cfg, cc, runner, p) — a helper function defined in Task 1b-init that handles steps 7-11 (service install, health-check, API key, user-ensure, pre-auth key). For now, stub completeInit as returning nil so the file compiles.

    upgrade RunE: parse version strings; compare with semver semantics (strip "v", split by ".", compare numeric). BEFORE binary write: backup DB via audit.Backup(cfg.Server.Headscale.DBPath). Service stop/start via runner.Run (systemctl/launchctl). Health-check via doHeadscaleHealthCheck helper (poll GET /api/v1/node, 30s timeout, 2s interval).

    Dry-run gate: at top of init and upgrade RunE, after TLS gate, check cc.dryRun — if true, emit slog.Info plan but skip mutations (steps 5-6 in init, binary swap in upgrade). TLS gate always runs.

    Test file cmd_server_headscale_test.go: TestServerHeadscaleUpgradeRefusesDowngrade — MockRunner returns older version string from "headscale version"; assert error contains "refusing downgrade". TestServerHeadscaleInitDryRunMutatesNothing — call init RunE with dryRun=true and a valid mock cert; verify audit log has no new entries. TestServerHeadscaleInitTLSGateRejects — cfg with missing cert path in BYO mode; assert init returns non-nil error before downloading binary (verify by checking MockRunner was never called for downloadFile). TestServerHeadscaleInitTLSGateRejectsACMEMissingHostname — ACME mode with empty ACMEHostname; assert init returns error.
  </action>
  <verify>
    <automated>go build ./internal/cli/... 2>&1 && echo BUILD_OK && go test ./internal/cli/... -run "TestServerHeadscaleUpgradeRefusesDowngrade|TestServerHeadscaleInitDryRun|TestServerHeadscaleInitTLSGate" -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    go build ./internal/cli/... exits 0; TLS gate tests PASS; downgrade-refusal test PASS; dry-run test PASS; grep confirms "backend.ValidateHeadscaleTLS" in cmd_server_headscale.go (NOT a local function — calls the exported backend function); grep confirms no "func ValidateHeadscaleTLS" defined in cmd_server_headscale.go (function lives in internal/backend, not cli)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b-init: cmd_server_headscale.go — service bring-up + secrets: install + start + health-check + API key + user-ensure + pre-auth key</name>
  <files>
    internal/cli/cmd_server_headscale.go
  </files>
  <read_first>
    - internal/cli/cmd_server_headscale.go (Task 1a stub — fill in completeInit and helpers defined here)
    - internal/cli/cmd_lock.go (print-once secret pattern: renderSecretsToBox + attestSecretsStored; never log secrets via slog or audit)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §RF-3 "Confirmed REST endpoints" (POST /api/v1/apikey, GET /api/v1/user, POST /api/v1/user, POST /api/v1/preauthkey — request/response bodies)
    - .planning/phases/12-headscale-backend/12-RESEARCH.md §RF-2 macOS launchd (service install steps; dscl idempotency)
    - .planning/phases/12-headscale-backend/12-CONTEXT.md §D-08 (all admin ops via REST — the ONE-TIME bootstrap shell exception for first API key, with required inline comment)
    - .planning/phases/12-headscale-backend/12-CONTEXT.md §D-13 (user-ensure: GET /api/v1/user list, POST /api/v1/user create if absent, BEFORE pre-auth key; called AFTER service is running)
    - .planning/phases/12-headscale-backend/12-CONTEXT.md §D-11 (pre-auth key: explicit expiry, keychain, never on argv)
    - CLAUDE.md (no secrets on argv; no secrets in audit log; context.Context; Apache-2.0 header; no fmt.Println)
  </read_first>
  <behavior>
    - completeInit(ctx context.Context, cfg *config.Config, cc *cmdContext, runner shell.Runner, p Printer) error implements init steps 7-11:
        7. Service install + start:
           - Linux: write /etc/systemd/system/headscale.service unit file via audit.WriteFile (User=headscale, ExecStart=binaryPath --config configPath); runner.Run systemctl daemon-reload; runner.Run systemctl enable headscale; runner.Run systemctl start headscale
           - macOS: code path exists but MUST be gated — the macOS dscl + launchd plist path is only exercised after checkpoint:human-verify (Task 2); on darwin, completeInit returns early with slog.Info("macOS service install: awaiting human-verify checkpoint") and sets a flag so the caller knows to surface Task 2
        8. Health-check: poll doHeadscaleHealthCheck(ctx, baseURL) — GET baseURL+"/api/v1/node" (or "/api/v1/apikey" — whichever returns 200 without auth); retry every 2s for up to 30s; fail closed on timeout with error "headscale: health-check timed out after 30s — service may not have started". NO REST admin calls (steps 9-11) until this check passes.
        9. API key bootstrap via ensureAPIKey(ctx, cfg, runner, binaryPath) (string, error):
           - Check keychain for existing key; if present and GET /api/v1/apikey returns 200 → return existing key (re-init path uses REST POST /api/v1/apikey to rotate)
           - First install (no key in keychain): runner.Run(ctx, binaryPath, "apikeys", "create", "--expiration", "90d") — parse output for the key; store in platform.Keychain; print via Printer ONLY (never slog, never audit content field). Add inline comment: "first-time bootstrap only — headscale binary invoked once to obtain the initial API key; no REST auth key exists yet; all subsequent admin ops use REST per D-08"
           - Re-init path (key in keychain but need rotation): REST POST /api/v1/apikey with body {"expiration": RFC3339 90d from now}; parse {"apiKey":"<key>"}; store in keychain; print via Printer
        10. D-13 user-ensure via ensureHeadscaleUser(ctx, baseURL, apiKey, userName string) error:
            - GET /api/v1/user with Authorization: Bearer apiKey header; iterate JSON users list; if user named cfg.Server.Headscale.User (default "abysslink") found → return nil
            - If not found → POST /api/v1/user with body {"name":"abysslink"}; accept 200 or 409 Conflict; return error on other status codes
        11. Pre-auth key mint: POST /api/v1/preauthkey with Authorization: Bearer apiKey; body: {"user": cfg.Server.Headscale.User, "reusable": false, "ephemeral": true, "expiration": RFC3339 time.Now().Add(preAuthKeyExpiry)}; parse response; store key in platform.Keychain; print via Printer (never argv, never logged)
    - dryRun gate: all of steps 7-11 are skipped in dry-run mode; the caller (init RunE) checks cc.dryRun before calling completeInit
    - Tests: TestServerHeadscaleInitServiceStartBeforeREST (verify mock REST is NOT called if mock service-start fails — service must start first); TestServerHeadscaleInitHealthCheckTimeout (health-check mock returns non-200 for 30s; assert init returns timeout error before REST calls); TestServerHeadscaleInitAPIKeyBootstrap (first install: mock keychain empty; assert runner called with "apikeys create"; assert key printed via Printer); TestServerHeadscaleInitUserEnsure_AlreadyExists (D-13 GET returns user; assert POST not called); TestServerHeadscaleInitUserEnsure_Creates (D-13 GET returns no user; assert POST called with {"name":"abysslink"}); TestServerHeadscaleInitPreAuthKey (assert POST /api/v1/preauthkey called after user-ensure, key stored in keychain)
  </behavior>
  <action>
    Add to internal/cli/cmd_server_headscale.go (Task 1a created the file — Task 1b-init fills in the completeInit stub and adds the helper functions).

    Replace the completeInit stub from Task 1a with the full implementation.

    doHeadscaleHealthCheck(ctx, baseURL string) error: use a ticker/deadline pattern — context.WithTimeout(ctx, 30*time.Second); loop: GET baseURL+"/api/v1/node" with http.DefaultClient; if 200 → return nil; sleep 2s; on deadline exceeded → return descriptive error. This is a no-auth GET — Headscale returns 401 once running, which also counts as "server is up" (401 != network unreachable). Adjust the health-check to treat any non-connection-error response (200 or 401 or 403) as "server is up" — only connection errors (dial tcp: connection refused) mean the server is not ready.

    ensureAPIKey: implement keychain check via platform.Keychain.Get("headscale-api-key"). If found and REST probe returns 200 → return key. If found but REST probe fails → proceed to rotation (REST POST). If not found → first-bootstrap shell path (runner.Run). Shell call uses runner.Run (not os/exec directly). Add the required inline comment about the D-08 exception.

    ensureHeadscaleUser: implement with net/http (not shell.Runner — REST call per D-08). Use context-aware http.NewRequestWithContext. Handle 409 Conflict from POST as success (idempotent).

    Pre-auth key helper: implement as mintPreAuthKey(ctx, baseURL, apiKey, user string, expiry time.Duration) (string, error). POST /api/v1/preauthkey. Parse {"preAuthKey":{"key":"...","expiration":"..."}} response. Return the key string. Caller stores in keychain and prints via Printer.

    Test file additions (add to cmd_server_headscale_test.go): TestServerHeadscaleInitServiceStartBeforeREST; TestServerHeadscaleInitHealthCheckTimeout; TestServerHeadscaleInitAPIKeyBootstrap; TestServerHeadscaleInitUserEnsure_AlreadyExists; TestServerHeadscaleInitUserEnsure_Creates; TestServerHeadscaleInitPreAuthKey.
  </action>
  <verify>
    <automated>go build ./internal/cli/... 2>&1 && echo BUILD_OK && go test ./internal/cli/... -run "TestServerHeadscaleInit" -v -count=1 2>&1 | tail -40</automated>
  </verify>
  <done>
    go build ./internal/cli/... exits 0; all TestServerHeadscaleInit* tests PASS; grep confirms "/api/v1/user" in cmd_server_headscale.go (D-13 user-ensure present); grep confirms "/api/v1/preauthkey" in cmd_server_headscale.go (pre-auth key mint present); grep confirms "apikeys.*create" or "apikeys create" in cmd_server_headscale.go (bootstrap shell exception present); grep confirms inline comment "first-time bootstrap only" in cmd_server_headscale.go; grep confirms no bare fmt.Println in cmd_server_headscale.go
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1c: cmd_server_headscale.go status + backup; root.go + cmd_doctor.go wiring</name>
  <files>
    internal/cli/cmd_server_headscale.go
    internal/cli/root.go
    internal/cli/cmd_doctor.go
  </files>
  <read_first>
    - internal/cli/root.go lines 90-140 (opsCmds slice — append newServerCmd() here)
    - internal/cli/cmd_doctor.go (findingFix map + doctorRunE pattern — extend findingFix with nine hs-* entries; locate where per-backend findings are collected to add the headscale case)
    - internal/cli/cmd_backup.go (backup manifest pattern — SQLite DB path joins here)
    - .planning/phases/12-headscale-backend/12-PATTERNS.md §"internal/cli/cmd_doctor.go" (findingFix map entry format for hs-* checks; hs-lock has empty fix string — permanent WARN)
    - internal/backend/headscale_doctor.go (HeadscaleDoctorChecks func signature — needed to wire into doctorRunE)
    - CLAUDE.md (no fmt.Println; context.Context; Apache-2.0 header)
  </read_first>
  <behavior>
    - server headscale status RunE: call ensureAPIKey helper from Task 1b-init to verify API key present and working; runner.Run(ctx, binaryPath, "version"); runner.Run for systemctl/launchctl is-active; assemble and print status struct via Printer (JSON-capable)
    - server headscale backup RunE: call audit.Backup extended to include cfg.Server.Headscale.DBPath; emit status via Printer
    - root.go: append newServerCmd() to opsCmds slice after newDaemonCmd(); do not reorder existing commands
    - cmd_doctor.go findingFix map: add all nine hs-* entries; hs-lock entry has empty fix string (permanent WARN, no fix action)
    - cmd_doctor.go doctorRunE: add if cfg.Backend.Type == "headscale" { findings = append(findings, backend.HeadscaleDoctorChecks(ctx, cfg, runner, adapter.doRequest)...) }
    - Tests: TestServerHeadscaleDoctorFindings_AllNinePresent (calls backend.HeadscaleDoctorChecks with mock config; asserts all nine check names in results); TestServerHeadscaleRootRegistered (verifies "server" command appears in root command's children list)
  </behavior>
  <action>
    Add to internal/cli/cmd_server_headscale.go (Tasks 1a+1b-init built init+upgrade — Task 1c adds status + backup to the already-created newServerHeadscaleCmd() group):

    status sub-command: RunE calls ensureAPIKey helper to verify API key; calls runner.Run for "headscale version"; calls runner.Run for systemctl/launchctl is-active check; assembles and prints a status struct via Printer.

    backup sub-command: RunE calls the same backup mechanism as cmd_backup.go — extend the backup manifest to include cfg.Server.Headscale.DBPath if backend.type == "headscale". Print success message via Printer.

    In root.go: locate opsCmds slice; append newServerCmd() after newDaemonCmd() before the for-loop that assigns GroupID values. Do not reorder existing commands.

    In cmd_doctor.go findingFix map: add nine entries keyed by the exact hs-* check names:
    - "hs-tls": "Renew or replace the TLS certificate; ensure key file is 0600 owned by the service user"
    - "hs-bind": "Set metrics_listen_addr: 127.0.0.1:9090 and grpc_allow_insecure: false in Headscale config.yaml"
    - "hs-api-auth": "Rotate the Headscale API key: abysslink server headscale init --apply (re-init mints a new key)"
    - "hs-key-expiry": "Expire affected pre-auth keys via the Headscale admin panel or REST API; re-mint with explicit expiry"
    - "hs-db-perms": "chmod 0600 /var/lib/headscale/db.sqlite && chown headscale:headscale /var/lib/headscale/db.sqlite"
    - "hs-lock": "" (empty — permanent WARN, no fix possible; TKA is not available on Headscale)
    - "hs-oidc-filter": "Set oidc.allowed_domains or oidc.allowed_users in Headscale config.yaml to restrict OIDC registration"
    - "hs-proc-user": "Ensure Headscale runs as the dedicated service user: check systemd User= or launchd UserName= in the service unit"
    - "hs-derp-failclosed": "Set derp.server.verify_clients: true in Headscale config.yaml (R-02 correct key)"

    In cmd_doctor.go doctorRunE: after existing finding collection, add the headscale case. The doRequest func passed to HeadscaleDoctorChecks is sourced from the headscaleAdapter (cast via type assertion if available, or construct a minimal net/http wrapper with the API key from keychain).

    Test file: TestServerHeadscaleDoctorFindings_AllNinePresent — construct a minimal mock headscale config; call backend.HeadscaleDoctorChecks with nil httpClient (for checks that do not need REST); assert all nine check names appear in the returned Finding slice.
  </action>
  <verify>
    <automated>go build ./internal/cli/... 2>&1 && echo BUILD_OK && go test ./internal/cli/... -run "TestServerHeadscaleDoctorFindings|TestServerHeadscaleRootRegistered" -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    go build ./internal/cli/... exits 0; grep confirms "newServerCmd()" in root.go; grep confirms "hs-lock" in cmd_doctor.go; grep confirms "HeadscaleDoctorChecks" in cmd_doctor.go; all nine hs-* fix entries present in findingFix; make lint test exits 0
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: macOS launchd — _headscale service account creation + launchd plist review (R-03 MEDIUM-confidence path)</name>
  <what-built>
    The macOS launchd provisioning path in cmd_server_headscale.go's completeInit function is complete but has been marked as a MEDIUM-confidence path by research (R-03). The code:
    1. Creates a dedicated _headscale service account via dscl (sudo) — check for existing account before running to be idempotent
    2. Creates /var/lib/headscale and /etc/headscale with chown to _headscale:_headscale
    3. Writes /Library/LaunchDaemons/net.abysslink.headscale.plist with UserName=_headscale, RunAtLoad=true, KeepAlive=true
    4. Runs launchctl load /Library/LaunchDaemons/net.abysslink.headscale.plist
    5. Probes ps -p $(pgrep headscale) -o user= and asserts result == "_headscale" (not root)

    Research confirmed: macOS post-Mojave allows non-root UserName in /Library/LaunchDaemons plists. listen_addr: ":443" (0.0.0.0:443) is required on macOS because binding a specific IP as non-root on port 443 fails. UID 399 is used for the _headscale account (ASSUMED — may conflict on some systems).
  </what-built>
  <how-to-verify>
    On a macOS test machine (Mojave+), run:
    1. `abysslink server headscale init --dry-run` — should print the full plan including the dscl commands without executing them
    2. `abysslink server headscale init --apply` — executes the provisioning (requires sudo for dscl and launchctl load)
    3. After init completes: `launchctl list net.abysslink.headscale` — should return a PID row (not just "-")
    4. `ps aux | grep headscale | grep -v grep` — the headscale process should be running as _headscale (not root)
    5. `id -u _headscale` — should return UID 399 (or the chosen unused UID — verify it is less than 500 and not in use by any existing system account)
    6. Run `abysslink doctor` with backend.type: headscale — hs-proc-user should report PASS
    7. On a second run: `abysslink server headscale init --apply` — should be a no-op (dscl checks for existing account, plist only written if not present, launchctl load is idempotent or skipped if already loaded)

    Expected outcome: Headscale runs as _headscale, not root. hs-proc-user returns PASS. Second run is idempotent.

    If the _headscale UID 399 conflicts on the test machine: change the UID to an unused value less than 500 and re-run init. Document the chosen UID.

    If Headscale starts as root despite the plist UserName key: this is the A1 assumption failure. In that case, report here so the fallback (port > 1024 or explicit sudo drop) can be planned.
  </how-to-verify>
  <resume-signal>Type "approved" if macOS service runs as _headscale and hs-proc-user PASS. Type "root-issue" if Headscale runs as root despite plist. Type "uid-conflict" if UID 399 was already in use (specify chosen UID). Type "skip-macos" to defer macOS launchd to a follow-on phase (Linux-only for Phase 12).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TLS gate → init sequence | backend.ValidateHeadscaleTLS called as step 0; BEFORE any disk mutation; ACME mode asserts hostname non-empty; fail-closed on any miss; lives in internal/backend (cli→backend direction, no cycle) |
| Binary download → headscale binary on disk | HTTPS download + SHA-256 checksum; no cosign (R-01 — Headscale publishes no cosign sigs) |
| Service start → REST calls | Health-check loop (30s timeout) ensures server is running before API key bootstrap, user-ensure, or pre-auth key calls; fail closed on timeout |
| API key bootstrap → OS keychain | First-time only: shell call to local headscale binary; key stored via platform.Keychain immediately; printed once via Printer only; never in audit log, never on argv |
| API key steady-state → REST POST /api/v1/apikey | Re-init and rotation use REST only per D-08; Bearer auth with existing key |
| D-13 user-ensure → REST GET+POST /api/v1/user | REST only; Bearer auth; idempotent (409 = already exists = OK); called AFTER service running and API key obtained |
| Pre-auth key → TS_AUTHKEY env | Mint with explicit expiry; store in keychain; never on argv; consumed by tailscale up once |
| macOS dscl → _headscale OS account | Requires sudo; MEDIUM confidence (R-03); gated by checkpoint:human-verify |
| cmd_doctor.go → HeadscaleDoctorChecks() | Read-only REST probes + file stat; no mutations in doctor flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-00 | Tampering | init proceeds without valid TLS cert (D-05/D-06 / SC-2) | mitigate | backend.ValidateHeadscaleTLS() called as FIRST step in init RunE before any mutation; BYO: cert not expired, SAN matches FQDN, key perms 0600; ACME: hostname non-empty; error returned = init aborts; function in internal/backend ensures cli→backend direction only (no circular import) |
| T-12-04-01 | Tampering | Binary download — no cosign (R-01) | mitigate | SHA-256 checksum from checksums.txt fetched over TLS; expected per-version hash pinned in abysslink source (headscaleDefaultVersion constant); mismatch fails closed; residual risk: checksums.txt is unsigned; HTTPS provides transport integrity; documented known gap |
| T-12-04-02 | Information Disclosure | API key print-once | mitigate | API key routed ONLY through cli.Printer — never to slog, audit.Append, audit.WriteFile content, or argv; pattern mirrors cmd_lock.go print-once (CLAUDE.md hard rule D-10) |
| T-12-04-03 | Information Disclosure | Pre-auth key storage | mitigate | Mint with explicit Expiration (1h default); stored immediately in platform.Keychain; never printed, never logged; bcrypt-hashed on server side after v0.28.0 (irreversible) |
| T-12-04-04 | Elevation of Privilege | Binary downgrade via upgrade | mitigate | Version comparison refuses downgrade with explicit error; backup runs before swap; audit.WriteFile preserves backup |
| T-12-04-05 | Elevation of Privilege | Headscale running as root (macOS) | mitigate | launchd plist sets UserName=_headscale; hs-proc-user FAIL if root; checkpoint:human-verify gates the macOS path (R-03); ASSUMED A1/A2 risks documented |
| T-12-04-06 | Elevation of Privilege | Deny-all ACL baseline not pushed before first node | mitigate | Up() in Wave 2 calls SetACL(deny-all) synchronously before returning; init command uses the adapter's Up() method — ordering is correct by construction |
| T-12-04-07 | Elevation of Privilege | init with version below v0.23.0 floor | mitigate | headscale version output checked on temp binary before any mutation; error returned if < v0.23.0 |
| T-12-04-08 | Tampering | Gatekeeper quarantine skip on macOS binary | mitigate | xattr -d com.apple.quarantine via shell.Runner; errors ignored (attribute may not exist); not security-relevant (quarantine prevents accidental exec, not attacker exec) |
| T-12-04-09 | Spoofing | D-13 user creation: race between check and create | accept | 409 Conflict on POST /api/v1/user is treated as success (idempotent); the race window is local-machine-only init (D-01) — no concurrent attackers in this threat model |
| T-12-04-10 | Denial of Service | REST calls before server is ready | mitigate | Health-check loop (30s, 2s interval) validates server responds before any admin REST call; fail closed on timeout — init returns error rather than proceeding with REST calls against a non-running server |
| T-12-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run after all tasks complete (Task 2 requires macOS hardware for full verification):
1. `make lint test` exits 0
2. `go build ./...` exits 0 — all packages compile
3. `abysslink server --help` lists "headscale" subcommand
4. `abysslink server headscale --help` lists init, status, upgrade, backup
5. `abysslink server headscale init --dry-run` prints plan, exits 0, creates no files
6. `grep -c "hs-lock" internal/cli/cmd_doctor.go` — returns >= 1 (fix map entry)
7. `grep -c "newServerCmd" internal/cli/root.go` — returns >= 1
8. `grep -n "cosign\|verifyCosign\|checkCosign" internal/cli/cmd_server_headscale.go` — returns 0 (no cosign for Headscale, R-01)
9. `grep -c "checksums.txt\|verifyChecksum" internal/cli/cmd_server_headscale.go` — returns >= 1 (SHA-256 used instead)
10. `grep -c "verify_client_url_fail_open" internal/cli/cmd_server_headscale.go` — returns 0
11. `grep -c "backend.ValidateHeadscaleTLS" internal/cli/cmd_server_headscale.go` — returns >= 1 (TLS gate calls exported backend function, no cycle)
12. `grep -c "func ValidateHeadscaleTLS" internal/cli/cmd_server_headscale.go` — returns 0 (function NOT defined in cli — it lives in internal/backend)
13. `grep -c "/api/v1/apikey" internal/cli/cmd_server_headscale.go` — returns >= 1 (REST API key rotation present)
14. `grep -c "/api/v1/user" internal/cli/cmd_server_headscale.go` — returns >= 1 (D-13 user-ensure present)
15. `grep -c "first-time bootstrap only" internal/cli/cmd_server_headscale.go` — returns >= 1 (D-08 exception documented inline)
16. `grep -c "healthCheck\|waitReady\|doHeadscaleHealthCheck" internal/cli/cmd_server_headscale.go` — returns >= 1 (health-check before REST calls)
17. All nine hs-* check names appear in cmd_doctor.go findingFix map
</verification>

<success_criteria>
- `make lint test` is green; `go build ./...` exits 0
- abysslink server headscale init/status/upgrade/backup subcommands are registered and respond to --help
- init TLS gate calls backend.ValidateHeadscaleTLS (exported from internal/backend); no circular import between internal/cli and internal/backend
- init sequence order: TLS gate → download/verify → version floor → write binary → config merge → service install+start → health-check → API key bootstrap → D-13 user-ensure → pre-auth key; REST admin calls NEVER precede service health-check
- init refuses with error BEFORE any mutation when TLS cert is missing/expired/invalid (BYO mode) or ACMEHostname is empty (ACME mode) — per D-05/D-06 and ROADMAP SC-2
- init ensures the 'abysslink' Headscale user exists via REST GET/POST /api/v1/user before minting any pre-auth key (D-13); user-ensure is called AFTER service is running and API key is obtained
- API key: one-time shell bootstrap on first install (with required inline comment documenting D-08 exception); steady-state re-init uses REST POST /api/v1/apikey
- Health-check (30s timeout) gates all REST admin operations; init fails closed if server does not start
- init --dry-run prints a plan but mutates nothing (test verified); TLS gate still runs in dry-run
- upgrade refuses downgrade (test verified)
- Binary download uses SHA-256 checksums.txt, NOT cosign (R-01 confirmed)
- Hardened config key derp.server.verify_clients used, NOT verify_client_url_fail_open (R-02 confirmed)
- macOS launchd path gated by checkpoint:human-verify (R-03 confirmed)
- cmd_doctor.go findingFix has all nine hs-* entries including hs-lock with empty fix (permanent WARN)
- doctor pipeline calls HeadscaleDoctorChecks when backend.type==headscale
- HS-01 success criterion 1: deny-all pushed before first node admitted (via Up() in Wave 2)
- HS-02 success criterion 2: TLS gate enforced, embedded DERP configured, non-root service, pre-auth key with explicit expiry
- HS-03 success criterion 5: all nine hs-* checks wired into doctor output
</success_criteria>

<output>
Create `.planning/phases/12-headscale-backend/12-04-SUMMARY.md` when done
</output>
