---
phase: 21-optional-modules-fleet-polish
plan: 03
type: execute
wave: 3
depends_on:
  - "21-01"
  - "21-02"
files_modified:
  - internal/modules/sandbox/module.go
  - internal/modules/sandbox/sandbox_linux.go
  - internal/modules/sandbox/sandbox_stub.go
  - internal/cli/cmd_doctor_mod3.go
autonomous: false
requirements:
  - MOD3-03

user_setup:
  - service: go-landlock
    why: "go-landlock v0.8.1 is a new Go dependency (44 days old, slopcheck [ASSUMED OK]) that must be verified before installation"
    env_vars: []
    dashboard_config:
      - task: "Verify package on pkg.go.dev before go get"
        location: "https://pkg.go.dev/github.com/landlock-lsm/go-landlock/landlock"

must_haves:
  truths:
    - "sandbox module on macOS (runtime.GOOS=darwin) returns ErrNotSupported from Detect and Apply; never panics"
    - "sandbox module on Linux probes Landlock V1 kernel support via landlock.V1.RestrictPaths() without BestEffort"
    - "go-landlock import is ONLY in sandbox_linux.go (//go:build linux); macOS build never references it"
    - "sandbox-landlock-supported WARN doctor check is present in mod3DoctorFindings"
  artifacts:
    - path: "internal/modules/sandbox/module.go"
      provides: "dispatch to platform-specific implementation; ErrNotSupported on non-linux"
    - path: "internal/modules/sandbox/sandbox_linux.go"
      provides: "//go:build linux; applyLandlockProfile, isLandlockSupported using go-landlock"
    - path: "internal/modules/sandbox/sandbox_stub.go"
      provides: "//go:build !linux; ErrNotSupported var; stub functions"
    - path: "internal/cli/cmd_doctor_mod3.go"
      provides: "mod3DoctorFindings extended: sandbox-landlock-supported WARN"
  key_links:
    - from: "internal/modules/sandbox/sandbox_linux.go"
      to: "github.com/landlock-lsm/go-landlock/landlock"
      via: "//go:build linux import"
      pattern: "landlock-lsm/go-landlock"
---

<objective>
Rewrite the sandbox module with Linux-only Landlock (go-landlock v0.8.1 behind //go:build linux) and a macOS stub returning ErrNotSupported. The plan includes a blocking human checkpoint to verify the go-landlock package legitimacy before running go get. Extend cmd_doctor_mod3.go with the sandbox-landlock-supported WARN check (per D-03).

Purpose: MOD3-03 — Landlock is kernel-native Linux LSM; macOS is permanently out of scope.
Output: Rewritten sandbox module (linux/stub), go-landlock in go.mod, sandbox doctor check.
</objective>

<execution_context>
@/Users/mohandoz/u01/innovate/abysslink/workflows/execute-plan.md
@/Users/mohandoz/u01/innovate/abysslink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/PROJECT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-CONTEXT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-RESEARCH.md
@/Users/mohandoz/u01/innovate/abysslink/CLAUDE.md
@/Users/mohandoz/u01/innovate/abysslink/internal/modules/sandbox/module.go
@/Users/mohandoz/u01/innovate/abysslink/.golangci.yml
@/Users/mohandoz/u01/innovate/abysslink/go.mod
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_doctor_mod3.go
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The research identified go-landlock v0.8.1 (github.com/landlock-lsm/go-landlock) as the dependency for Linux Landlock support. slopcheck returned [OK] with a "relatively new" flag (44 days). The landlock-lsm org is the official Landlock LSM project on GitHub. One transitive dep: kernel.org/pub/linux/libs/security/libcap/psx v1.2.77 (LGPLv2+, acceptable per project depguard rules — only AGPL is banned).</what-built>
  <how-to-verify>
    1. Visit https://pkg.go.dev/github.com/landlock-lsm/go-landlock/landlock — verify the package exists, has a documented API (landlock.V1, landlock.Config, BestEffort, RestrictPaths), and is from the landlock-lsm org.
    2. Visit https://github.com/landlock-lsm/go-landlock — confirm this is the official Landlock LSM project (not a typosquat), has recent commits, and v0.8.1 is a tagged release.
    3. Check the transitive dep: https://pkg.go.dev/kernel.org/pub/linux/libs/security/libcap/psx — confirm it is a kernel.org project, LGPLv2+, and does not introduce AGPL obligations.
    4. Run `grep -r "AGPL\|agpl" go.sum` after `go get github.com/landlock-lsm/go-landlock@v0.8.1` to confirm no AGPL packages were pulled in.
  </how-to-verify>
  <resume-signal>Type "approved" once you've verified the package is legitimate and the transitive deps are acceptable. Type "rejected" to use the raw golang.org/x/sys/unix Landlock syscall fallback instead (Claude will implement that path).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite sandbox module with Linux Landlock (go-landlock) and macOS stub</name>
  <files>internal/modules/sandbox/module.go, internal/modules/sandbox/sandbox_linux.go, internal/modules/sandbox/sandbox_stub.go, internal/modules/sandbox/module_test.go, internal/cli/cmd_doctor_mod3.go, internal/cli/cmd_doctor_mod3_test.go</files>
  <read_first>
    - internal/modules/sandbox/module.go: read fully — the existing firejail/sandbox-exec logic will be replaced.
    - go.mod: read to verify golang.org/x/sys is already present as an indirect dep (fallback path); read the go directive version.
    - .golangci.yml: search for depguard configuration to confirm AGPL ban and what is permitted; verify github.com/landlock-lsm is NOT in any ban list.
    - internal/cli/cmd_doctor_mod3.go: read the current state after Plan 02 to understand the function signature and append point.
  </read_first>
  <behavior>
    - TestModule_Detect_macOS: on darwin (runtime.GOOS), Detect returns a Finding with Severity=SeverityWarning and Message containing "ErrNotSupported" or "Linux-only". The module must not call landlock or any linux-specific syscall.
    - TestModule_Apply_macOS: Apply returns ErrNotSupported on darwin.
    - TestIsLandlockSupported_Stub: on darwin (via sandbox_stub.go), isLandlockSupported() returns false.
    - TestSandboxModule_Detect_Disabled: Detect returns nil findings when cfg.Modules.Sandbox.Enabled = false.
    - TestDoctorSandboxLandlockSupported_Warn: mod3DoctorFindings returns sandbox-landlock-supported WARN when sandbox module is enabled.
  </behavior>
  <action>
    Run `go get github.com/landlock-lsm/go-landlock@v0.8.1` (only after human-verify checkpoint above is approved). If checkpoint was rejected, use golang.org/x/sys/unix Landlock syscall fallback instead (document the choice in a code comment).

    Rewrite internal/modules/sandbox/module.go. This file has NO build tag — it compiles on all platforms. The Module struct contains runner, cfg. The Detect/Apply/Plan/Verify/Repair methods dispatch to platform functions defined in the build-tagged files:
    - Detect: if not enabled return nil. Call isLandlockSupported() (build-tag-polymorphic). If returns false, emit Finding{Check: "sandbox-landlock-supported", Severity: SeverityWarning, Message: "Landlock not supported on this platform (Linux kernel >= 5.13 required) — sandbox module is unavailable"}. If true, return nil (no finding when supported).
    - Apply: call applyLandlockProfile() from the build-tagged file; return its error (which is ErrNotSupported on macOS).
    - Plan: if not enabled, return nil. Return Action{Description: "sandbox: apply Landlock process isolation (Linux kernel >= 5.13 required)"}.
    - Verify and Repair delegate to Detect.

    Create internal/modules/sandbox/sandbox_linux.go:
    - Build tag: //go:build linux (first non-blank, non-copyright line — CRITICAL for CI).
    - Import: "github.com/landlock-lsm/go-landlock/landlock" (if approved) OR use raw golang.org/x/sys/unix syscalls (if rejected at checkpoint).
    - License note comment: "// go-landlock transitive dep kernel.org/pub/linux/libs/security/libcap/psx is LGPLv2+ — acceptable per project depguard (only AGPL is banned); see NOTICE."
    - isLandlockSupported() bool: probe by calling landlock.V1.RestrictPaths() (no BestEffort, empty rules). Returns err == nil. This is a safe no-op kernel probe.
    - applyLandlockProfile(ctx context.Context) error: apply a minimal read-only Landlock profile using landlock.MustConfig(landlock.V1).BestEffort().RestrictPaths(...) with no rules (no-op profile) OR log a slog.Info that Landlock is supported and return nil (the actual profile rules are operator-configured, not managed here). BestEffort() ensures kernels < 5.13 degrade gracefully.
    - ErrNotSupported is not declared here (it lives in sandbox_stub.go on macOS).

    Create internal/modules/sandbox/sandbox_stub.go:
    - Build tag: //go:build !linux (first non-blank, non-copyright line).
    - var ErrNotSupported = errors.New("sandbox: Landlock is Linux-only (kernel >= 5.13 required)")
    - isLandlockSupported() bool { return false }
    - applyLandlockProfile(ctx context.Context) error { return ErrNotSupported }

    The function signatures MUST match between sandbox_linux.go and sandbox_stub.go so the dispatch in module.go compiles on all platforms.

    Extend internal/cli/cmd_doctor_mod3.go mod3DoctorFindings to add one new finding:
    - sandbox-landlock-supported (WARN when cfg.Modules.Sandbox.Enabled is true): call sandbox.IsLandlockSupported() (exported wrapper around the build-tag-polymorphic function). If returns false: "WARN: sandbox-landlock-supported — Landlock LSM not available on this platform (Linux kernel >= 5.13 required); sandbox module is a no-op". If returns true: "OK: sandbox-landlock-supported — Landlock LSM available" with SeverityOK. Note: this means you need to export IsLandlockSupported() from the sandbox package as a thin wrapper.

    License header in all new files. No fmt.Println. No bare os/exec.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test ./internal/modules/sandbox/... ./internal/cli/... -count=1 -run 'TestModule_Detect_macOS|TestModule_Apply_macOS|TestIsLandlock|TestSandboxModule|TestDoctorSandbox' -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>go build ./... exits 0 on macOS (no linux-tagged symbols leak); sandbox tests pass; sandbox-landlock-supported WARN in mod3DoctorFindings; make lint exits 0 for sandbox package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go get → CI supply chain | New dependency (go-landlock) must be verified before installation |
| Landlock kernel probe → process isolation | isLandlockSupported() is a read-only probe; no security-impacting syscall without user intent |
| macOS stub → ErrNotSupported | macOS build must never reference Linux syscalls; build tag enforces this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | go-landlock new dependency supply chain | mitigate | Blocking human-verify checkpoint before go get; slopcheck [OK]; pkg.go.dev verified; AGPL check via grep go.sum |
| T-21-03-02 | Tampering | Landlock build tag missing → macOS CI failure | mitigate | //go:build linux must be first non-blank non-copyright line; TestModule_Detect_macOS verifies ErrNotSupported on darwin; CI runs macOS matrix |
| T-21-03-03 | Elevation of Privilege | Landlock profile too permissive | accept | Profile is a no-op advisory this phase (actual profile rules are operator-configured); BestEffort() degrades gracefully on old kernels |
| T-21-03-04 | Tampering | LGPLv2+ transitive dep introducing copyleft obligations | mitigate | depguard bans AGPL only; LGPLv2+ permits Apache-2.0 use; license note in sandbox_linux.go; grep AGPL go.sum at checkpoint |
| T-21-03-SC | Tampering | go get install | mitigate | Human checkpoint gate is blocking; approves landlock import only after pkg.go.dev + GitHub org verification |
</threat_model>

<verification>
1. `go build ./...` exits 0 on macOS (no linux-only symbols in non-linux files).
2. `go test ./internal/modules/sandbox/...` exits 0; TestModule_Apply_macOS confirms ErrNotSupported.
3. `grep -r "landlock-lsm" internal/modules/sandbox/sandbox_stub.go` returns no matches (import only in sandbox_linux.go).
4. `grep -rn "os/exec" internal/modules/sandbox/` returns no matches.
5. `go test ./internal/cli/... -run TestDoctorSandbox` passes.
</verification>

<success_criteria>
- sandbox module on macOS (//go:build !linux) returns ErrNotSupported; never panics (TestModule_Apply_macOS passes).
- go-landlock import is ONLY in sandbox_linux.go; macOS build compiles without it.
- sandbox-landlock-supported WARN present in mod3DoctorFindings.
- go build ./... and go test ./... exit 0 after both tasks.
- make lint exits 0 for sandbox package.
</success_criteria>

<output>
Create `.planning/phases/21-optional-modules-fleet-polish/21-03-SUMMARY.md` when done.
</output>
