---
phase: 21-optional-modules-fleet-polish
plan: 05
type: execute
wave: 5
depends_on:
  - "21-04"
files_modified:
  - internal/cli/root.go
  - internal/cli/cmd_doctor.go
  - docs/headscale-ha.md
  - docs/netbird-scim.md
autonomous: true
requirements:
  - MOD3-06
  - MOD3-01
  - MOD3-02
  - MOD3-03
  - MOD3-04
  - MOD3-05

must_haves:
  truths:
    - "abysslink wol, abysslink asciinema, abysslink netbird posture, abysslink netbird events are all registered in buildRootCmd"
    - "abysslink doctor output includes mod3DoctorFindings (wol-apply-gate, upsnap-bind, upsnap-no-public, atuin-bind, atuin-key-backed-up, asciinema-rec-warning, sandbox-landlock-supported, nb-posture-active)"
    - "docs/headscale-ha.md explains Headscale has no upstream HA and documents LiteFS+Consul workaround pattern; no implementation attempted"
    - "docs/netbird-scim.md explains SCIM is NetBird commercial-only and out of scope; no implementation attempted"
    - "make lint test exits 0 with all new module code included"
    - "grep finds zero bare os/exec calls in internal/modules/upsnap, atuin, sandbox, asciinema and new cli files"
    - "grep finds zero bare os.WriteFile calls in internal/modules/atuin, asciinema and internal/cli/cmd_wol.go"
  artifacts:
    - path: "internal/cli/root.go"
      provides: "newWolCmd, newAsciinemaCmd, newNetBirdPostureCmd, newNetBirdEventsCmd registered"
      contains: "newWolCmd"
    - path: "internal/cli/cmd_doctor.go"
      provides: "mod3DoctorFindings call integrated into newDoctorCmd RunE"
      contains: "mod3DoctorFindings"
    - path: "docs/headscale-ha.md"
      provides: "Scope-cut doc: Headscale HA out of scope; LiteFS+Consul workaround"
    - path: "docs/netbird-scim.md"
      provides: "Scope-cut doc: NetBird SCIM commercial-only; out of scope"
  key_links:
    - from: "internal/cli/root.go"
      to: "internal/cli/cmd_wol.go"
      via: "newWolCmd() registered in opsCmds or advancedCmds"
      pattern: "newWolCmd"
    - from: "internal/cli/cmd_doctor.go"
      to: "internal/cli/cmd_doctor_mod3.go"
      via: "mod3DoctorFindings(ctx, cc.cfg, cc.runner) call in RunE"
      pattern: "mod3DoctorFindings"
---

<objective>
Wire all new Phase 21 CLI commands into buildRootCmd (root.go), integrate mod3DoctorFindings into the doctor command's RunE, write the two scope-cut documentation files, and run the make lint test gate confirming the full phase is green. This is the final Phase 21 plan — it closes MOD3-01 through MOD3-06 (per D-01..D-06).

Purpose: MOD3-06 (scope-cut docs) + phase-wide MOD3-01..05 integration verification via make lint test.
Output: Wired CLI, two scope-cut docs, green make lint test.
</objective>

<execution_context>
@/Users/mohandoz/u01/innovate/abysslink/workflows/execute-plan.md
@/Users/mohandoz/u01/innovate/abysslink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/PROJECT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-CONTEXT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-RESEARCH.md
@/Users/mohandoz/u01/innovate/abysslink/CLAUDE.md
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/root.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_doctor.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_doctor_mod3.go
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire new commands into root.go and integrate mod3DoctorFindings into cmd_doctor.go</name>
  <files>internal/cli/root.go, internal/cli/cmd_doctor.go</files>
  <read_first>
    - internal/cli/root.go lines 94-150: read the full buildRootCmd to understand which group to add the new commands to. wol and asciinema belong in the "ops" group (alongside notify, logs, backup). netbird posture and netbird events are subcommands of the existing netbird command — check if a newNetBirdCmd parent already exists or needs to be created.
    - internal/cli/cmd_doctor.go lines 530-565: read the RunE section that assembles findings to understand exactly where to add mod3DoctorFindings call. It should come after secDoctorFindings and before the --all-rigs fan-out section.
    - internal/cli/cmd_doctor_mod3.go: read the mod3DoctorFindings function signature to confirm it matches what cmd_doctor.go will call.
  </read_first>
  <action>
    In internal/cli/root.go buildRootCmd:

    1. Add newWolCmd() to opsCmds slice (alongside newNotifyCmd, newLogsCmd, etc.) with GroupID = "ops".
    2. Add newAsciinemaCmd() to advancedCmds slice (alongside newClaudeCodeCmd, newEnableCmd, etc.) with GroupID = "advanced".
    3. For NetBird posture and events: check if there is already a "netbird" parent command (newNetBirdCmd). If it exists, look at its subcommands and add newNetBirdPostureCmd and newNetBirdEventsCmd as sub-subcommands. If no netbird parent exists, create a minimal newNetBirdCmd parent with Use: "netbird" and Short: "NetBird backend management" and add it to opsCmds; then add posture and events as subcommands.

    In internal/cli/cmd_doctor.go RunE, after the secDoctorFindings call (line ~560) and before the --all-rigs section, add:

    findings = append(findings, mod3DoctorFindings(ctx, cc.cfg, cc.runner)...)

    This is one line of change to cmd_doctor.go. The mod3DoctorFindings function is defined in cmd_doctor_mod3.go (same package cli), so no import is needed.

    Verify: `go build ./...` exits 0 after these changes.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test ./internal/cli/... -count=1 -run 'TestDoctor|TestRoot|TestBuildRoot' -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>go build ./... exits 0; abysslink --help shows wol, asciinema, netbird commands; mod3DoctorFindings is called from newDoctorCmd RunE.</done>
</task>

<task type="auto">
  <name>Task 2: Write scope-cut docs and run make lint test gate</name>
  <files>docs/headscale-ha.md, docs/netbird-scim.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 358-366: read the Out-of-Scope section for the exact framing of why HA and SCIM are out of scope.
    - .planning/phases/21-optional-modules-fleet-polish/21-RESEARCH.md: no specific HA/SCIM research content — use the CONTEXT.md locked framing and general knowledge.
  </read_first>
  <action>
    Create docs/headscale-ha.md: a concise Markdown document (200-400 words) explaining:
    - Headscale has NO upstream HA support as of v0.x — it uses SQLite and there is no built-in clustering or replication.
    - Why this is out of scope for abysslink v3: would require managing a distributed database (PostgreSQL/MySQL) outside abysslink's single-binary ethos; not achievable without significant upstream changes.
    - Known workaround patterns documented (NOT implemented): LiteFS (Litestream for SQLite replication over S3/object storage) for single-primary HA; Consul-based leader election for failover; both require infrastructure beyond abysslink's scope.
    - Recommendation: for HA Headscale, use a managed PostgreSQL provider + community Helm chart or wait for upstream HA support.
    - Footer: "This is a scope-cut document — no HA implementation is included in abysslink."

    Create docs/netbird-scim.md: a concise Markdown document (150-300 words) explaining:
    - SCIM 2.0 provisioning in NetBird is a commercial-edition feature (NetBird Cloud and NetBird Enterprise only) — not available in the open-source community edition.
    - Why this is out of scope for abysslink v3: abysslink targets the community/self-hosted NetBird; SCIM requires the commercial license; no open API is available to implement against.
    - What SCIM would provide (informational): automated user provisioning from IdPs (Okta, Azure AD, etc.) into NetBird groups.
    - Alternative: manual user/group management via `abysslink netbird posture` and the NetBird admin UI.
    - Footer: "This is a scope-cut document — no SCIM implementation is included in abysslink."

    After writing both docs, run the full phase gate:
    1. `grep -r "os/exec" internal/modules/upsnap/ internal/modules/atuin/ internal/modules/sandbox/ internal/modules/asciinema/ internal/cli/cmd_wol.go internal/cli/cmd_asciinema.go internal/cli/cmd_netbird_posture.go internal/cli/cmd_netbird_events.go` — must return NO matches.
    2. `grep -v '^//' internal/modules/atuin/module.go internal/modules/asciinema/module.go internal/cli/cmd_wol.go | grep 'os\.WriteFile'` — must return NO matches.
    3. `make lint test` — must exit 0.

    If make lint test fails, investigate and fix. Common failure modes:
    - golangci-lint errcheck: a deferred body.Close() lacking the nolint comment → add //nolint:errcheck // errcheck: response body close error is non-actionable; best-effort cleanup
    - golangci-lint unused: a function exported from sandbox_stub.go not used on the darwin build → check that module.go always calls the platform-polymorphic function.
    - golangci-lint staticcheck: a field in a struct that is never read → check all new struct fields are used.
    - depguard: go-landlock import outside sandbox_linux.go → ensure import only in build-tagged file.
  </action>
  <verify>
    <automated>ls docs/headscale-ha.md docs/netbird-scim.md &amp;&amp; grep -c "out of scope" docs/headscale-ha.md docs/netbird-scim.md &amp;&amp; make lint test 2>&amp;1 | tail -20</automated>
  </verify>
  <done>docs/headscale-ha.md and docs/netbird-scim.md exist and contain "out of scope" framing; make lint test exits 0; grep finds zero bare os/exec in new module code; grep finds zero bare os.WriteFile in mutation paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/headscale-ha.md | Doc-only; no code; no security surface |
| docs/netbird-scim.md | Doc-only; no code; no security surface |
| root.go wiring | Adds new commands to the Cobra tree; no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Tampering | mod3DoctorFindings not wired into doctor | mitigate | cmd_doctor.go integration + TestDoctor integration test verifies the call is present; wol-apply-gate FATAL visible in doctor output |
| T-21-05-02 | Information Disclosure | scope-cut docs revealing internal architecture | accept | Docs describe upstream upstream limitations only; no abysslink credentials, keys, or config in docs |
| T-21-05-03 | Tampering | make lint failures masking security issues | mitigate | make lint test is the phase gate; plan requires it to exit 0 before SUMMARY is written; any lint suppression requires explicit justification |
| T-21-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
1. `abysslink --help` output includes wol, asciinema, and netbird subcommands.
2. `abysslink doctor --dry-run` output includes wol-apply-gate FATAL (when upsnap is enabled), confirming mod3DoctorFindings is wired.
3. `ls docs/headscale-ha.md docs/netbird-scim.md` — both files exist.
4. `grep -r "os/exec" internal/modules/upsnap/ internal/modules/atuin/ internal/modules/sandbox/ internal/modules/asciinema/ internal/cli/cmd_wol.go internal/cli/cmd_asciinema.go` returns no matches (MOD3-06 grep gate).
5. `grep -v '^//' internal/modules/atuin/module.go internal/cli/cmd_wol.go | grep 'os\.WriteFile'` returns no matches (MOD3-06 mutation gate).
6. `make lint test` exits 0.
</verification>

<success_criteria>
- abysslink wol, asciinema, netbird posture, netbird events are all reachable from the binary.
- abysslink doctor includes wol-apply-gate FATAL, atuin-bind FATAL, asciinema-rec-warning FATAL, sandbox-landlock-supported WARN, nb-posture-active WARN (when relevant modules are enabled).
- docs/headscale-ha.md and docs/netbird-scim.md exist and explain why the features are out of scope.
- make lint test exits 0.
- Zero bare os/exec in all four new module implementations (grep-verifiable).
- Zero bare os.WriteFile in mutation paths in new module code (grep-verifiable).
</success_criteria>

<output>
Create `.planning/phases/21-optional-modules-fleet-polish/21-05-SUMMARY.md` when done.
</output>
