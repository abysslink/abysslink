---
phase: 21-optional-modules-fleet-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "21-01"
files_modified:
  - internal/modules/atuin/module.go
  - internal/modules/asciinema/module.go
  - internal/cli/cmd_asciinema.go
  - internal/cli/cmd_doctor_mod3.go
autonomous: true
requirements:
  - MOD3-02
  - MOD3-04

must_haves:
  truths:
    - "atuin Apply writes ~/.config/atuin/config.toml via audit.WriteFile (not bare os.WriteFile)"
    - "atuin Apply appends eval $(atuin init zsh/bash) to shell rc via audit.WriteFile; idempotent (no duplicate lines)"
    - "atuin-bind FATAL doctor check is present; atuin-key-backed-up WARN doctor check is present"
    - "asciinema rec command rejects non-interactive contexts (non-TTY returns error without showing prompt)"
    - "asciinema rec command shows NON-SUPPRESSIBLE credential warning via Deps.Prompt before any recording"
    - "asciinema-rec-warning FATAL doctor check verifies: binary present + no bypass env-var path"
  artifacts:
    - path: "internal/modules/atuin/module.go"
      provides: "atuin Apply: shell-rc append via audit.WriteFile; idempotent strings.Contains guard"
      contains: "atuinKeyPath"
    - path: "internal/modules/asciinema/module.go"
      provides: "asciinema Apply with Deps.Prompt credential warning gate; Prompt field wired"
    - path: "internal/cli/cmd_asciinema.go"
      provides: "newAsciinemaCmd with rec subcommand; interactive gate returns error in non-TTY"
      exports: ["newAsciinemaCmd"]
    - path: "internal/cli/cmd_doctor_mod3.go"
      provides: "mod3DoctorFindings extended: atuin-bind FATAL, atuin-key-backed-up WARN, asciinema-rec-warning FATAL"
  key_links:
    - from: "internal/modules/atuin/module.go"
      to: "internal/audit"
      via: "m.audit.WriteFile for shell-rc append"
      pattern: "audit\\.WriteFile"
    - from: "internal/cli/cmd_asciinema.go"
      to: "internal/cli/term.go"
      via: "interactive() check before Deps.Prompt"
      pattern: "interactive\\("
---

<objective>
Extend the atuin module with real shell-rc integration (append eval shell-init line via audit.WriteFile, idempotent) and the atuin-key-backed-up/atuin-bind doctor checks. Create the asciinema CLI rec subcommand with its HARD FLOOR non-suppressible credential warning (interactive-only; rejects non-TTY contexts) and the asciinema-rec-warning FATAL doctor check. Extend cmd_doctor_mod3.go with four new findings (per D-02, D-04).

Purpose: MOD3-02 (atuin full automation) + MOD3-04 (asciinema credential warning hard floor).
Output: Extended atuin module, new cmd_asciinema.go, cmd_doctor_mod3.go extended with 4 new checks.
</objective>

<execution_context>
@/Users/mohandoz/u01/innovate/abysslink/workflows/execute-plan.md
@/Users/mohandoz/u01/innovate/abysslink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/abysslink/.planning/PROJECT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-CONTEXT.md
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-RESEARCH.md
@/Users/mohandoz/u01/innovate/abysslink/CLAUDE.md
@/Users/mohandoz/u01/innovate/abysslink/internal/modules/atuin/module.go
@/Users/mohandoz/u01/innovate/abysslink/internal/modules/asciinema/module.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/context.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/term.go
@/Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_doctor_mod3.go
@/Users/mohandoz/u01/innovate/abysslink/.planning/phases/21-optional-modules-fleet-polish/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend atuin module with shell-rc append via audit.WriteFile</name>
  <files>internal/modules/atuin/module.go, internal/modules/atuin/module_test.go</files>
  <read_first>
    - internal/modules/atuin/module.go: read the full current implementation; note that Apply currently calls slog.Info telling users to manually add shell-init. Replace that slog.Info with the actual write.
    - internal/audit/interface.go and write.go: confirm WriteFile(path, content, perm, dryRun) signature.
    - internal/modules/deps.go: confirm audit.AuditWriter is the Audit field type.
  </read_first>
  <behavior>
    - TestApply_WritesShellRC_Zsh: when SHELL=.../zsh, Apply writes the line `eval "$(atuin init zsh)"` to ~/.zshrc via a mock AuditWriter; the written content contains the init line.
    - TestApply_WritesShellRC_Bash: when SHELL=.../bash, Apply writes `eval "$(atuin init bash)"` to ~/.bashrc.
    - TestApply_Idempotent: when the rc file already contains the init line (strings.Contains), Apply does NOT call AuditWriter.WriteFile a second time.
    - TestApply_WritesConfig: Apply writes ~/.config/atuin/config.toml via audit.WriteFile when it does not already exist.
    - TestAtuinKeyPath_Linux: atuinKeyPath() under GOOS=linux returns ~/.local/share/atuin/key (or $XDG_DATA_HOME/atuin/key when XDG_DATA_HOME is set).
  </behavior>
  <action>
    Extend internal/modules/atuin/module.go Apply function. After writing config.toml (existing behavior), add shell-rc integration:

    1. Detect shell via os.Getenv("SHELL"): if ends with "zsh" → use eval "$(atuin init zsh)" and ~/.zshrc; else → eval "$(atuin init bash)" and ~/.bashrc. Resolve rcPath via os.UserHomeDir().
    2. Read existing rc file content with os.ReadFile (non-fatal if missing; treat as empty).
    3. If strings.Contains(string(rcData), initLine) is true → skip (idempotent guard).
    4. Otherwise append "\n" + initLine + "\n" to rcData and call m.audit.WriteFile(rcPath, newData, 0o600, false).
    5. Log slog.Info("atuin apply: appended shell integration", "rc", rcPath) on success.

    Also add atuinKeyPath() string function: returns $XDG_DATA_HOME/atuin/key when set, else ~/Library/Application Support/atuin/key on darwin (runtime.GOOS), else ~/.local/share/atuin/key on linux. Export as package-level function for use in doctor checks.

    Keep all existing config.toml write logic. No bare os.WriteFile for any mutation path. License header. No fmt.Println.
  </action>
  <verify>
    <automated>go test ./internal/modules/atuin/... -count=1 -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>go test ./internal/modules/atuin/... exits 0; TestApply_WritesShellRC passes; TestApply_Idempotent confirms no double-write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cmd_asciinema.go (rec subcommand with non-suppressible credential warning) and extend cmd_doctor_mod3.go</name>
  <files>internal/cli/cmd_asciinema.go, internal/cli/cmd_asciinema_test.go, internal/cli/cmd_doctor_mod3.go, internal/cli/cmd_doctor_mod3_test.go, internal/modules/asciinema/module.go</files>
  <read_first>
    - internal/cli/context.go lines 195-205: exact Prompt wiring — Prompt returns errMissingInput when not interactive; Prompt routes through tui.Pause when interactive.
    - internal/cli/term.go lines 75-90: interactive() function — returns false when yes=true OR jsonOut=true OR !stdinIsTTY().
    - internal/cli/cmd_doctor_mod3.go (from Plan 01): read the mod3DoctorFindings signature and the three existing wol findings to understand how to extend.
    - internal/modules/asciinema/module.go: read the existing stub; note Apply currently does not show a credential warning.
  </read_first>
  <behavior>
    - TestAsciinemaRec_RequiresInteractiveTTY: calling newAsciinemaRecCmd with yes=false and stdinIsTTY()=false (non-TTY stdin) returns an error containing "terminal required" without invoking asciinema binary.
    - TestAsciinemaRec_RejectsYesFlag: calling with --yes flag set also returns the terminal-required error (--yes is non-interactive, so warning cannot be shown).
    - TestAsciinemaRec_PromptCalledBeforeExec: in a mock-interactive context (stdinIsTTY stubbed to true, yes=false), Deps.Prompt is called before shell.Runner.RunInteractive("asciinema", "rec", ...).
    - TestDoctorAsciinemaRecWarning_Fatal: mod3DoctorFindings returns asciinema-rec-warning FATAL when asciinema module is enabled.
    - TestDoctorAtuinBind_Fatal: mod3DoctorFindings returns atuin-bind FATAL when atuin module is enabled.
    - TestDoctorAtuinKeyBackedUp_Warn: mod3DoctorFindings returns atuin-key-backed-up WARN when atuin module is enabled (checks os.Stat on atuinKeyPath(); both "key absent" and "key present but warn to backup" are valid WARN states).
  </behavior>
  <action>
    Create internal/cli/cmd_asciinema.go with newAsciinemaCmd() *cobra.Command. The root asciinema command has one subcommand: rec.

    The rec subcommand (newAsciinemaRecCmd):
    1. HARD FLOOR non-interactive check: call interactive(cc.yes, cc.jsonOut). If false → return fmt.Errorf("asciinema rec: terminal required — credential warning cannot be shown in non-interactive mode; asciinema rec must be run in a live TTY"). Return this error BEFORE any Prompt call or runner call.
    2. Show non-suppressible credential warning: define const credWarning (multi-line string explaining that terminal recordings may capture credentials, close sensitive sessions, terminal scrollback is a credential store). Call deps.Prompt(ctx, credWarning) — if Prompt returns error, propagate it.
    3. After user confirms: call cc.runner.RunInteractive(ctx, "asciinema", "rec", args...) where args are any positional arguments passed to the rec subcommand (e.g., output filename). Return RunInteractive's error.

    The rec subcommand must use cc.runner.RunInteractive (not cc.runner.Run) because asciinema rec needs a TTY. No bare os/exec. The args are passed as discrete argv elements, not via sh -c (CLAUDE.md hard rule).

    Also update internal/modules/asciinema/module.go Apply to accept deps.Prompt and call it before running asciinema (consistent with the CLI command); add a Prompt func(ctx context.Context, msg string) error field to the Module struct, wired from Deps.Prompt in New(d modules.Deps).

    Extend internal/cli/cmd_doctor_mod3.go's mod3DoctorFindings to add four new findings after the existing three wol findings:

    - atuin-bind (FATAL when cfg.Modules.Atuin.Enabled is true): "FATAL: atuin-bind — atuin sync_address must not point to a public cloud endpoint; local-only mode enforced by abysslink config (sync_address = \"\")"
    - atuin-key-backed-up (WARN when cfg.Modules.Atuin.Enabled is true): check os.Stat(atuinKeyPath()) — if file exists: "WARN: atuin-key-backed-up — atuin sync key found at <path>; back it up with `atuin key` and store in your password manager"; if file absent: "WARN: atuin-key-backed-up — atuin sync key not found at <path>; run `atuin key` after setup to get your backup key"
    - asciinema-rec-warning (FATAL when cfg.Modules.Asciinema.Enabled is true): "FATAL: asciinema-rec-warning — structural invariant: `abysslink asciinema rec` requires an interactive TTY and shows a non-suppressible credential warning before any recording; no bypass env-var or flag exists; verified by TestAsciinemaRec_RequiresInteractiveTTY"

    Note: atuin-key-backed-up uses os.Stat (not runner) — it reads the key file path to check existence only, never reads content.

    The mod3DoctorFindings function after this plan handles 7 findings (3 wol + 4 here). Subsequent plans add more.

    License header in every new file. No fmt.Println. context.Context as first param.
  </action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/modules/atuin/... ./internal/modules/asciinema/... -count=1 -run 'TestAsciinema|TestDoctorAtuin|TestDoctorAsciinema' -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>TestAsciinemaRec_RequiresInteractiveTTY and TestAsciinemaRec_RejectsYesFlag pass; asciinema-rec-warning FATAL in mod3DoctorFindings; atuin-bind FATAL and atuin-key-backed-up WARN present; go test ./... exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asciinema rec → terminal scrollback | Recording captures all visible terminal output including potential credentials |
| atuin → shell rc | Shell rc mutation is audited; content comes from a hardcoded constant, not user input |
| atuin sync_address | If set to api.atuin.sh, history leaks to public cloud; atuin-bind FATAL enforces local-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | asciinema terminal scrollback credential capture | mitigate | HARD FLOOR: non-suppressible credential warning via Deps.Prompt; rec command rejects non-interactive contexts; enforced by TestAsciinemaRec_RequiresInteractiveTTY |
| T-21-02-02 | Information Disclosure | atuin history sync to public cloud | mitigate | sync_address = "" in managed config; atuin-bind FATAL doctor check; config cloud-sync check in Detect() |
| T-21-02-03 | Tampering | shell rc append without audit | mitigate | All shell rc writes go through audit.WriteFile; strings.Contains idempotency guard prevents double-write |
| T-21-02-04 | Tampering | asciinema rec called from non-interactive script | mitigate | interactive() check returns error before Prompt is called; no bypass env-var path exists |
| T-21-02-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in this plan |
</threat_model>

<verification>
1. `go test ./internal/modules/atuin/... ./internal/modules/asciinema/... ./internal/cli/...` exits 0.
2. `grep -r "os\.WriteFile" internal/modules/atuin/ internal/modules/asciinema/` returns no matches.
3. `grep -r "os/exec" internal/cli/cmd_asciinema.go` returns no matches.
4. `grep -r "fmt\.Println" internal/modules/atuin/ internal/modules/asciinema/ internal/cli/cmd_asciinema.go` returns no matches.
5. TestAsciinemaRec_RequiresInteractiveTTY and TestAsciinemaRec_RejectsYesFlag both pass.
</verification>

<success_criteria>
- atuin Apply appends eval shell-init to shell rc via audit.WriteFile; idempotent (no duplicate on second run).
- asciinema rec returns "terminal required" error in non-TTY context (TestAsciinemaRec_RequiresInteractiveTTY passes).
- asciinema rec calls Deps.Prompt before RunInteractive in interactive context.
- mod3DoctorFindings includes 7 findings: wol-apply-gate FATAL, upsnap-bind WARN, upsnap-no-public WARN, atuin-bind FATAL, atuin-key-backed-up WARN, asciinema-rec-warning FATAL.
- go test ./internal/modules/atuin/... ./internal/modules/asciinema/... ./internal/cli/... exits 0.
</success_criteria>

<output>
Create `.planning/phases/21-optional-modules-fleet-polish/21-02-SUMMARY.md` when done.
</output>
