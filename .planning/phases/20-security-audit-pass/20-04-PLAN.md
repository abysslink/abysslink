---
phase: 20-security-audit-pass
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security/threat-model.md
  - internal/cli/cmd_threat_model.go
  - internal/cli/cmd_threat_model_test.go
autonomous: true
requirements:
  - SEC-03
must_haves:
  truths:
    - "docs/security/threat-model.md is refreshed with per-backend sections (Tailscale, Headscale, NetBird), fleet surface, and v3 surfaces (metrics endpoint, Web UI, audit chain)"
    - "`abysslink threat-model --backend=tailscale` renders Tailscale-specific threat rows in addition to the base rows"
    - "`abysslink threat-model --backend=headscale` renders Headscale-specific threat rows (hs-tls, hs-api-auth, hs-lock WARN, hs-oidc-filter)"
    - "`abysslink threat-model --backend=netbird` renders NetBird-specific threat rows (nb-tls, nb-version, nb-zitadel, nb-lock WARN)"
    - "`abysslink threat-model` (no --backend) renders base rows + v3 surface rows"
    - "v3 surface rows (metrics-bind, webui-bind, audit-anchor-age) always rendered for all backends"
    - "TestThreatModelBackend table-tests pass for all four cases (no backend, tailscale, headscale, netbird)"
  artifacts:
    - path: "docs/security/threat-model.md"
      provides: "Refreshed threat model with per-backend + fleet + v3 surface sections"
      contains: "Backend: Tailscale"
    - path: "internal/cli/cmd_threat_model.go"
      provides: "--backend flag + backendRows map + v3SurfaceRows"
      contains: "backendRows"
    - path: "internal/cli/cmd_threat_model_test.go"
      provides: "tests for --backend rendering"
      contains: "TestThreatModelBackend"
  key_links:
    - from: "internal/cli/cmd_threat_model.go"
      to: "docs/security/threat-model.md"
      via: "CLI renders sections; doc is the authoritative source"
      pattern: "--backend"
    - from: "internal/cli/cmd_threat_model.go"
      to: "internal/modules/module.go"
      via: "live doctor pass reads modules.Finding.Check to set ✓/✗ for each row"
      pattern: "failChecks"
---

<objective>
Refresh the threat-model document (docs/security/threat-model.md) with per-backend sections (Tailscale, Headscale, NetBird), fleet threat surface, and every v3 surface (metrics endpoint, Web UI, audit chain). Extend `cmd_threat_model.go` with a `--backend=<name>` flag that renders the backend-specific threat rows in addition to the base rows and v3 surface rows (SEC-03).

Purpose: The threat model predates v2/v3 — it has no Headscale/NetBird sections and no metrics/webui/audit-chain rows. SEC-03 closes this gap.
Output: Refreshed docs/security/threat-model.md, extended cmd_threat_model.go, cmd_threat_model_test.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-security-audit-pass/20-CONTEXT.md
@.planning/phases/20-security-audit-pass/20-RESEARCH.md

@docs/security/threat-model.md
@internal/cli/cmd_threat_model.go
@internal/cli/cmd_doctor.go
@internal/modules/module.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refresh docs/security/threat-model.md with per-backend + fleet + v3 surfaces</name>
  <files>docs/security/threat-model.md</files>
  <read_first>
    Read the current docs/security/threat-model.md. The file has:
    - Assets table (5 assets — extend with v3 assets: metrics endpoint, Web UI dashboard, audit signing key)
    - Threats and mitigations table (10 rows — preserve all existing rows)
    - Out of scope (v1) section — move Headscale/NetBird from "out of scope" to their own backend sections
    - Security defaults section
    - Reporting section

    The RESEARCH.md §Threat Model Extension specifies the target doc structure and content for all backend/fleet/v3 rows. Use that as the authoritative content guide.
  </read_first>
  <action>
    Rewrite docs/security/threat-model.md with the following structure. Preserve the Apache-2.0 SPDX comment if any, and all existing row content. Expand the document:

    STRUCTURE:

    # Abysslink Threat Model

    ## Overview

    Brief: same as existing opening paragraph. Add one sentence: "As of v3.0.0, the threat surface has expanded to include per-backend trust models (Tailscale, Headscale, NetBird), a multi-rig fleet, and three new in-process surfaces (metrics endpoint, Web UI dashboard, tamper-evident audit chain)."

    ## Assets

    Expand the existing assets table with v3 assets:
    - Metrics endpoint — Prometheus-format exposition; must be tailnet-scoped
    - Web UI dashboard — in-process HTTPS server; must be tailnet-scoped + WhoIs-authenticated
    - Audit signing key — HMAC key in OS keychain; controls audit chain integrity
    - Per-rig enrolled keys — fleet enrolment tokens in keychain

    ## Threats and Mitigations (Base)

    Keep all existing 10 rows unchanged. Add three v3 surface rows:
    | Threat | Mitigation |
    | Metrics endpoint exposed to internet | `doctor` FATAL on 0.0.0.0/:: bind_addr (sec-metrics-bind); hard floor in config.Validate |
    | Web UI exposed to internet or unauthenticated | `doctor` FATAL on 0.0.0.0/:: bind_addr (sec-webui-bind); WhoIs auth gate in handler; read_only:false rejected at schema level |
    | Audit log tampered or truncated | HMAC-signed entries; anchor-age WARN after 24h; chain integrity checked by `abysslink audit verify` |

    ## Backend: Tailscale

    ### Trust Assumptions
    - Control plane: Tailscale SaaS (trusted by default); mitigated by Tailnet Lock
    - Tailnet Lock: required by default; disablement secrets printed once, never stored
    - ACL policies: enforced by Tailscale control plane; `abysslink acl apply` pushes via admin API

    ### Threat Rows (Tailscale-specific)
    | Threat | Check | Severity |
    |--------|-------|----------|
    | Tailnet Lock disabled | lock_enabled | FATAL |
    | ACL policy drift | acl_drift | WARN |
    | Tailscale Funnel accidentally enabled | sec-funnel-schema | FATAL |
    | New device joins without signing | lock_enabled | FATAL |

    ## Backend: Headscale

    ### Trust Assumptions
    - Self-hosted: the operator controls the control plane; standard TLS + OIDC is the auth boundary
    - Tailnet Lock: absent on Headscale — permanent doctor WARN (hs-lock), not FATAL (per v2 decision)
    - OIDC filter: misconfigured OIDC policy allows unintended users to join the tailnet

    ### Threat Rows (Headscale-specific)
    | Threat | Check | Severity |
    |--------|-------|----------|
    | Headscale API served without TLS | hs-tls | FATAL |
    | Headscale API key unauthenticated | hs-api-auth | FATAL |
    | No Tailnet Lock equivalent on Headscale | hs-lock | WARN (permanent) |
    | OIDC allow-list misconfigured | hs-oidc-filter | WARN |

    ## Backend: NetBird

    ### Trust Assumptions
    - Self-hosted: ZITADEL is the IdP; NetBird management API requires a valid management token
    - No Tailnet Lock equivalent — permanent doctor WARN (nb-lock)
    - Version gate: nb-version check enforces minimum compatible server version

    ### Threat Rows (NetBird-specific)
    | Threat | Check | Severity |
    |--------|-------|----------|
    | NetBird management API without TLS | nb-tls | FATAL |
    | NetBird server version below minimum | nb-version | WARN |
    | ZITADEL admin token misconfigured | nb-zitadel | WARN |
    | No Tailnet Lock equivalent on NetBird | nb-lock | WARN (permanent) |

    ## Fleet Threat Surface

    | Threat | Mitigation |
    |--------|-----------|
    | Cross-rig ACL drift (one rig not in the same policy) | fleet doctor fan-out; per-rig acl_drift check |
    | Rig key uniqueness (same SSH key across rigs) | enroll-rig generates per-rig key; doctor warns on shared keys |
    | Fleet command lateral movement | fleet fan-out requires --apply; doctor --all-rigs is read-only |
    | Stale rig token in keychain | keychain rotation check; periodic rig re-enrolment |

    ## v3 New Surfaces

    | Surface | Check | Severity |
    |---------|-------|----------|
    | Metrics endpoint bind floor | sec-metrics-bind | FATAL |
    | Web UI bind floor | sec-webui-bind | FATAL |
    | Web UI read-only gate | webui-read-only | FATAL |
    | Audit chain integrity | sec-audit-anchor-age | WARN (> 24h) |
    | Audit log permissions | sec-audit-log-perms | FATAL (mode ≠ 0600) |
    | Config file world-readable | sec-no-world-readable-config | FATAL |

    ## Security Defaults You Must Not Weaken

    Preserve the existing section verbatim.

    ## Reporting Security Issues

    Preserve the existing section verbatim.

    Format: GitHub-flavoured Markdown tables. Keep the document focused on the threat model, not operational procedures.
  </action>
  <verify>
    <automated>grep -c "Backend: Tailscale\|Backend: Headscale\|Backend: NetBird\|Fleet Threat\|v3 New Surfaces" /Users/mohandoz/u01/innovate/abysslink/docs/security/threat-model.md</automated>
  </verify>
  <done>
    docs/security/threat-model.md contains all 5 new sections (Tailscale, Headscale, NetBird, Fleet, v3 Surfaces); existing rows preserved; v3 assets added to Assets table.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend cmd_threat_model.go with --backend flag + v3SurfaceRows + tests</name>
  <files>internal/cli/cmd_threat_model.go, internal/cli/cmd_threat_model_test.go</files>
  <behavior>
    - TestThreatModelBackend/no_backend: output contains all 12 base rows + 3 v3 surface rows; no backend-specific rows
    - TestThreatModelBackend/tailscale: output contains base + v3 rows + tailscale-specific rows (lock_enabled, acl_drift, sec-funnel-schema)
    - TestThreatModelBackend/headscale: output contains base + v3 rows + headscale-specific rows (hs-tls, hs-api-auth, hs-lock, hs-oidc-filter)
    - TestThreatModelBackend/netbird: output contains base + v3 rows + netbird-specific rows (nb-tls, nb-version, nb-zitadel, nb-lock)
    - TestThreatModelBackend/invalid_backend: --backend=foobar → error or ignored gracefully (no panic)
  </behavior>
  <action>
    Read cmd_threat_model.go fully before editing. The current structure:
    - `threatRow` struct with `desc string` and `failChecks []string`
    - `var threatRows = []threatRow{...}` with 12 existing base rows
    - `newThreatModelCmd()` returning a cobra.Command whose RunE builds a `present map[string]bool` from a doctor pass then renders each row

    CHANGES TO MAKE:

    1. Add v3SurfaceRows (always rendered, appended after base rows):
    ```
    var v3SurfaceRows = []threatRow{
        {"Metrics endpoint bound to tailnet IP only (sec-metrics-bind)", []string{"sec-metrics-bind", "metrics-bind-tailnet"}},
        {"Web UI bound to tailnet IP only + WhoIs-authenticated (sec-webui-bind)", []string{"sec-webui-bind", "webui-bind"}},
        {"Audit chain integrity verified < 24h (sec-audit-anchor-age)", []string{"sec-audit-anchor-age", "audit-anchor-age"}},
    }
    ```
    Note: failChecks includes BOTH the sec-* alias ID and the original check ID, so the row shows ✗ if either fires.

    2. Add backendRows map:
    ```
    var backendRows = map[string][]threatRow{
        "tailscale": {
            {"Tailnet Lock enabled", []string{"lock_enabled"}},
            {"ACL policy not drifted", []string{"acl_drift"}},
            {"Tailscale Funnel rejected at schema level (sec-funnel-schema)", []string{"sec-funnel-schema", "funnel"}},
        },
        "headscale": {
            {"Headscale API served over TLS (hs-tls)", []string{"hs-tls"}},
            {"Headscale API authenticated (hs-api-auth)", []string{"hs-api-auth"}},
            {"No Tailnet Lock on Headscale — permanent advisory (hs-lock)", []string{"hs-lock"}},
            {"Headscale OIDC allow-list configured (hs-oidc-filter)", []string{"hs-oidc-filter"}},
        },
        "netbird": {
            {"NetBird management API over TLS (nb-tls)", []string{"nb-tls"}},
            {"NetBird server meets minimum version (nb-version)", []string{"nb-version"}},
            {"ZITADEL admin token configured (nb-zitadel)", []string{"nb-zitadel"}},
            {"No Tailnet Lock on NetBird — permanent advisory (nb-lock)", []string{"nb-lock"}},
        },
    }
    ```

    3. Add `--backend` flag to newThreatModelCmd:
    ```go
    cmd.Flags().String("backend", "", "render backend-specific threat rows (tailscale, headscale, netbird)")
    ```

    4. In RunE, after the doctor pass and present map:
    - Read `backend, _ := cmd.Flags().GetString("backend")`
    - Build `rows := append(threatRows, v3SurfaceRows...)` (always include both base + v3)
    - If `backend != ""`, append `backendRows[backend]` (if backend is unknown, emit a warning via printerInfo and skip — no panic, no error return)
    - Render rows using the existing ✓/✗ loop

    5. Add a section header before rendering to distinguish sections visually:
    - Before base rows: `printerInfo(p, styleBold.Render("  Base threat model"))`
    - Before v3 rows: `printerInfo(p, "")`+`printerInfo(p, styleBold.Render("  v3 surfaces"))`
    - Before backend rows: `printerInfo(p, "")`+`printerInfo(p, styleBold.Render(fmt.Sprintf("  Backend: %s", backend)))`

    Do NOT refactor the existing threatRows slice — preserve its 12 entries exactly. Only add the new vars and extend the RunE.

    Create `internal/cli/cmd_threat_model_test.go` with Apache-2.0 header.
    Test approach: use a test cobra.Command that captures Printer output via a buffer (follow the pattern of other cmd_*_test.go files that capture output). The doctor pass in RunE uses `buildDeps(ctx, cc)` which may fail in tests — the code already has a best-effort pattern (`if deps, derr := buildDeps(ctx, cc); derr == nil { ... }`). Tests can provide a minimal cmdContext that returns a deps build error; the threat rows will render with all ✓ (no failing checks, since `present` is empty). The test then checks for the presence of row description strings in the output.

    All new //nolint: directives must carry justification comments.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/cli/... -run "TestThreatModelBackend" -v 2>&1 | tail -20</automated>
    <automated>go build ./... 2>&1</automated>
    <automated>grep -n "backendRows\|v3SurfaceRows\|--backend" /Users/mohandoz/u01/innovate/abysslink/internal/cli/cmd_threat_model.go</automated>
  </verify>
  <done>
    - TestThreatModelBackend table-tests pass for all 4 cases
    - cmd_threat_model.go contains backendRows map and v3SurfaceRows var and --backend flag
    - go build exits 0
    - `abysslink threat-model --backend=headscale` renders headscale-specific rows in addition to base + v3 rows
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --backend flag input → backendRows lookup | User-supplied backend name is used as a map key; unknown keys silently return nil (no panic) |
| docs/threat-model.md → cmd_threat_model.go | Doc is authoritative; CLI renders a subset; they can drift over time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Spoofing | --backend=foobar rendering empty rows | accept | Unknown backend emits a printerInfo warning "unknown backend: X" and renders base + v3 rows only; no error return; no panic |
| T-20-04-02 | Information Disclosure | threat model doc reveals internal check IDs | accept | Check IDs are already in doctor output; the threat model makes them explicit for transparency; no new attack surface |
| T-20-04-03 | Tampering | threat model rows not updated when new checks added | mitigate | v3SurfaceRows includes BOTH sec-* alias AND original check ID in failChecks so rows correctly reflect posture even if alias naming evolves |
| T-20-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "Backend: Tailscale\|Backend: Headscale\|Backend: NetBird\|Fleet Threat\|v3 New Surfaces" docs/security/threat-model.md` returns 5
- `go test -race -count=1 ./internal/cli/... -run TestThreatModelBackend` exits 0
- `go build ./...` exits 0
- Running `abysslink threat-model 2>&1 | grep "v3 surfaces"` shows the v3 surface section header
- Running `abysslink threat-model --backend=headscale 2>&1 | grep "Backend: headscale"` shows the backend section
</verification>

<success_criteria>
1. docs/security/threat-model.md covers Tailscale, Headscale, NetBird, fleet, and v3 surfaces (5 new sections)
2. `abysslink threat-model --backend=<name>` renders the backend-specific section for all 3 valid backend names
3. TestThreatModelBackend passes for no-backend, tailscale, headscale, netbird cases
4. go build exits 0; make lint exits 0 (no new nolint violations)
</success_criteria>

<output>
Create `.planning/phases/20-security-audit-pass/20-04-SUMMARY.md` when done.
</output>
