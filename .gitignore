# Go
/abysslink
/abysslinkd
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/vendor/
/dist/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Build
goreleaser/
site/
_site/

# Local planning (not committed)
.planning/

# Generated code-review / audit artifacts — kept on disk, never tracked.
reviews/

# Local-only agent instructions + scratch backgrounds — kept on disk, never
# tracked in the public repo (purged from history 2026-06-14).
CLAUDE.md
.specter*

# Local-only agent / editor / terminal state and scratch dirs — never tracked.
.claude/
.mine/
.ghostty*

# Local-only Codex agent instructions (mirror of the gitignored CLAUDE.md) and
# its hooks — kept local for the same reason CLAUDE.md is: never tracked publicly.
AGENTS.md
.codex/

# Coverage
coverage.out
coverage.html

# nix build output
/result

# locally built binaries
/abysslink-conformance
/abysslink
/abysslinkd

# never commit TLS keys/certs (e.g. stray `tailscale cert` output)
*.key
*.crt
*.pem
*.ts.net.crt
*.ts.net.key
