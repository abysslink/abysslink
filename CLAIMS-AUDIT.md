# Abysslink Claims Audit

Every verifiable security claim in README.md and docs/ maps to a code file or doctor-check pointer here. Run `bash scripts/claims-check.sh` to validate all pointers resolve.

---

## Claims Register

| # | Claim (exact quote) | Source | Code Pointer | Status |
|---|---|---|---|---|
| C-01 | "ntfy binds exclusively to the tailnet IP (never `0.0.0.0`)" | README §Notifications | `internal/modules/ntfy/module.go` + `doctor:ntfy-bind` | ✓ |
| C-02 | "`abysslink doctor` exits `2` (fatal) if FileVault (macOS) / LUKS (Linux) is off" | README §Security hardening | `internal/modules/hardening/module.go` + `doctor:disk-encryption` | ✓ |
| C-03 | "audit log stores diff hashes only; no secrets on argv" | README §Threats & mitigations | `internal/audit/audit.go` | ✓ |
| C-04 | "Funnel rejected at the schema level; listeners never bind `0.0.0.0`" | README §Threats & mitigations | `internal/config/config.go` | ✓ |
| C-05 | "the push payload carries only routing metadata — no body, no secrets, no code" | README §Notifications | `internal/notifyv2/message.go` + `internal/push/gateway.go` | ✓ |
| C-06 | "every mutation is backed up and recorded in a hash-chained, optionally-signed audit log" | README §Safety & auditability | `internal/audit/audit.go` | ✓ |
| C-07 | "SSH re-check period (`checkPeriod`) that can be lowered but never silently raised" | README §Networking & access | `internal/modules/ssh/module.go` | ✓ |
| C-08 | "Tailnet Lock by default — every device that joins must be cryptographically signed" | README §Networking & access | `internal/modules/lock/module.go` | ✓ |
| C-09 | "Secrets are read from stdin or the OS keychain; the audit log records titles and diff hashes only" | README §Security hardening | `internal/audit/audit.go` + `internal/cli/cmd_panic.go` | ✓ |
| C-10 | "`abysslink panic` tears down the VPN session, revokes the phone's auth key, and destroys the local API key" | README §Security hardening | `internal/cli/cmd_panic.go` | ✓ |
| C-11 | "The binary makes no outbound calls except to your VPN's local API and your own ntfy server" | README §Design philosophy | `doctor:no-telemetry` | ✓ |
| C-12 | "Every release carries a cosign-signed checksum manifest and a GitHub-attested SLSA L3 provenance bundle" | docs/why-abysslink.md §What Abysslink adds | `.github/workflows/release-build.yml` | ✓ |
| C-13 | "cosign signature + SLSA provenance verified on install/upgrade" | README §Threats & mitigations | `.github/workflows/release.yml` | ✓ |
| C-14 | "Every file mutation is backed up and appended to a hash-chained audit log" | docs/why-abysslink.md §What Abysslink adds | `internal/audit/backup.go` + `internal/audit/write.go` | ✓ |
| C-15 | "Rig ntfy topics generated by Abysslink carry a ≥128-bit random suffix; a derived topic below the floor is rotated on config write" | docs/security.md §Guarantees | `internal/config/topic_entropy.go` | ✓ |
| C-16 | "Ships in shadow mode by default — it observes and logs without interrupting until you arm it explicitly" | docs/why-abysslink.md §What Abysslink adds (agent kill-switch) | `internal/modules/budget/module.go` | ✓ |
| C-17 | "Disablement secrets are printed once to stdout and never written to disk" | README §Networking & access | `internal/modules/lock/module.go` | ✓ |
| C-18 | "Every mutation is backed up and recorded — nothing calls `os.WriteFile` outside `internal/audit`" | README §Design philosophy | `internal/audit/write.go` | ✓ |
| C-19 | "One DENY is a veto. No combination of ALLOW votes can override it" | docs/agent-safety.md §The quorum action gate | `internal/quorum/verdict.go` + `internal/quorum/quorum_test.go` | ✓ |
| C-20 | "A verifier error, timeout, panic, abstention, or low-confidence ALLOW escalates — never allows" | docs/agent-safety.md §The quorum action gate | `internal/quorum/quorum.go` + `internal/quorum/quorum_test.go` | ✓ |
| C-21 | "A compiled-in deny-floor runs before the lattice and before any approval token ... a valid approval token does not bypass them, and no YAML key exists to remove or disable them" | docs/agent-safety.md §The quorum action gate | `internal/quorum/floor.go` + `internal/gate/gate.go` + `doctor:sec-quorum-floor` | ✓ |
| C-22 | "Configuration is tighten-only ... Any loosening below the shipped defaults is a config load error" | docs/agent-safety.md §The quorum action gate | `internal/config/config.go` + `internal/config/config_quorum_test.go` | ✓ |
| C-23 | "Unanswered escalations deny. The approve loop's fail-closed timeout semantics are inherited unchanged: headless timeout denies, TTY timeout falls back to a terminal prompt, nothing auto-approves" | docs/agent-safety.md §The quorum action gate | `internal/approve/registry.go` + `internal/gate/gate_quorum_test.go` | ✓ |
| C-24 | "no LLM and no network call participate in the decision path" | docs/agent-safety.md §The quorum action gate | `internal/quorum/invariants_test.go` | ✓ |
| C-25 | "Every decision — enforcing and shadow — is appended to the hash-chained audit log as a full vote vector ... never raw argv, environment values, full paths, or capability URLs" | docs/agent-safety.md §The quorum action gate | `internal/quorum/audit.go` + `internal/quorum/quorum_test.go` | ✓ |
| C-26 | "Unanimous garbage consensus on a floor action still denies" | docs/agent-safety.md §The quorum action gate | `internal/quorum/floor_test.go` + `internal/quorum/adversarial_corpus_test.go` | ✓ |
| C-27 | "Everything short of that escalates to the existing phone/TTY approve loop at the demanded tier (sensitive: phone or TTY; critical: TTY-only in v4)" | docs/agent-safety.md §The quorum action gate | `internal/approve/tier.go` + `internal/quorum/quorum.go` | ✓ |
| C-28 | "the quorum covers every exec that passes gate enforcement ... Armed long-running agent spawns (`RunArmed`/`RunArmedMinimal`, i.e. `abysslink arm`) are deliberately outside it — they are covered by the budget watcher" | docs/agent-safety.md §The quorum action gate (D-01a/KILL-03) | `internal/gate/gate.go` + `docs/agent-safety.md` | ✓ |
| C-29 | "abysslink never silently falls back to a software key" | docs/security/hardware-keys.md §The fail-closed contract | `internal/hwkey/provider.go` + `doctor:sec-hwkey-kind` | ✓ |
| C-30 | "the file abysslink stores is a **handle**; the private key is generated inside, and never leaves, the Secure Enclave or the authenticator" | docs/security/hardware-keys.md §Why hardware keys | `internal/hwkey/enclave_darwin.go` | ✓ |
| C-31 | "`-O verify-required` is always set; **`-O no-touch-required` is not expressible** — there is no config field and no code branch that can emit it" | docs/security/hardware-keys.md §FIDO2 authenticators | `internal/hwkey/fido2.go` + `internal/config/config.go` | ✓ |
| C-32 | "authenticator PINs and passphrases are entered directly on your terminal and read by OpenSSH itself — abysslink never reads, stores, or transmits them" | docs/security/hardware-keys.md §Enabling | `internal/hwkey/enclave_darwin.go` + `internal/hwkey/fido2.go` | ✓ |
| C-33 | "`internal/attest` is **local-only by construction**: an import-allowlist test (`TestAttestNoNetworkImports`) fails the build gate if the package ever gains a network-capable import" | docs/security/hardware-keys.md §Boot-state attestation | `internal/attest/nonet_test.go` | ✓ |
| C-34 | "A missing tool, a permission error, or unrecognized output is WARN — never a false OK" | docs/security/hardware-keys.md §Boot-state attestation | `internal/attest/attest.go` + `doctor:sec-attest-sip` + `doctor:sec-attest-secureboot` + `doctor:sec-attest-tpm` | ✓ |
| C-35 | "Both ship **disabled**. Enabling them never weakens any existing default" | docs/security/hardware-keys.md §top | `internal/config/config.go` | ✓ |
| C-36 | "`abysslink status` reports `key_kind: hardware:<provider>` **only after** re-verifying the on-disk public key is sk-backed. An unverifiable key is `unknown`, never hardware" | docs/security/hardware-keys.md §The fail-closed contract | `internal/cli/cmd_status.go` + `internal/hwkey/classify.go` | ✓ |
| C-37 | "abysslink reads local boot state (SIP, Secure Boot variables, TPM PCR *presence*). It does not produce signed TPM quotes and cannot prove runtime integrity to a remote party" | docs/security/hardware-keys.md §Honest limitations | `docs/security/hardware-keys.md` + `internal/attest/doc.go` | ✓ |
| C-38 | "`abysslink up --apply` refuses when Tailnet Lock is not enabled on the live tailnet — the gate reads `tailscale lock status`, not config, so `tailnet.lock.enabled=false` cannot bypass it" | docs/security.md §Host-posture hardening | `internal/cli/cmd_up.go` + `doctor:sec-tailnet-lock` | ✓ |
| C-39 | "Using `--accept-lock-disabled` records an audited consent entry (`op=lock-override-consent`, `target=tailnet-lock`) with no secret and no message body" | docs/security.md §Host-posture hardening | `internal/cli/cmd_up.go` + `internal/audit/audit.go` | ✓ |
| C-40 | "If `tailscale lock status` cannot be determined (probe error, non-zero exit, or unparseable output), the gate fails closed and cannot be overridden even with `--accept-lock-disabled`" | docs/security.md §Host-posture hardening | `internal/cli/cmd_up.go` | ✓ |
| C-41 | "While FileVault encryption is in progress, `abysslink doctor` reports `sec-disk-encryption` as FATAL — mid-encryption is gated exactly like an unencrypted disk" | docs/security.md §Host-posture hardening | `internal/platform/darwin/platform.go` + `doctor:sec-disk-encryption` | ✓ |
| C-42 | "The in-progress state is matched before the `FileVault is On` prefix, so a disk mid-encryption is never misreported as encrypted, and unrecognized `fdesetup status` output is reported as UNKNOWN, never as encrypted" | docs/security.md §Host-posture hardening | `internal/platform/darwin/platform.go` | ✓ |
| C-43 | "The per-rig notification HMAC binds a fixed protocol domain tag, a format version, and the rig identity as length-prefixed fields, so a signature cannot be replayed across rigs or protocols even under a shared key; the comparison is constant-time (`hmac.Equal`)" | docs/security.md §Host-posture hardening | `internal/fleet/identity.go` | ✓ |
| C-44 | "`sec-tailnet-lock`, `sec-autologin`, and `sec-remote-login` are WARN by default and become FATAL under `abysslink doctor --profile at-risk`" | docs/security.md §Host-posture hardening | `internal/cli/cmd_doctor.go` + `doctor:sec-autologin` | ✓ |
| C-45 | "Build a signed, externally-verifiable evidence bundle (`.alevidence`) — hash-chained entries + a human-readable report + an ed25519-signed manifest attesting the chain-verification result." | docs/cli-reference.md §Operations | `internal/evidence/bundle.go` + `internal/cli/cmd_audit_evidence.go` | ✓ |
| C-46 | "It is **read-only** — the audit log is never modified." | docs/operations/audit-evidence.md §Producing a bundle | `internal/evidence/bundle.go` | ✓ |
| C-47 | "Any edit to any file — a reordered entry, a deleted action, a doctored report — breaks verification." | docs/operations/audit-evidence.md §Verifying a bundle | `internal/evidence/verify.go` | ✓ |
| C-48 | "Prints the signing-key fingerprint; `--expect-key` fails closed on a fingerprint mismatch." | docs/cli-reference.md §Operations | `internal/cli/cmd_audit_evidence.go:168` | ✓ |
| C-49 | "Verification fetches the audit log's own chain result from the signed manifest; it does **not** need the secret HMAC key, so anyone can run it." | docs/operations/audit-evidence.md §Trust model (read this) | `internal/evidence/verify.go` | ✓ |
| C-50 | "It is an honest attestation *by the operator*, not a zero-trust proof." | docs/operations/audit-evidence.md §Trust model (read this) | `internal/evidence/bundle.go` + `internal/evidence/verify.go` | ✓ |
| C-51 | "A bundle signed by any other key still verifies as *internally* consistent but presents a **different fingerprint** — which is exactly why the out-of-band pin matters." | docs/operations/audit-evidence.md §Trust model (read this) | `internal/evidence/verify.go` + `internal/cli/cmd_audit_evidence.go:168` | ✓ |
| C-52 | "Entering the decoy credential renders a benign rig view ... and, at the same time, fires the kill-switch: armed agents are frozen and killed and a persisted lockdown latch is set so nothing re-arms" | docs/security.md §Duress decoy | `internal/duress/duress.go` + `doctor:sec-duress` | ✓ |
| C-53 | "The decoy is non-destructive by design and by test. There is no code path that deletes, truncates, or overwrites real data; a destructive duress-wipe is an explicit anti-feature" | docs/security.md §Duress decoy | `internal/duress/nowipe_test.go` | ✓ |
| C-54 | "The credential comparison is constant-time over fixed-width digests ... reduced to a 32-byte argon2id digest before a crypto/subtle compare, and every candidate slot is evaluated with no short-circuit, so neither the credential length nor which slot matched leaks through timing" | docs/security.md §Duress decoy | `internal/duress/duress.go` + `internal/duress/duress_test.go` | ✓ |
| C-55 | "Duress credentials never live in config. Only a non-secret secret_source: keychain selector is stored in abysslink.yaml; the real and decoy credential digests live in the OS keychain" | docs/security.md §Duress decoy | `internal/config/config.go` + `internal/duress/duress.go` | ✓ |
| C-56 | "Duress activation is audit-logged without revealing which credential was used. The activation records a single generic hash-only entry (no decoy-vs-real discriminator, no credential body)" | docs/security.md §Duress decoy | `internal/duress/duress.go` + `internal/audit/audit.go` | ✓ |
| C-57 | "If duress is enabled but no decoy credential is resolvable, abysslink doctor reports sec-duress as FATAL (a false sense of safety is worse than none)" | docs/security.md §Duress decoy | `internal/cli/cmd_doctor_sec.go` + `doctor:sec-duress` | ✓ |
| C-58 | "The duress decoy defends only casual coercion (seconds-to-minutes); it does not hide the real config from a forensic adversary, and there is deliberately no destructive duress-wipe" | docs/security.md §Explicit non-goals | `internal/duress/nowipe_test.go` + `internal/config/duress_config_test.go` | ✓ |
| C-59 | "SENTINEL flags one specific, narrow, high-signal pattern: a file-reading tool touches a known-sensitive path and then, within a few commands and a short window, a known egress tool opens an outbound connection to a non-allowlisted host — in that order" | docs/agent-safety.md §The compromised-agent sentinel | `internal/modules/sentinel/rules.go` + `internal/modules/sentinel/engine.go` | ✓ |
| C-60 | "SENTINEL is defense-in-depth, not a security boundary … trivially evadable by a determined attacker; it reasons only about command names and arguments — never file contents, never network payloads" | docs/agent-safety.md §The compromised-agent sentinel | `docs/agent-safety.md` + `internal/modules/sentinel/doc.go` | ✓ |
| C-61 | "On the shipped benign baseline corpus the rules produced 0 false positives … the corpus is not stacked in the rules' favour: alongside the easy cases it includes the hard read-then-external-egress class most likely to false-fire … None fire" | docs/agent-safety.md §The compromised-agent sentinel | `internal/modules/sentinel/testdata/benign_baseline.json` + `internal/modules/sentinel/engine_test.go` (`TestSentinel_BenignBaseline_ZeroFP` computes the FP count from the corpus so the doc figure cannot drift) | ✓ |
| C-62 | "Every detection is written to the tamper-evident audit log (hash-only — no secrets, no raw paths, no host) so it appears in your evidence bundle for free" | docs/agent-safety.md §The compromised-agent sentinel | `internal/modules/sentinel/engine.go` + `internal/audit/audit.go` | ✓ |
| C-63 | "Quarantine (a reversible, non-destructive disarm-and-latch that reuses the dead-man lockdown) is available only when you explicitly opt in; it ships off because a false quarantine would kill your own agents" | docs/agent-safety.md §The compromised-agent sentinel | `cmd/abysslinkd/sentinel.go` + `internal/deadman/lockdown.go` | ✓ |
| C-64 | "The sentinel ships off by default and every knob is tighten-only (window bounds accept only values at or below the shipped defaults; the sensitive-path set and egress allowlist are add-only)" | docs/configuration.md §sentinel | `internal/config/config.go` + `doctor:sec-sentinel-enabled` | ✓ |
