# Summary

<!-- What this PR changes and why. Link the issue if one exists. -->

## Checklist

- [ ] PR title is a conventional commit (`feat:`, `fix:`, `docs:`, `test:`, `chore:`, `refactor:`)
- [ ] `make lint test` passes locally — output pasted below
- [ ] Every commit is signed off (DCO, `git commit -s`)
- [ ] No secrets in the diff
- [ ] Docs updated if behavior changed
- [ ] Security-relevant change flagged for a CLAIMS-AUDIT.md row update (or not security-relevant)

## `make lint test` output

<!-- Paste the tail of your local run inside the block. -->

```text
```
